# PRD — Portfólio de Raul Rodrigues

| | |
|---|---|
| **Versão** | 10.7 |
| **Última revisão** | 13/09/2026 |
| **Responsável** | Raul Rodrigues ([@RaulRodriguexz](https://github.com/RaulRodriguexz)) |
| **Status** | Aprovado — **fila de código encerrada e verificada em 13/09**. O último item de código saiu no `dc6ea61`, e a conferência seguinte não achou mais nada: nenhuma guarda morta em `src/`, nenhum texto visível fora do inglês, nenhum defeito de acessibilidade no botão de tema. **O que falta não é código** — é conferência no celular do Raul, o preview do link e publicar. Nada disso bloqueia: o site está no ar |
| **Prazo de publicação** | 10/10/2026 (embarque para Dublin: 26/10/2026) |
| **Repositório** | `portfolio-3d-v2` |

> ### ⚠️ Regra de manutenção deste documento
>
> **Este documento inteiro é escrito pelo Claude (Cowork)**, que audita o site
> em produção e a pasta `docs/`, e grava direto aqui — decisões novas, seção 0,
> changelog e inventário de movimento. O **Claude Code atualiza estado** —
> mover linha de "a fazer" para "concluído", marcar `✅` no inventário de
> movimento, e acrescentar o que foi de fato implementado ao final de uma
> decisão existente.
>
> **O Claude Code não reescreve nem reordena este documento**, não apaga
> decisões, não renumera IDs e não altera a estrutura das seções. **O alcance
> dessa proibição é este PRD**, porque aqui os IDs são referências que outros
> documentos citam. Num arquivo que ele foi encarregado de reescrever — como o
> `WORKFLOW.md` na D-47 — renumerar seções é parte do saneamento: deixar §1,
> §3, §5 e §6 seria cicatriz visível. Decisão antiga cujo
> **motivo** não estiver aqui está em `docs/DECISOES-ARQUIVADAS.md` — o ID e a
> decisão continuam neste documento, e **nada é arquivado enquanto dirigir
> trabalho pendente**. Ideia nova de
> escopo vai para `docs/BACKLOG.md`, decisão nova entra ao **final** do log com
> o próximo ID livre. Se algo aqui parecer errado, **aponte em vez de
> corrigir** — três vezes em 04/09 o que parecia erro era leitura apressada, e
> uma vez o documento estava certo e o diagnóstico é que estava errado.
>
> A seção 0 já ficou obsoleta quatro vezes em dois dias. Se você entregou algo,
> **atualize o estado dela na mesma sessão** — é a única linha de manutenção
> que é sua.
>
> **A data de uma entrada é a data do fato, não a do relógio de quem escreveu.**
> Quando as máquinas divergirem — e elas divergiram, o que deixou duas entradas
> do changelog datadas **no futuro** —, vale a **data combinada com o Raul**, e
> não o horário local da sessão. Na dúvida sobre que dia é, **pergunte em vez de
> carimbar**: data errada num log só é descoberta quando alguém tenta
> reconstruir a ordem dos fatos, que é exatamente quando ela custa caro.

> **Como ler este documento.** A seção 0 é o painel: estado atual e fila de
> trabalho. As seções 5 e 6 são o contrato: o que entra e o que não entra. A
> seção 14 é o log de decisões — nada muda sem passar por lá.
> Todo pedido de código feito ao Claude Code deve citar um `RF-xx` ou `RNF-xx`
> daqui. Se um pedido não encontra respaldo neste documento, o pedido está
> errado ou o documento está desatualizado; resolva isso antes de escrever
> código.

> ### 🔋 Como trabalhar sem queimar a sessão
>
> O Raul já perdeu **quatro sessões** por estouro de contexto. Numa conversa
> longa, **cada mensagem reenvia tudo o que veio antes** — não é uma coisa
> cara, é tudo somado e repetido a cada turno. As regras, por ordem de ganho:
>
> 1. **Sessão nova a cada bloco de trabalho.** É de longe o maior ganho, e é
>    seguro: **este documento é a passagem de bastão**, e a seção 0 diz onde
>    retomar. Nada se perde.
> 2. **Relatar em uma linha:** item, hash do commit, e a pergunta. **Nunca
>    colar o relatório inteiro do Claude Code** — o estado real está aqui.
> 3. **Pedir verificação dirigida**, não auditoria completa: seção 0 mais a
>    última decisão é onde o erro aparece. Auditoria inteira só quando algo
>    estiver visivelmente estranho.
> 4. **Não repetir medição que já veio no relatório.**
> 5. **Respostas curtas dos dois lados.**

---

## Changelog

> As versões **1.0 a 5.9** estão em `DECISOES-ARQUIVADAS.md`. Aqui ficam as da
> reta final, que é o que uma sessão nova precisa ler.

| Versão | Data | O que mudou |
|---|---|---|
| 10.7 | 13/09/2026 | **Última divergência do documento fechada, e ela era minha: a D-21 dizia "Reaberta" enquanto a §0 já a dava por entregue.** O card está no ar desde `78cf4d0`. Duas partes do PRD discordando por dois dias, e quem achou foi o Claude Code — **comparando o PRD com o repositório, não com ele mesmo**. **A assimetria vale guardar:** a v9.4 fechou o que não estava feito; esta deixou aberto o que estava. O segundo é o erro seguro **e ainda assim é erro** — status que não bate com a árvore faz a próxima sessão trabalhar duas vezes ou nenhuma. **Regra:** *status se confere contra o código, nunca contra outra parte do documento* — foi o documento concordando consigo mesmo que deixou o erro da v9.4 passar. **Adendo final na entrada 10 da §16, e é a parte acionável:** o sinal de alerta da falha que não responde não é *"deu erro"*, é *"está demorando mais do que deveria e eu ainda não sei por quê"* — porque **lentidão parece progresso lento**. **E uma confirmação do método, dita por ele:** o que impediu a troca do `aria-label` certo não foi o meu argumento, foi a **D-88 mandar diagnosticar antes de consertar e dizer o que fazer em cada ramo**. Se a decisão tivesse dito "troque o `aria-label`", ele teria trocado e quebrado um botão que estava certo. **É a regra da v10.2 funcionando na direção contrária:** lá ela impediu uma execução cega de lista; aqui impediu uma execução cega de diagnóstico. **Ciclo encerrado:** 80 arquivos versionados, zero guarda morta, zero componente sem uso, zero português chegando ao usuário, `origin/main` em `ff8fe21`, produção em `raulrodrigues.dev`. **Abertas e todas para depois de publicar:** D-86, D-82, D-76, D-61. |
| 10.6 | 13/09/2026 | **Código fechado, e o último achado virou não-achado com um argumento melhor que o meu.** A D-88 foi verificada em execução: `aria-pressed` em `ThemeToggle.tsx:70`, `true → false → true` em dois cliques, `data-theme` acompanhando — a frente 7 do D-44 entregou o que diz. **E o rótulo fixo `"Dark theme"` está certo.** Eu havia sugerido, na hipótese de mexer, nomear o controle (`"Theme"`); o Claude Code mostrou que produziria *"Theme, botão de alternância, pressionado"* — pressionado o quê? **Nomear o estado que o botão liga é o que dá sentido ao `aria-pressed`.** Ele também diagnosticou o próprio erro: avaliou o `aria-label` **isolado da junção**, que é a forma da D-79 com outra roupa. **Adendo na entrada 10 da §16:** o caro não foi a espera pendente, foi ter esperado 21 s e mais 15 achando que era lentidão. **É a única falha muda do catálogo que se disfarça de progresso** — as outras nove ao menos respondem, e resposta falsa pode ser contestada. **§0 sincronizada com a realidade:** três linhas da fila estavam velhas — a revisão do inglês (estado mudado pela D-74, não é mais bloqueante), a performance mobile (68 → **94** desde a D-62/D-63) e o 4º card (entregue na D-81). **O status do cabeçalho deixa de dizer que há bloqueante: não há.** O que falta são minutos de conferência no celular do Raul e publicar. |
| 10.5 | 13/09/2026 | **Fila de código encerrada de verdade: os dois resíduos saíram e nenhum pixel mudou.** A prova veio por **assinatura da página inteira** — tag, `left`, `top` absoluto, largura e altura de cada elemento, antes e depois, com a animação de revelação neutralizada por CSS nas duas capturas para medir **layout final e não quadro de animação**. 385 → 385 elementos a 1350 px, 374 → 374 a 390 px, altura idêntica nos dois. **As duas linhas que diferiram foram explicadas em vez de arredondadas:** a cortina do Intro e seu rótulo, mesma largura e altura, só deslocamento vertical — a cortina em quadros diferentes da saída. E o único texto diferente foi o relógio da Location, 06:52 → 06:53 IST. **Esse é o padrão de "pixel a pixel igual" do projeto daqui em diante.** **D-87 fechada:** varredura de português nos dois sentidos (792 literais no código, 255 nós e atributos no DOM) não achou **nenhum** texto visível a trocar; o que sobrou é identificador. **D-88 aberta**, e de propósito sem conserto: o `aria-label` fixo do botão de tema pode ser o padrão correto ou um defeito, **e o que decide é se o `aria-pressed` está lá** — que eu não consigo conferir da minha cópia. **Entrada 10 da §16, e é do Claude Code:** um probe dele esperou `load` numa aba já carregada e ficou pendente para sempre. **Forma nova no catálogo — as nove primeiras responderam errado, essa não respondeu.** Estado se lê; notificação se perde. |
| 10.4 | 13/09/2026 | **Faxina fechada em seis commits, e o defeito mais caro do dia foi meu de novo.** O `README.md` que eu dei por **escrito e commitado** estava só no disco: o GitHub seguia servindo *"Este repositório é um esqueleto"* para quem clicasse em *Code* no card do próprio portfólio. O Claude Code achou na **primeira** verificação, com `git status`. **Entrada 9 da §16 e quinta regra: arquivo escrito não é arquivo publicado** — o disco responde sobre o disco, não sobre o que está no ar. Somadas, as duas falhas mudas de hoje (a cópia velha de `src/` e o README não commitado) têm a mesma forma: **eu relatei estado sem conferir estado**. **Abertas D-84** (AGENTES.md → backlog) **e D-85** (a faxina). **Dois resíduos ficaram de fora por não estarem no aprovado, e entram na próxima:** `Stack.tsx:34` tem `{stackGroups.length > 0 && …}` em volta do marquee — a mesma guarda morta do `EmptyState`, 8 linhas — e o comentário do `Projects.tsx` ainda manda *"PASSO 5 — criar o componente `<ProjectCard />`"*, instrução para trabalho entregue há dias. **Ele também notou que o texto do `EmptyState` estava em português**, contra a regra 9. **Mapa do PRD medido:** 258,3 KB, dos quais **D-54+ = 134,0 KB (51,9%)**, changelog 44,5 KB, §0 26,2 KB, e as 53 decisões arquivadas apenas 12,3 KB. Média de **4.732 bytes por decisão nova contra 232 nas arquivadas — 20×** — e a D-73 sozinha (13.241 bytes) é maior que as 53 antigas somadas. **O que o número diz: o documento não cresce por ter muitas decisões, cresce por ter decisões longas**, e o corte de 07/09 funcionou justamente por mover o *motivo* e deixar uma linha. Segundo corte aberto na D-86. |
| 10.3 | 13/09/2026 | **O Claude Code recusou o primeiro item da faxina com medição, e estava certo.** Eu mandei apagar `EmptyState.tsx` dizendo que tinha **zero usos**; ele achou **três**, todos como o ramo `length === 0`. **A causa é a mesma família da D-71:** minha varredura rodou numa **cópia estagiada e velha de `src/`**, não no repositório — busca que termina com sucesso e devolve vazio, sem erro nenhum. Virou a **entrada 8 da §16** e uma **quarta regra**: *achar prova que existe; não achar só prova que não achou ali*, e antes de apagar por ausência de uso a busca se repete na árvore de onde sai o `build`. **O que muda de gravidade em relação às outras sete:** as anteriores apareceram em medição, esta apareceu **dentro de uma ordem de apagar arquivo** — se ele tivesse obedecido, o build quebrava. **Decisão mantida, motivo trocado (D-83):** apagar continua certo, mas não por ninguém usar — e sim porque **nenhum dos três usos pode acontecer** (os arrays têm 15, 4 e 18 itens vindos de dados estáticos), e o próprio arquivo pede para ser apagado no comentário. Ramo que nunca roda é ramo que ninguém testa. |
| 10.2 | 13/09/2026 | **Duas correções do Claude Code sobre o registro de ontem, e as duas foram aceitas.** **(1)** Eu havia escrito que a reordenação do array "consertou um erro meu". Ele devolveu: se a decisão tivesse dito apenas "ligue as duas flags", ele as teria ligado e entregado a hierarquia invertida sem perceber — **foi descrever o resultado esperado em palavras que tornou o mecanismo verificável**. Vira regra de escrita de decisão neste projeto: **dizer o RESULTADO em linguagem observável, não a lista de mudanças** — a lista alguém executa sem conferir, o resultado alguém confere. **(2)** Sobre as duas previsões falhadas, ele repartiu o crédito: a da altura caiu por coincidência aritmética, mas a de 390 px **ele tinha material para pegar** e não olhou. **Adendo na D-82 com um quarto caminho, melhor que os três meus:** mover a **fileira de tags** para a metade direita do destaque — é a única parte do card que **não é prosa**, então não briga com o `max-w-[30rem]`, não cria número grande (não colide com o D-34) e não mexe na grade. Continua para depois de publicar, mas agora com ponto de partida. |
| 10.1 | 12/09/2026 | **D-81 entregue: três linhas, zero espaço vazio, órfão eliminado.** E **das minhas duas previsões, as duas erradas, em direções opostas** — previ que a seção cresceria (não cresceu: 6.075 px idênticos) e que a 390 px nada mudaria (mudou 33 px, porque o badge `FEATURED` é novo neste card e empurra o cabeçalho de 28 para 89 px). O Claude Code mediu as duas e **disse que a previsão falhou em vez de deixar passar**. **Lição: previsão sobre consequência de layout é tão frágil quanto previsão sobre mecanismo.** Ele também corrigiu um erro meu sem precisar perguntar: **ligar as duas flags não produzia o layout que eu descrevi** — com a ordem antiga do array o trabalho pago continuava sendo a última coisa da seção, que era a hierarquia invertida que a decisão existia para consertar. **Eu escrevi o resultado visual e não o mecanismo.** Ele reordenou e conferiu que as 37 strings seguiram idênticas. **Aberta a D-82**, registrando sem consertar os **753 px vazios à direita dos destaques** — anterior a tudo, e o conserto óbvio (o número grande à direita) **colide com o D-34**, que só admite um número grande na página inteira. Fica escrito como decisão em vez de omissão. |
| 10.0 | 12/09/2026 | **Quarto card no ar (D-21) e o órfão apareceu exatamente como o D-14 previa — com o agravante de que o órfão era o card mais importante do site.** Medido a 1440 px: o trabalho pago, pago e de cliente real, sozinho na última linha com 661 px vazios ao lado. O Claude Code mediu e **não escolheu**, e a observação dele fechou a análise: reordenar o array não resolve, só troca quem fica órfão. **Aberta a D-81: dois destaques e dois compactos.** As linhas fecham e o órfão some **por construção**. O segundo destaque é o card novo, e não por geometria — depois da D-78 o site se apresenta como **AI Solutions Engineer**, e em destaque têm de estar os dois trabalhos que provam que ele resolve problema de alguém: o Cover Letter, público e clicável, e a automação paga. Titanic e o próprio site viram compactos — são credibilidade técnica, não prova de negócio. **Isso revisa o D-14 explicitamente:** ele fixou "um destaque, dois compactos" quando havia três projetos e nenhum era pago; **a premissa mudou, a regra muda junto**. |
| 9.9 | 12/09/2026 | **A D-21 foi reaberta: ela nunca foi entregue, e fui eu que a fechei por engano.** Na faxina de status da v9.4 eu a fechei **por atacado, sem conferir** — a mesma falha que eu tinha acabado de acusar o documento de ter. O site segue com **três cards, todos projetos pessoais**, e para quem se apresenta como AI Solutions Engineer isso mostra três trabalhos de estudo e **zero prova de que alguém pagou**. O que a travava desde 03/09 era o número de impacto, e o Raul o deu hoje: **leads parados até duas semanas**. Conteúdo do quarto card escrito e pronto na decisão, **sem repo nem demo** — é cliente, o código não é público, e isso tem de ler como escolha. **Risco a medir antes:** o D-14 fixou dois cards compactos numa grade de duas colunas; com três, **um fica órfão numa linha**. **Aberta e fechada a D-80:** duas rodadas de pergunta evitaram um erro grave — a pesquisa dizia que o programa do CAMP tem carteira assinada, e eu ia escrever "empregado registrado" num documento que vai para empregador. O Raul corrigiu: **recusou a colocação CLT**, saiu com os certificados e foi trabalhar por fora. **Fato sobre a vida de alguém não se deriva de documentação — pergunta-se.** |
| 9.8 | 12/09/2026 | **Escrita a seção 16 — o catálogo de falhas mudas.** O Claude Code notou que a quarta armadilha de instrumento (`innerText` respeitando `text-transform`) é da **mesma família** do `background-color` no `<body>` e dos documentos velhos na pasta de cima: coisas que **não quebram nada** — build verde, lint limpo, zero erro — e devolvem **um resultado bem-formado e falso**. Contadas, são **sete ocorrências** no projeto. Ganharam seção própria com o mecanismo de cada uma, como foi pega, e as três regras que saem daí. **O padrão por trás das sete, e é o que vale levar para os próximos projetos:** em todas, alguma coisa **respondeu com sucesso a uma pergunta ligeiramente diferente da que foi feita** — o canvas respondeu sem a camada WebGL, a busca respondeu sobre o texto original e não o renderizado, o `build` respondeu sobre tamanho e não sobre download. Registrado também que **o número errado da D-79 era do Claude Code e ele o assumiu sem ser perguntado** — 339 px vindos de um `span` sintético com `nowrap` medido numa janela de 1920 px, apresentado como largura de rótulo num celular. |
| 9.7 | 12/09/2026 | **Auditoria de fechamento: a fila de código está vazia e quatro decisões foram encerradas.** **D-79 entregue — e a alínea (b) estava construída sobre um número errado.** O controle mostrou que o rótulo de setores **já ocupava duas linhas antes** da troca: a quebra nunca foi causada por ela, e a decisão **herdou uma medição ruim sem questioná-la**. Regra nova, e é diferente das anteriores: **hipótese errada a medição derruba; número errado a medição confirma**, porque ninguém remede o que já foi medido — um número herdado de outra rodada é dado de segunda mão e se remede antes de virar justificativa. Registrada a **quarta armadilha de instrumento** da série: `innerText` respeita `text-transform: uppercase`, então uma busca sensível a caixa teria "provado" que o cargo não estava no hero. **D-66 concluída, e não como previsto** — o que chegou não foi informação nova e sim **correção da que existia**: quatro clientes reais, logística nunca existiu e estava publicada, e leads parados até duas semanas. A lição: **"me diga os clientes e um número" destravou em uma mensagem o que "o que mudou?" não destravou em cinco dias.** **D-70 descartada por ausência de reclamação** — cinco dias olhando o site todo dia, reclamando de quatro outras coisas e nunca da borda; silêncio de quem olha diariamente é evidência. **D-77 parcialmente entregue**, restando só o que depende de informação: as certificações e a empresa do CAMP. |
| 9.6 | 12/09/2026 | **D-78 entregue, e o Claude Code achou três lugares que a decisão não listou — todos com consequência real.** O **`og:title`** ainda dizia `Developer`, e é o título do card no LinkedIn, ou seja **onde o recrutador olha primeiro**; o **`sectionMeta.about`** era um segundo lugar com os setores e guardava o `Dental` que a D-74 tirou da prosa **um dia antes**; e a **seção 3 deste documento** — persona viva, não log — listava `transporte`. Ele corrigiu as três e **deixou as outras cinco menções intactas de propósito**, para não apagar o registro de onde o erro entrou. **Regra nova, na terceira mordida da mesma coisa: quando um fato muda, não se enumera de memória onde ele aparece — procura-se.** Um fato mora em mais lugares do que qualquer decisão consegue listar. **E o achado que dói:** a **seção 2 diz desde a primeira versão** "posicionar o Raul como AI + negócio, não como mais um dev front-end" — o site foi ao ar como `Developer` e ficou nove dias assim. Não faltou decisão: **faltou ler a que já estava escrita.** **Aberta a D-79** com duas arestas que só existem onde as strings se encostam: o rodapé repetindo "AI Solutions" (conserto: campo `availabilityShort` próprio, em vez de o rodapé recortar a string grande) e o rótulo de setores dobrando para duas linhas no celular. |
| 9.5 | 12/09/2026 | **Aberta a D-78 — o site deixa de dizer "Developer".** O Raul já tinha decidido perseguir **AI Solutions Engineer** e **Growth Engineer**, e registrado que não quer "developer"; **o site nunca soube** e continuou se apresentando com o posicionamento antigo. Escolhido **`AI Solutions Engineer`** como título único nos três lugares — site, CV e LinkedIn —, com growth no perfil: é o que recrutador procura em Dublin, enquanto `Growth Engineer` por lá costuma cair em martech e descreve menos do que ele faz. Escritas as quatro trocas exatas, incluindo a que se esquece: **o `jobTitle` do JSON-LD**, que é como o Google diz o que ele é e não aparece na tela. Registrado também que trocar só o `role` deixaria o site se contradizendo, porque a palavra `developer` está **dentro** do subtítulo. **CV reescrito em uma página, padrão europeu**, com o site no topo, três números reais e o bloco `Growth` que não existia. Continuam pendentes de informação do Raul, e **não foram inventados**: o nível real de inglês e o conteúdo das três certificações. |
| 9.4 | 12/09/2026 | **Auditoria de senior no próprio documento: a coluna de status estava mentindo em 15 linhas.** Decisões entregues e em produção há dias continuavam marcadas `Aberta` ou `Ativa` — D-44 (modo escuro, 7 de 7 frentes), D-58, D-59, D-62, D-63, D-69, D-71 e outras. **Quem lesse o log para achar trabalho pendente encontraria quinze falsos positivos**, e o log é exatamente o lugar onde se procura isso. Todas passaram a **Concluída**. A **D-64** virou **Substituída**, com o motivo escrito: o pacote de revisão nunca foi gerado porque o revisor humano caiu (D-74), e ele existia para entregar texto a quem não abre o código — fica registrada, não apagada, porque a ideia volta se um revisor aparecer depois de publicar. **Aberta a D-77:** o `cv.pdf` foi auditado contra o texto que está no ar e **conta uma história diferente e pior** — oito divergências, sendo a mais cara que **o CV não traz o endereço do site**, construído justamente para ser a prova de que ele entrega. O cargo aparece em três versões, o perfil é a versão corporativa do que o site diz melhor, o CV manteve o `ecosystem` que a D-74 tirou, e **a experiência não tem um único número**. Registrada a ordem que vale daqui em diante: **CV e LinkedIn saem do site**, porque o site é o único dos três que passou por revisão e por medição. |
| 9.3 | 11/09/2026 | **Quatro decisões fechadas e no ar, e duas delas corrigiram quem as escreveu.** **D-73:** o diagnóstico de "terra faltando em 25 linhas" estava **errado** — um detector melhor achou uma **fronteira de inversão na linha 142** com 98,10% dos pixels trocados e o complemento a 1,90%: era **paridade invertida**, não falta, e são **43 linhas**. A diferença não era acadêmica — copiar das vizinhas teria **apagado geografia real** em 32 linhas que só precisavam ser invertidas de volta. Pontos de controle 6 → 0, arco sumiu, **pin cravado na costa leste da Irlanda**. **D-75:** o Claude Code achou um erro **no meu critério de aceite** — "selecionar e colar" já passava antes do conserto, porque o serializador do `getSelection()` quebra blocos sozinho; o defeito real estava no `textContent` (79 → 39 trechos). **Um aceite precisa medir a mesma coisa que o argumento afirma.** **D-72 entregue sem crescer o canvas**, com degrau de **0,000000** na fronteira, e a decisão de não crescer registrada com motivo: o terço de cima é onde mora o nome, e partícula atrás dele compete. **D-74 aplicada**, e o item 11 decidido: `138 KB over the wire on a phone…` — métrica que o visitante de fato baixa, conferível no devtools e que **não envelhece a cada commit**. **Aberta a D-76:** a largura de leitura tem **três números** no documento (74, 75, 82) para a mesma grandeza — dívida de documento, não defeito de produto, e fica para depois de publicar. Registrado o achado de ritmo: `xhigh effort` nas rodadas de diagnóstico, normal nas de aplicação. |
| 9.2 | 11/09/2026 | **D-73 resolvida, e a causa é a melhor descoberta do projeto: o arco não era objeto da cena, era a TEXTURA.** Por eliminação — o arco sobrevive sem atmosfera, sobrevive sem o marcador e some sem os continentes; o anel de radar caiu por geometria antes mesmo da captura (0,141 contra 1,6 de raio). O `world-dots.png` tem **três cicatrizes horizontais** (linhas 105–112, 128–141 e 604–606), pintadas e renderizadas caem exatamente sobre o arco, e **uma linha horizontal em textura equirretangular é um paralelo inteiro na esfera** — 13 pixels viram uma faixa dando a volta no mundo. A explicação estava no `public/images/README.md` o tempo todo: varredura par-ímpar, e vértice em cima da linha **inverte a paridade**. **Lição: o defeito nasceu na geração do dado (D-58a), e três rodadas olharam para a cena porque o sintoma aparecia lá — quando o sintoma é visual, o dado também é suspeito.** Conserto proposto e não aplicado, como a decisão manda. O degrau horizontal ficou como **não reproduzido**, não como inexistente. **D-72 medida, e a medição derrubou a minha descrição:** a aresta é **1,19% de Weber**, invisível — o que se vê é o **campo de estrelas cortado em retângulo**, não um degradê cortado. Logo a primeira tentativa passa a ser **fazer as partículas sumirem antes da borda**, não aumentar o canvas: resolve sem tocar em enquadramento, sem o Memoji crescer 1,49× e sem compensação de câmera. **Aberta a D-75:** um corretor gramatical real leu a página e **os falsos positivos dele são o defeito** — `ProblemJob`, `BuiltA`, `JavaScriptOpenAI APICloudflare` são rótulos colando no texto. O D-45 tinha aceitado isso por teoria; uma ferramenta de verdade errando é evidência nova. |
| 9.1 | 11/09/2026 | **O RNF-09 mudou de estado e o lançamento deixou de ter item bloqueante.** O Raul dispensou o revisor humano e definiu o alvo com precisão: *"não precisa ser exatamente como o irlandês; estamos vindo do Brasil, tenho que fazer bonito, mas não precisa estar 100%"*. Isso muda um requisito, então mudou **por escrito** (D-74) e não por omissão — a D-66 já tinha previsto exatamente esta bifurcação. A revisão foi feita e está em **`docs/REVISAO-INGLES.md`**: **11 pontos, só 3 são erro de fato** — `dental` como nome de setor, `turns a task into under a minute` e `the website` sem antecedente. Os outros são frase que soa traduzida, com destaque para `I work where things are ambiguous`, literal e repetida duas vezes. **Achado que não é de inglês:** o card "This site" afirma **70 KB** de JavaScript, número anterior à D-62 e à D-63 — hoje são 64,17 KB crus e 138 KB na carga completa. Número errado num portfólio é o que um entrevistador confere. |
| 9.0 | 11/09/2026 | **Duas reclamações de aparência do Raul, olhando o site rodando: D-72 e D-73.** **D-72 — o campo de estrelas dentro de um quadrado.** Confirmado na captura: há uma **aresta horizontal dura** onde o brilho e as partículas terminam. A causa é estrutural — o WebGL pinta dentro dos limites do canvas, e o `Backdrop` põe o fundo **dentro da cena**; onde o canvas acaba, o degradê acaba, e isso vira linha. Escolhida a saída que o próprio Raul sugeriu: **a cena cobre o hero inteiro e decai até o fundo da página nas bordas** — o retângulo continua existindo, mas perde a aresta. A alternativa (canvas transparente, brilho em CSS) mataria a paralaxe das partículas. **D-73 — o risco no globo voltou**, e a captura tem **duas coisas diferentes**: um **arco** cruzando o planeta e uma **descontinuidade horizontal**. A segunda é suspeita de ser da ferramenta: pelo D-19 o globo **cresce com a rolagem**, e captura de página inteira é montada em faixas, cada uma com o planeta num tamanho — a emenda vira degrau. **Regra: capturar UMA viewport parada, nunca página inteira.** O arco é outra coisa e não tem explicação: a malha saiu no `09e5abe`. Ordem de **isolar por eliminação incluindo o anel de radar do marcador**, que é o único objeto circular restante — e a ordem vem com o registro de que nesta família de problemas eu já errei **três hipóteses seguidas**. |
| 8.9 | 11/09/2026 | **Os aceites da D-69 que dependiam de navegador foram medidos pelo Cowork em produção — e o processo quase produziu um defeito falso.** Carga limpa a **360 px**: nenhum `<svg>` do fio no DOM, 0 `canvas`, grão presente, `<body>` transparente, sem rolagem horizontal. Carga limpa a **1440 px**: fio de volta com 6247 px e as duas cenas 3D montando. **O caminho de arrastar a janela não pôde ser medido:** na primeira leitura o fio parecia sobreviver a 360 px, o que contradizia a promessa do `useTelaLarga` — mas instrumentar o instrumento mostrou que **a emulação de viewport do painel não dispara `resize` nem `matchMedia change`**: `innerWidth` foi de 360 a 1200 com **zero eventos**. O código nunca foi avisado. **Terceira vez que a ferramenta engana** (depois do `clip` sem WebGL no D-65 e da calibração do headless no D-63), então vira regra escrita: **resultado que contraria o código é suspeito do instrumento antes de ser defeito.** Registrado também que o Claude Code fez certo ao entregar a D-69 dizendo que tinha **garantia estrutural e não medição** — a distinção é o que permitiu fechar isto sem refazer trabalho. |
| 8.7 | 11/09/2026 | **Aberta a D-71: cópias obsoletas dos documentos estão soltas um nível acima do projeto, e isso é uma falha que não grita.** Numa captura de 11/09 o terminal do Claude Code estava em `Documents\Recomeço` ("not a git repo") e ao lado dele havia **`PRD.md` de 24 KB datado de 02/09** e **`WORKFLOW.md` de 18,6 KB**, a versão de 520 linhas que a D-47 substituiu. O PRD real tem 173 KB e vive em `portfolio-3d-v2\docs\`. Pedir "leia o PRD.md" naquela pasta devolve um documento **coerente, bem escrito e nove dias errado**, sem erro e com build verde — mesma família do `background-color` no `<body>` e dos 237 KB do RNF-02. Os quatro soltos saem, e entra no `CLAUDE.md` a regra do diretório de trabalho. **Segunda vez que esta pasta morde:** a primeira foi o `Missing script: "dev"`, que ao menos gritava. |
| 8.6 | 07/09/2026 | **D-65 fechada, e a parte (d) rendeu o melhor achado de mecanismo do projeto.** O aro é a **atmosfera**, provado por perfil radial em quatro estados × dois temas — e a costura entre as esferas ficou descartada **com prova**, não por eliminação preguiçosa. **Eu estava errado no mecanismo:** escrevi que a casca `BackSide` "acumula brilho na silhueta"; não acumula — `meshBasicMaterial` não tem fresnel, então ela pinta um **disco chapado de borda dura**, e a diferença importa porque acúmulo se conserta com opacidade e disco se conserta com **raio**. Registrado também o efeito que o modelo previu antes da medição: no tema claro a atmosfera **escurece**, porque o alfa do `AdditiveBlending` também acumula e o canvas é composto sobre a página — previsto −0,120, medido −0,1165. Conserto em `89ed004`: raio 1,14 → 1,035, degrau −116,5 → −67,4 no claro e +3,07 → +1,82 no escuro. **O aceite passou no escuro e não passou inteiro no claro, e está escrito assim** — sobrou uma borda de 6,7% de Weber em 12 px, que é o teto de "opacidade e raio" com material chapado. Abriu a **D-70**, que só existe se o Raul ainda enxergar a linha. **Achado de método:** o `clip` do `captureScreenshot` devolve o quadro **sem a camada WebGL** e quase produziu a conclusão falsa de que não havia aro — vira regra: **instrumento novo se calibra contra resultado conhecido antes de valer como prova**. Confirmado ao Claude Code que o favicon modificado foi o Cowork (D-68) e **pode entrar** — ele fez certo em não commitar mudança de identidade de carona. **Faxina:** as tabelas do Lighthouse e a lista de requisições do RNF-02 saíram da seção 0 para o arquivo, reduzidas a seis linhas com os números que importam: são prova cumprida, não trabalho pendente. |
| 8.5 | 07/09/2026 | **Aberta a D-69: o fio roxo não é montado no celular.** O Raul viu que numa tela estreita ele "perde o clima", e o motivo é de desenho: o fio nasce da **geometria medida dos rótulos** e existe para costurar uma coluna larga pela margem que sobra — **a 360 px essa margem não existe**, e o traço que passava por trás do conteúdo vira um risco atravessando o texto. Limiar escolhido em **640 px, o mesmo do `useCanRender3D`**, para o projeto ter **uma** definição de tela pequena e não duas. **Não montar, não esconder por CSS** — `display:none` deixaria `ResizeObserver` e `rAF` rodando no aparelho mais fraco para desenhar o invisível, que é a mesma lição do D-62 e do D-63: o custo não é o que aparece, é o que roda. Registradas as duas armadilhas: mexer nessa camada **não pode** devolver `background-color` ao `<body>` (o grão sumiria junto, sem erro), e o ponto do **M-28** sai junto do fio, porque ele lê a ponta do próprio `<path>` e sozinho viraria um ponto solto. |
| 8.4 | 07/09/2026 | **D-68 resolvida com o desenho pronto, e a renderização em tamanho real derrubou a minha ordem de preferência.** Desenhei as três propostas e as vi a 16, 20, 32 e 64 px, claro e escuro, dentro de uma aba simulada: **a 16 px massa vence linha**. O anel e a órbita — que eu tinha posto em primeiro por serem a assinatura do site — viram fiapo; a **faísca**, que era o pedido do Raul, é a única com massa. **Ele estava certo e eu estava errado, e o erro tem nome: escolhi pelo significado em vez de escolher pelo tamanho em que a peça é vista.** Escolhida a **B3** depois de quatro tentativas (a B4, com segunda faísca, foi rejeitada porque a segunda vira sujeira a 16 px), com troca de cor por `prefers-color-scheme` dentro do SVG — 490 bytes. **Corrigida a alínea (c) da própria D-68:** o laptop **fica** no cabeçalho. Símbolo e favicon iguais era regra minha e está errada aqui — o laptop não sobrevive a 16 px, e a faísca não significa nada no tamanho em que há espaço para significar. Duas peças, cada uma na escala em que funciona. **D-67 descartada** a pedido do Raul, e fica registrada com a nota de que a reclamação nasceu de uma contradição minha: eu listei "arrastar o globo com o dedo no celular" numa conferência de lançamento, e o globo não existe no celular desde o RNF-04. |
| 8.3 | 07/09/2026 | **Abertas a D-67 e a D-68, das duas reclamações do Raul olhando o site, e em duas delas eu discordo dele por escrito.** **D-67 — o globo some no celular:** o sintoma é real e o comportamento está certo. A recusa da cena abaixo de 640 px é o que comprou os **94 de Lighthouse**, então a saída não é ligar o 3D e sim repetir o padrão que o hero já usa — **imagem estática**, uma por tema porque as cores invertem, com o globo **assentado em Dublin**, `alt` descritivo e dimensões explícitas para não reintroduzir deslocamento numa página com **CLS 0**. **D-68 — a marca:** o "R" branco em quadrado roxo é genérico e sai, mas **estrela de cinco pontas é o pior substituto** — a 16 px ela empasta e lê como "favorito". Duas propostas: **anel com ponto de luz** (a assinatura do próprio site, nas órbitas e no M-28) ou **faísca de quatro pontas**. E **o nome fica no cabeçalho**: ele é fixo, é a única coisa que permanece na tela enquanto a pessoa rola, e o objetivo do site é que o recrutador **lembre o nome** — símbolo sozinho não comunica isso, e trocar o nome pela URL gastaria o único espaço permanente repetindo o que a barra do navegador já mostra. **Domínio principal confirmado** no painel: apex em Produção, `www` redirecionando. |
| 8.2 | 07/09/2026 | **Aberta a D-66 e a fila reordenada: conteúdo novo ANTES do pacote de revisão.** O Raul traz informação nova de trabalho em 08/09 à tarde, e a D-64 gera o pacote **a partir do texto que está no ar** — gerar hoje e trocar amanhã jogaria o pacote fora e criaria a chance de o revisor receber a versão velha. Ordem: **D-66 → D-64 → revisão**. Escrito na D-66 o que texto novo obriga a remedir e ninguém lembra: a largura de 74 caracteres por linha, o recorte do `WordReveal`, o fio do M-25 (gerado da geometria dos rótulos), a ênfase de uma por parágrafo, e os **três lugares fora do `src/data/`** que descrevem a mesma pessoa e envelhecem calados — JSON-LD, `og:description` e `<title>`. Registrado sem rodeio que **correção própria do inglês não cumpre o RNF-09**: o requisito existe para pegar o que **soa estrangeiro**, não o que está gramaticalmente errado; se o Raul optar por publicar sem revisor, o requisito muda de estado **aqui**, e não por omissão. **Faxina de senior:** a seção 0 tinha **duas filas para o mesmo trabalho**, com o domínio principal listado como "a fazer" nas duas — duas listas é duas verdades. Ficou a que tem dono e tempo; os 13 itens antigos e o bloco que anunciava a fila "encerrada" enquanto a linha de baixo dizia que ela reabriu foram para o arquivo. Marcado como concluído o arrasto no desktop, que o Raul confirmou na tela. |
| 8.1 | 07/09/2026 | **A malha era uma das causas, não a única: sobrou uma linha CONTORNANDO o globo.** Aberta a parte (d) da D-65 com **ordem de isolar por eliminação** — sobraram três camadas, então quatro capturas nos dois temas dizem qual perde o traço, e só então se propõe conserto. A ordem é explícita porque nesta mesma decisão eu escrevi duas hipóteses e a medição derrubou as duas; teorizar de longe já custou uma rodada. Suspeitos nomeados **como suspeitos**: a **atmosfera** (`BackSide` + `AdditiveBlending` acumula brilho na silhueta, e o raio 1,14 dá a esse acúmulo um contorno concêntrico e maior que o planeta — e ela não é remedida desde o D-58a e o D-60) e a **costura** entre o corpo em 0.995 e os continentes em 1.0. Se for a atmosfera, o conserto tende a ser **opacidade e raio, não remoção**: ela dá volume à bola, ao contrário da malha, que era enfeite puro. **Confirmado pelo Raul: o assentamento em Dublin voltou** — 5 a 6 s, que é o `livre` vencendo mais o `REST_DELAY`, como projetado. |
| 8.0 | 07/09/2026 | **D-65 (a) entregue (`09e5abe`) e a causa de (c) encontrada — não era nenhuma das duas hipóteses que eu tinha escrito.** A malha saiu inteira e o contraste **subiu** nos dois temas: claro 3,77 → 4,53:1, escuro 4,13 → 4,94:1. Já o assentamento: o Lenis **não** carimba em repouso (0 eventos de scroll em 15 s) e o alvo em X **centraliza sim** (pin a 0,0 px do centro) — as duas hipóteses derrubadas por medição. **A causa é o `onDown` carimbar `lastInteraction` antes de saber se o gesto vira arrasto:** cada clique parado adia o assentamento em 10 s, e `6,1 + 10,0 = 16,1` fechou exato na corrida com dois cliques. O relato do Raul se explica inteiro, e com ironia: ele clicava no globo **para acordá-lo**, e era o clique que empurrava a volta. **Decidido que o clique parado LIMPA o `livre` e o carimbo FICA** — pelo D-55 rolar já significa "volta a Dublin", clicar passa a significar o mesmo, e o carimbo mantido vira a folga de 1,5 s que faz a volta parecer decidida em vez de seca. |
| 7.9 | 07/09/2026 | **A fila de código reabriu, por duas reclamações do Raul que a captura mostrou serem o mesmo objeto: a D-65.** A "listra roxa no meio do globo" e o borrão no polo são os dois a **malha *wireframe***: quando o globo assenta em Dublin ele inclina ~53°, e nessa inclinação **o anel de latitude na altura do olho é visto de perfil — e um círculo de perfil é uma reta**; a mesma inclinação vira o polo norte para a câmera e põe a convergência dos meridianos no centro do quadro. A malha **sai inteira**, com a linha dela na paleta: entrou no D-56 para imitar a referência, o D-58a passou a entregar esse desenho com continentes sólidos, e ela custa **0,42 de contraste** para produzir dois artefatos. Reaberto também o **assentamento**, com duas hipóteses a medir e a ordem de **não chutar** — o `LIVRE_TIMEOUT` de 10 s está no código, então ou algo carimba `lastInteraction` sozinho (Lenis), ou o alvo em X leva Dublin à altura do olho sem levá-la ao centro. **Documentos:** corrigidos no `ARCHITECTURE.md` os dois desencontros que o Claude Code mediu — a ordem real do `<head>` (o preload é o **último**, não o segundo, e é essa ordem que deu os 94) e o **`useMagnetic`** que faltava na lista de hooks. O **`CONTENT.md`** ganhou tarja de **HISTÓRICO**: seis campos no ar nunca existiram nele e três passagens foram reescritas depois da aprovação, então o pacote da D-64 sai de `src/data/` e nunca dali; o andaime de decisão — os conflitos e as opções de headline, **dois deles ainda marcados "decisão sua" dias depois de decididos** — foi para o arquivo. |
| 7.8 | 07/09/2026 | **Varredura de pré-lançamento feita em produção, e ela fechou três itens do §10 que estavam abertos por falta de medição, não por defeito.** (1) **RNF-10 — zero cookies:** relido com o Web Analytics **já ligado e servindo**, que era exatamente a dúvida que sobrava; `document.cookie` vazio. (2) **RNF-05 — 360 px:** remedido depois do modo escuro e da troca de fonte, `scrollWidth` 360 contra `clientWidth` 360, e **0 `canvas`** montado nessa largura, que confirma o RNF-04 no ar e não só no código. (3) **Links externos:** os 12 conferidos, com `/cv.pdf` respondendo 200 e a página do Cover Letter Assistant **aberta**, não só pingada — um link de projeto quebrado no portfólio é pior que não ter o projeto. Conferidos de passagem: `lang="en"`, um `<h1>` só, **zero imagens sem `alt`**, e a rota inexistente devolvendo **404 de verdade**. Refeita a lista de encerramento da §0 como **plano de execução com dono e tempo**: o que sobra são ~20 minutos do Raul mais a revisão do inglês, que é de terceiro. |
| 7.7 | 07/09/2026 | **A faxina passou pelos outros documentos, e três estavam mentindo.** No `ARCHITECTURE.md`: as fontes ainda eram descritas como "self-hosted **quando** sair do Google" (saíram na D-62 a); os tokens eram **"sete, e só eles"**, mas o modo escuro criou um oitavo, `on-primary`, porque no escuro o `primary-deep` é claro e `text-white` sobre ele caía a 2,1:1; e o `index.html` e o `vite.config.ts` **não apareciam na árvore**, apesar de carregarem hoje o script anti-flash, o preload da fonte e a ordem de chunks que **é** o RNF-02. Escrita ali a lição da D-63, no lugar onde alguém vai lê-la antes de mexer: **chunk próprio não é o mesmo que não ser baixado**. No `BACKLOG.md`: o PT/EN constava como cortado pela D-06 quando a **D-61 o trouxe de volta**, os links magnéticos estavam na fila e foram entregues como **M-27**, e as fontes self-hospedadas ainda eram "a fazer". Acrescentada a regra de que ideia entregue fica **riscada com o ID**, não apagada — e de que item cortado pode voltar, o que já aconteceu duas vezes. Respondida a observação do Claude Code sobre manter dois arquivos em sincronia: **o arquivo é congelado**, nada nele se edita, e a regra está no topo da §14. |
| 7.6 | 07/09/2026 | **Faxina estrutural, feita no dia em que a fila de código fechou — que é o momento mais barato, porque nada mais se mexe.** O PRD estava com **208 KB**: um documento de passagem de bastão que custava boa parte de uma sessão só para ser lido, ou seja, o instrumento gastava o que foi criado para economizar. Criado o **`DECISOES-ARQUIVADAS.md`**, para onde foram o **motivo** das decisões **D-01 a D-53** (a parte longa, que é história), o **changelog 1.0–5.9** e os **registros de sessão de 05–07/09** que ainda moravam na seção 0. **Todos os IDs continuam no PRD**, com a decisão inteira e o status, então nenhuma referência quebra. **PRD: 208 KB → 136 KB.** Corrigidas as **7 datas 08/09** que sobravam fora do changelog — inclusive a linha "onde a próxima sessão pega o trabalho", que é ponteiro vivo e estava datada no futuro, e os dois cabeçalhos das tabelas do Lighthouse, que são afirmação sobre quando a medição rodou. **O achado que mais importava não era o tamanho:** a seção 0 acumulava registros de sessões anteriores que apontavam como "próximo item" coisas entregues no mesmo dia — histórico dentro do painel não é contexto extra, é informação errada esperando alguém acreditar nela. Corrigido também um `##` órfão ("A fila de código está encerrada") que quebrava a numeração das seções, virando `###` dentro da seção 0. |
| 7.5 | 07/09/2026 | **Aberta a D-64 — o pacote de revisão do inglês**, porque o único item que trava o lançamento é o que menos anda, e o motivo é de desenho: pedir a alguém que "revise o inglês do meu site" é uma hora de trabalho sem formato, e por isso fica sempre para depois. O pacote transforma isso em **quinze minutos com uma lista numerada**. **Consertado um buraco no painel:** a tabela de encerramento da §0 listava **quatro** pendências, e duas outras — a releitura da aba Application (RNF-10) e a passada final de Tab (RNF-06) — viviam só na §10, ou seja o painel não era a lista completa e dependia de alguém lembrar delas de cabeça. Agora as seis estão numa tabela só, **ordenadas por tempo de espera**, com dono e com a resposta de "trava ou não trava". **Registrado o achado de estado:** o PRD v7.4 estava escrito no disco e **nunca foi commitado** — o último commit era o v7.3, então o documento que é a passagem de bastão estava sem cópia no repositório. **Datas do changelog acertadas, de carona:** as entradas **7.1 e 7.3** estavam em **08/09** — data no futuro, porque hoje é 07/09 e as duas foram escritas hoje — e passaram para 07/09, junto com o cabeçalho. Eram só essas duas: **a 7.2 já estava certa**, e a lista de três que eu tinha apontado na primeira leitura estava errada. Nada mais do log foi tocado. Ficou escrita no topo a regra que impede a repetição: **a data de uma entrada é a data do fato**, e quando as máquinas divergirem vale a data combinada com o Raul, não o relógio local de quem escreveu. |
| 7.4 | 07/09/2026 | **D-63 entregue (`8833165`) e a fila de código fechou.** A prova de rede passou: `three` e `r3f` **desapareceram** da carga a 412 px, a página caiu de 372 KB para **138 KB**, e o mobile foi de 68 → 82 → **94** (desktop 99). O RNF-02, que estava descumprido desde o Passo 4 sem ninguém saber, passou a ser cumprido **e medido pelo comportamento**. Marcados na §10 os três itens com prova dura (build, Lighthouse, arquivos de SEO no ar) e anotados os dois que **parecem** prontos e não estão: o **RNF-10** foi conferido antes de o Web Analytics ser ligado, então precisa de nova leitura na aba Application; e a auditoria de teclado do RNF-06 é anterior ao botão de tema. Registrada a ressalva honesta do Claude Code: **arrasto e assentamento não foram remedidos** depois do D-63 — o commit tocou um arquivo só e `useGlobeDrag.ts` e `Globe.tsx` seguem idênticos, mas isso é argumento, não medição. **Fecha com um arrasto no desktop.** |
| 7.3 | 07/09/2026 | **D-63 entregue (`8833165`) e a fila de código fecha.** O grupo `react` entrou **antes** dos grupos 3D, a família do React ganhou destino próprio e o grupo `r3f` voltou a conter só 3D. **Conferido no grafo do build, não na configuração:** a entrada agora importa `rolldown-runtime` e `react`, e mais nada — `three` e `r3f` só são alcançáveis pelos chunks das cenas. **RNF-02 cumprido pelo critério que o reprovava:** a 412 px em produção os dois **não aparecem na rede**; peso total de 372 KiB para **138 KiB**, JS não usado de 202 KiB para **28 KiB**. **Lighthouse: mobile 94** (era 82), **desktop 99** (era 98), as outras seis em 100. **Regressão conferida:** a 1350 px as duas cenas ainda montam. O caminho inteiro da semana no mobile foi **68 → 82 → 94**. |
| 7.2 | 07/09/2026 | **Aberta a D-63 — a causa real dos 237 KB.** A D-62 (b) tirou as dicas de `modulepreload` do HTML, e mesmo assim `three` e `r3f` continuaram baixando a 412 px: o preload só **antecipava** um download que aconteceria de qualquer jeito. A amarra é um **import estático da entrada**, porque o grupo `r3f` do `codeSplitting` **engoliu `react-dom` e `scheduler`**, e o `main.tsx` precisa deles para o `createRoot`. Conserto: grupo próprio para a família do React, declarado antes dos grupos 3D. Registrado que invalidar o cache do site inteiro é aceitável **agora**, porque o site ainda não tem público — e caro depois. **A lição ficou escrita na decisão:** o defeito passou pelo build, pelo lint, pelo TypeScript e pelo Lighthouse (82 com os 237 KB descendo); só o critério novo do RNF-02 reprovou, e ele se justificou na primeira vez que rodou. |
| 7.1 | 07/09/2026 | **D-62 entregue, e o aceite passou: mobile 82** (era 68), desktop **98** (era 95), as outras seis notas em 100 sem regressão. **Quem entregou os 14 pontos foi a (a) sozinha** — `9d8d99c`, fonte self-hospedada: o bloqueio de renderização caiu de **825 ms do Google Fonts + 155 ms do CSS** para **150 ms só do CSS**, e o site deixou de ter qualquer chamada a serviço do Google. Seis faces viraram **duas**, escolhidas por varredura do código: nenhum itálico e nenhum 300 eram usados. **A (b) foi implementada (`fc96578`) e NÃO atingiu o objetivo dela**, e a medição nova do RNF-02 é que pegou: `three` e `r3f` **continuam baixando** 237,5 KB a 412 px. A premissa estava incompleta — a dica de `modulepreload` só antecipava o download; a amarra é um **import estático** da entrada para o chunk `r3f`, que contém **`react-dom` e `scheduler`** além do `@react-three`. Conserto apontado e **não feito**, porque é desenho de chunk e não filtro de preload. **Pé do pin** de volta a menor que a cabeça (`72b39fd`): 10,2 px contra 14,5. |
| 7.0 | 07/09/2026 | **Aberta a D-62 — o conserto da nota 68 no mobile**, a partir das duas causas que o Lighthouse mediu: a folha do Google Fonts bloqueando a pintura por 825 ms (conserto: self-hospedar e cortar de seis faces para as usadas) e 237 KB de 3D que o celular **baixa e nunca monta**, porque o `modulepreload` do Vite não sabe do `useCanRender3D` (conserto: filtrar `three` e `r3f` em `build.modulePreload.resolveDependencies`). **O segundo conserta uma contradição do próprio documento:** o RNF-02 exige `three` carregado sob demanda, o chunk existe desde o Passo 4 e a demanda nunca foi respeitada — e a medição escrita no RNF-02 nunca pegaria, porque olha o tamanho dos chunks e não quem os baixa. **A medição do RNF-02 muda:** carga real em largura de celular, provando que os chunks 3D não aparecem na rede. Entraram também na fila o **pé do pin**, que no tema claro agora clareia a Irlanda, e a divergência **apex × www**, que é do painel. |
| 6.9 | 07/09/2026 | **Fila de código zerada e tudo publicado.** Os três commits locais foram ao ar e a produção passou por `91cd9fb` e `79eb442`. **O botão do painel foi ligado:** `/_vercel/insights/script.js` responde **200** — o Passo 9 fecha. Entregues o **prazo do `livre`** (`4ffa808`), a **inversão por tema mais as duas polaridades do pin** (`885881f`) e o **`canonical`** (`79eb442`). **Lighthouse rodado, e só uma das oito notas ficou abaixo:** desktop 95/100/100/100, mobile **68**/100/100/100. Duas causas medidas, nenhuma proposta ainda: a folha do Google Fonts bloqueia a renderização por **825 ms**, e o HTML traz `modulepreload` de **237 KB** de bibliotecas 3D que o mobile **nunca monta** — 202 KiB de JS não usado numa página de 372 KiB. Anotado também que o apex **redireciona 308 para `www`**, enquanto os quatro sinais de SEO apontam para o apex. |
| 6.8 | 07/09/2026 | A fila deixou de ter só o globo: entraram os **três commits locais ainda sem push** (o `git push` é o que leva o analytics e o pin menor ao ar), o **`canonical` ausente** — conferido no ar, o `index.html` não tem um, e o domínio acabou de mudar — e o **Lighthouse só depois do push**, senão mede a versão velha. Registrado o que já foi conferido em produção e está certo: `og:url`, `og:image` (200), JSON-LD, `robots.txt` e `sitemap.xml` no domínio novo. |
| 6.7 | 07/09/2026 | **Três defeitos do olho do Raul, e dois deles a auditoria confirmou que eram maiores do que ele viu.** (1) O globo não volta a Dublin: não é regressão — a flag `livre` do D-55 **só é desligada por rolagem**, então quem arrasta e continua olhando nunca vê o assentamento; adendo no D-55 dando **prazo de ~10 s** à flag. (2) e (3) O pin não é branco no escuro **e a inversão por tema nunca foi implementada** — os dois temas têm oceano roxo, e as quatro peças do pin compartilham uma cor. Adendo de auditoria no D-60 com o diff entre o que foi especificado e o que foi ao ar, e a causa: **a D-60 não estava no PRD quando a frente 2 foi executada**. Aberta a **D-61 — site bilíngue**, escrito e não traduzido, com duas URLs e `hreflang`, **por último e depois da revisão do inglês**. |
| 6.6 | 07/09/2026 | **Domínio no ar: `raulrodrigues.dev`**, comprado pela própria Vercel — DNS e certificado automáticos, sem registro `A` manual. Conferido no ar: `og:url`, `og:image` (200), JSON-LD, `robots.txt` e `sitemap.xml` **já no domínio novo**; `/_vercel/insights/script.js` ainda responde **404**, o que confirma que falta o botão do painel. O endereço antigo `.vercel.app` passou a responder `DEPLOYMENT_NOT_FOUND` e foi marcado como morto na seção 0. **Buraco tapado: a D-60 tinha sido implementada e nunca escrita** — a decisão da inversão por tema e das duas polaridades do pin viajou só dentro do prompt daquela sessão, e o log pulava do D-59 para o backlog. Agora está no log, com a restrição de não remontar a cena. Corrigidas três linhas velhas: inventário de movimento (dizia M-25, é M-28), Passo 10 e a lista de dependências, que ainda cobravam domínio e HTTPS. |
| 6.5 | 06/09/2026 | **Sessão de 06/09 publicada:** os seis commits do modo escuro e do globo foram para a `main`, e a produção saiu de `f724e23` para `7797d5c`. **`@vercel/analytics` ligado** (`3e060fc`), fechando o Passo 9 em código — e o **zero cookie do RNF-10 foi conferido em três camadas**, com um achado que vale registro: existe **um** caminho de cookie, o comando opt-in `va('enableCookie')`, que mora só no script remoto e **não existe dentro do pacote npm** — nenhuma atualização de dependência o liga sozinho. **Cabeça do pin reduzida à metade** (`5431c38`): `PONTA_RAIO` de 0.03 para 0.015, de ~29 px para **~14,5 px**, contra uma Irlanda de 20×22 px. Falta um passo que **não é código**: ligar Web Analytics no painel da Vercel. |
| 6.4 | 06/09/2026 | Acrescentado ao topo o bloco **"como trabalhar sem queimar a sessão"** — o Raul já perdeu quatro sessões por estouro de contexto, e o remédio principal é começar sessão nova a cada bloco, porque este documento é a passagem de bastão. |
| 6.3 | 06/09/2026 | **Frente 7 entregue** (`f724e23`) — botão medido com a página parada, e a decisão de usar evento próprio em vez de `scroll` sintético **evitou desfazer o D-55**. Aberta a **D-59: regressão — o globo parou de voltar a Dublin**, com três suspeitos e o que instrumentar. **Limpeza:** a seção 9 (Calendário) descrevia os dez passos por semana, todos executados em três dias — reescrita com o que de fato resta, que não é código. A fila "depois disso" foi reordenada por bloqueio e perdeu o item do hero oco, já resolvido. |
| 6.2 | 06/09/2026 | Aberta a **D-58**, do Raul olhando o tema escuro: os continentes do globo passam a ser **sólidos** — o D-56 trocou a cor mas manteve o pontilhado, e o pedido era massa branca —, e as partículas do hero ficam **brancas no escuro**, por tema e dentro da frente 2. A regeração da textura traz de volta o risco de projeção: **aceite é o pin cair sobre a Irlanda**. |
| 6.1 | 06/09/2026 | **Frente 6 entregue** (`dd32ee9`): zero flash, provado com 45 amostras desde antes do documento existir. Duas decisões fora da especificação e ambas certas — `color-scheme` inline fecha a janela em que nenhuma regra de fundo existe ainda; e **não** escrever `background-color` inline, porque a técnica anti-flash padrão teria **desfeito a frente 4**. Modo escuro: **3 de 7 frentes**. |
| 6.0 | 06/09/2026 | **D-57 entregue** (`d17ee23`) — o bloqueador saiu, o `<html>` segue as paradas escuras e o tema claro não regrediu. Achado que atravessa frentes: o `apply()` só roda em scroll, então **o botão da frente 7 tem de forçar reapply**. A **paleta escura derivada e medida** foi colada dentro do D-44 para não ser recalculada. Modo escuro: **1 de 7 frentes**. |

---

## 0. Onde estamos

> **Leia esta seção primeiro.** Ela é o painel do projeto: o que já está no ar,
> o que está sendo feito agora e o que falta. Mantenha-a atualizada ao fim de
> cada passo — é ela que impede a próxima sessão de recomeçar do zero.

**🟢 No ar:** <https://raulrodrigues.dev> — domínio próprio, HTTPS ativo (07/09)
> ⚠ O endereço antigo `portfolio-3d-v2-gilt.vercel.app` **responde `DEPLOYMENT_NOT_FOUND`** desde a troca. Não é defeito: o domínio novo virou o principal. **Não use mais o `.vercel.app` em lugar nenhum** — currículo, LinkedIn, assinatura
Repositório: <https://github.com/RaulRodriguexz/portfolio-3d-v2> · Deploy automático a partir da `main`

### Concluído

| | |
|---|---|
| **Passos 1 a 8** | identidade visual, conteúdo, hero, cena 3D, projetos, stack, contato, auditoria |
| **Passo 9** | **concluído em código** — SEO, JSON-LD, robots, sitemap, favicon, og.png e o `@vercel/analytics` ligado (`3e060fc`). Falta **ligar Web Analytics no painel da Vercel**, que é um botão, não código |
| **Passo 10** | parcial — repositório, deploy, **domínio e HTTPS** feitos (`raulrodrigues.dev`, 07/09). **Falta só o Lighthouse** |
| **Movimento** | **Inventário fechado: M-1 a M-28 entregues** (seção 5.2.1). Nada novo entra depois do M-28 (D-51) |
| **Qualidade** | auditoria do Passo 8 sem erros · bundle inicial 70 KB gzip · **zero cookies, agora conferido com o analytics ligado** (ver a linha do RF-09) |
| **Arrasto do globo** | D-29 e D-30 entregues — captura na `<section>`, critério a 45°, `touch-action: pan-y pinch-zoom`, inércia dobrada na `base` |
| **Largura e leitura** | D-31 e D-39 entregues — Container 96 rem, hero 104 rem, prosa em `rem` com máximo medido de 74 caracteres por linha |
| **Ritmo e densidade** | D-32 a D-35 e M-24 entregues — coluna de metadados por seção, numeração derivada do `id`, faixa de impacto, marcador `BR → IE` |
| **Modo escuro, frente 7 (botão)** | Sol e lua como um desenho só que se transforma, transição no ícone. Medido com a página parada no topo: fundo de `rgb(251,251,254)` para `rgb(22,18,27)` **sem nenhuma rolagem** — a dependência da frente 4 fechada por evento próprio `tema:mudou`, não por scroll sintético, que zeraria o `livre` do D-55. Foco 2px/offset 3px a 8,51:1, `aria-pressed` alternando |
| **Modo escuro, frente 6 (sem flash)** | Script inline no `<head>` antes de folha e bundle, com `color-scheme` inline fechando a janela anterior ao CSS. Medido com gravador instalado antes do documento: **0 quadros claros em 45 amostras**, e o bastão passa de script inline → folha → tom ambiente sem lacuna |
| **Modo escuro, frente 1 (tokens)** | Variante escura em `:root[data-theme='dark']`, sem uma linha de `body { background-color }`. Precisou de um **oitavo token**, `on-primary`: no escuro o `primary-deep` é claro e `text-white` sobre ele caía a 2,1:1. Medido — claro sem nenhuma regressão; escuro com ink 13,65, muted 7,35, primary-deep 8,62, primary 6,86; marca-texto do D-52 em 6,9 e 6,64; ênfase do D-53 em 8,58 e 8,62 |
| **Modo escuro, frente 4 (D-57)** | Bloqueador removido — o `useAmbientTint` ganhou jogo próprio de paradas escuras. Medido: `<html>` em `rgb(28 20 37)` e `rgb(20 17 25)` no escuro, e inalterado no claro. **Pendência amarrada:** o botão da frente 7 tem de forçar reapply, senão alternar parado no topo não repinta |
| **Modo escuro, frente 2 (cenas 3D)** | Última frente entregue (`51d6964`) — doze literais em cinco arquivos viram `three/paleta.ts` por tema, lido pelo `useTema` no mesmo `tema:mudou` do botão. **Derrubou uma suposição do D-44:** as órbitas nunca foram `AdditiveBlending`, só a poeira é — em mistura normal a opacidade quase não cai. Anel largo medido no pixel: 3,20:1 no claro, **1,31:1 no escuro antes**, 3,26:1 agora. Globo escuro com continente × oceano em 4,31:1 e pin em 7,45:1. Alternar pelo botão repinta as cenas sem recarregar, e o globo segue parado depois de duas trocas — 0,07% |
| **Modo escuro, frente 5 (fio)** | Entregue (`f06e08a`) — quase de graça, como o inventário previu: o `stroke` já era token. A opacidade não era, e virou `--thread-opacity`. Traço contra o próprio fundo: 0,50 dá 1,948:1 no claro; no escuro o mesmo 0,50 daria 2,630:1, e 0,36 devolve 1,946:1. O ponto da ponta (M-28) **não precisou de par**, medido: halo 1,216 contra 1,280, núcleo 6,389 contra 6,514 |
| **Modo escuro, frente 3 (grão)** | Entregue (`5f3836f`) — medido com o conteúdo escondido, para a variação ser só da camada de ruído: claro a 0,028 dá 1,0174:1; escuro a 0,028 dá 1,0281:1, **62% mais forte**; escuro a 0,020 empata. Texto miúdo remedido no escuro pelo pior pixel de fundo dentro da caixa de cada trecho: de 7,13:1 (coords) a 13,05:1 (prosa), todos acima de 4,5 |
| **Continentes sólidos (D-58a)** | Entregue (`2894e85`) — textura regerada do mesmo Natural Earth land-110m, mesma projeção, mesmo PNG indexado de 1 bit: 15,6 KB contra 11,4 KB. Projeção provada por comparação com o próprio PNG antigo — 96,90% dos 4.740 centros de ponto caem em terra cravada, 99,56% dentro de meia célula — e por 28 de 30 cidades de controle. **Aceite do D-58 cumprido, com o marcador escondido para poder olhar embaixo:** o centro do globo assentado é continente, na borda leste de uma ilha de 20×22 px com o Mar da Irlanda a leste. A armadilha foi a Antártida, que circunda o polo e cujo anel volta ao início por um salto atravessando o mapa — fechado assim, o preenchimento repartia o continente em faixas |
| **Globo: regressão do assentamento (D-59)** | Entregue (`6c13caf`) — e não era nenhum dos três suspeitos, que ficam verificados. Era o **clique parado**: pelo D-29 o mouse pula a classificação e `mode` nasce `'drag'`, então soltar sem ter movido ligava o `livre` do D-55. Medido na caixa de 773 px: um clique sem mover dava 15,47% dos pixels mudando e o marcador 161,6 px fora do centro; agora dá 0,14% e Δ 0,0 px. Arrasto de verdade segue livre em 12,42% |
| **Medição de audiência (RF-09 / D-05)** | `@vercel/analytics` ligado no `App` (`3e060fc`). **Zero cookie conferido em três camadas**, porque o RNF-10 promete "aba Application → Cookies vazia" e isso não quebra build nem lint: (1) o pacote npm não toca em `document.cookie`, `localStorage`, `sessionStorage` nem `indexedDB` — o único "cookie" do pacote está no *entrypoint* de servidor, que **repassa** o header da requisição recebida e não cria nada, e esse entrypoint não é importado aqui; (2) o script que a Vercel serve em `/_vercel/insights/script.js`, lido do próprio arquivo publicado (3.106 bytes): **nenhuma** API de cookie ou de armazenamento, e a resposta **sem `Set-Cookie`**; (3) o bundle construído: nenhum `document.cookie`, e o único `localStorage` que sobra é o do botão de tema. **O achado:** existe **um** caminho de cookie e ele é **opt-in** — o comando `va('enableCookie')` dispara um GET cuja resposta cria o cookie. Ele mora só no script remoto e **não existe dentro do pacote**, então nenhuma atualização de dependência o liga sozinha: alguém tem de escrever a chamada. Ficou comentado no `App.tsx` como o único jeito de derrubar o RNF-10 daqui. Custo: **986 bytes gzip** no chunk inicial, de 73.185 para 74.171. **Pendência que não é código:** enquanto o Web Analytics não for ligado no painel, `/_vercel/insights/script.js` responde **404** — conferido em produção — e a medição final do RNF-10 na aba Application só pode ser feita depois disso |
| **Cabeça do pin contra a ilha** | Entregue (`5431c38`) — `PONTA_RAIO` de 0.03 para **0.015**, e só o número: haste, pé, anel de duas faixas e a oclusão por profundidade ficaram como estavam. Foram os continentes sólidos do D-58a que tornaram a ilha mensurável, e a medida acusou o desproporcional: Irlanda ~20×22 px, cabeça ~29 px. Agora **~14,5 px**, 72% do lado estreito da ilha. **Não precisou de nova medição de tela, e o motivo importa:** o centro da cabeça fica em `HASTE_ALTURA` ao longo da normal **independente do raio**, e câmera, posição e escala não mudam — o diâmetro projetado é **linear** em `PONTA_RAIO`, então a correção é uma razão pura; ilha e cabeça saem da mesma sessão e da mesma unidade, então a comparação não depende de qual unidade era. Conferência pela geometria da cena (fov 34, câmera em 9.4, RADIUS 1.6, escala de 1 a 1.42): a cabeça a 0.03 dá 15,8 px CSS no zoom cheio, que vezes o teto de dpr 1,75 do `Canvas` dá 27,7 — os ~29 px medidos, ou seja o modelo reproduz a medição antiga antes de escalar a nova. Visibilidade preservada nos dois temas: a cor não mudou e o pin já era escuro nos dois de propósito (D-44, frente 2), a 7,70:1 sobre o continente. **Apontado, não corrigido:** o **pé da haste** (`circleGeometry` de raio 0.018) projeta ~16,6 px nas mesmas unidades — agora maior que a própria cabeça e perto dos 20 px da ilha. Se a pegada ainda incomodar, o próximo ajuste é esse raio |
| **RNF-02 finalmente cumprido (D-63)** | Entregue (`8833165`) — **um arquivo, 31 linhas, 237,5 KB a menos no celular.** O grupo `react` declarado **antes** dos grupos 3D: a família do React ganha destino próprio e o `r3f` volta a conter só 3D. A **ordem** é o conserto. Conferido no **grafo do build**: a entrada importa `rolldown-runtime` e `react` e mais nada, e os chunks 3D só são alcançáveis pelas cenas. Prova de rede a 412 px em produção: `three` e `r3f` **ausentes**, peso total **372 → 138 KiB**, JS não usado **202 → 28 KiB**, bundle inicial em **80 KB** contra o teto de 150. Lighthouse **mobile 94, desktop 99**. Regressão conferida a 1350 px: as duas cenas ainda montam. **Último item de código do projeto** |
| **Tipografia self-hospedada (D-62 a)** | Entregue (`9d8d99c`) — e foi ela sozinha que tirou a nota mobile de 68 para 82. A folha do Google Fonts era **render-blocking num domínio de terceiro**: 825 ms antes de a página pintar qualquer letra. Agora os `.woff2` saem do mesmo domínio, o `@font-face` mora no `index.css` e o `index.html` traz só o `preload` — necessário porque a fonte é referenciada de dentro do CSS, então sem ele o navegador só a descobriria depois de baixar e interpretar a folha. **Seis faces viraram duas**, por varredura: nenhum itálico e nenhum 300 eram usados de verdade — 13,9 KB somados, subset `latin` apenas. **Licença conferida, não presumida:** OFL 1.1, © 2011 Bernd Montag, com o `OFL.txt` ao lado dos binários. **O `404.html` foi junto**, senão a promessa de "nenhuma chamada a serviço do Google" seria falsa na página de erro. Não resta nenhuma referência a `googleapis` ou `gstatic` no repositório, o que também reforça o zero cookies do D-05 |
| **Pé do pin menor que a cabeça** | Entregue (`72b39fd`) — raio de 0.018 para **0.011**, só o número. Ele tinha ficado **maior que a cabeça** sem ninguém notar: quando ela caiu para 0.015, o pé seguiu em 0.018 e passou a projetar 16,6 px contra 14,5, ou seja **115%**. Passou despercebido porque no tema claro de então o pé era escuro sobre continente branco e lia como sombra da haste; a inversão do D-60 trocou o fundo debaixo dele e virou disco **claro** apagando a Irlanda roxa. Agora **10,2 px = 70% da cabeça**, ainda 1,8× a largura da haste e metade do lado estreito da ilha. O número virou constante `PE_RAIO` com o **teto de 0.0157 escrito junto** — foi a ausência desse vínculo que deixou o defeito entrar quando a cabeça encolheu |
| **Globo: a inversão por tema e as duas polaridades do pin (D-60)** | Entregue (`885881f`) — as duas partes que a auditoria de 07/09 achou faltando, juntas porque uma muda a resposta da outra: inverter o claro troca a superfície que o pé do pin pisa. **(a)** O tema claro volta a ser o oposto do escuro — oceano `#fbfafd`, continentes `#8c62ac` —, e o par é **exatamente o que existia antes do D-56**, de propósito: não é número novo, é o estado que o Raul já tinha visto e aprovado. Zero KB novos, a textura é a mesma multiplicada. **(b)** `pin` vira `pinNaSuperficie` (o pé) e `pinAcimaDaSuperficie` (haste e cabeça), e o comentário que dizia "o pin continua escuro nos dois temas" saiu — ele estava **meio certo, que é pior que errado inteiro**. **O anel não segue nenhuma das duas, e essa foi a parte que quase passou batido:** o pulso cresce 2,2× e atravessa a costa, então amarrar a faixa escura à cor do pé deixaria as **duas faixas claras** no tema claro e o anel sumiria inteiro sobre o oceano — por isso entrou `anelEscuro` próprio. **Efeito colateral consertado junto, e só visível com a inversão:** a atmosfera lia `paleta.oceano`, o que passava despercebido porque os dois temas tinham oceano roxo; com o claro invertido virava halo branco somado sobre página branca, ou seja sumia. Voltou a ter token próprio, como antes do D-56 |
| **Contrastes do globo nos dois temas (D-60)** | Continentes × oceano: **4,53:1** no claro e **4,94:1** no escuro puros; **3,79:1** e **4,13:1** com a malha composta por cima das duas superfícies. Cabeça do pin: **8,52:1** contra o oceano e **8,58:1** contra o fundo no claro; **6,38:1** e **18,48:1** no escuro. O anel se mede pela garantia do D-56 — **ao menos uma faixa acima de 3:1 em cada superfície**, e as outras duas combinações são baixas de propósito, que é justamente o motivo de as faixas serem duas: claro/oceano **8,52** (escura), claro/continente **4,21** (clara), escuro/oceano **5,71** (clara), escuro/continente **8,55** (escura). Pior caso entre tudo que foi pedido: **3,79:1**. **Método:** sem navegador nesta sessão, é contraste WCAG calculado das cores de material, com a malha composta analiticamente e a textura multiplicada como o material faz — **não é leitura de pixel de captura**, como foram as medições do D-56 e do D-58a |
| **Globo: o giro livre ganha prazo (D-55)** | Entregue (`4ffa808`) — `LIVRE_TIMEOUT` de 10 s. Não era regressão nem briga entre o D-55 e o D-28: era **especificação faltando**, porque a flag só era desligada por rolagem e quem arrasta olhando a seção nunca rola. **Não entrou um segundo temporizador**, e isso foi deliberado: o prazo se mede contra o mesmo `lastInteraction` que já governa o repouso do D-28 — um carimbo só, como aquela decisão exigiu. Um `setTimeout` daria o mesmo resultado hoje, criaria o relógio paralelo que o D-28 evitou de propósito e ainda precisaria ser cancelado e rearmado a cada gesto. A flag é **apagada de verdade** ao vencer, e não só ignorada na conta, para o estado não passar a mentir para quem o ler depois |
| **`canonical` e o domínio** | Entregue (`79eb442`) e conferido no ar. Aponta para o apex, no mesmo endereço que `og:url`, `sitemap.xml` e `robots.txt` já declaram — quatro sinais concordando. ⚠ **Mas o servidor discorda dos quatro:** o apex responde **308 para `www.raulrodrigues.dev`**, porque a Vercel pôs o `www` como domínio principal na compra. Canônico apontando para endereço que redireciona não é erro — o buscador segue o redirecionamento —, mas é sinal torto. **O conserto é no painel, não no código:** trocar o domínio principal para o apex. A alternativa oposta é mover os quatro para `www`; o que não pode é ficar como está |
| **Medição de audiência ligada de verdade** | O botão do painel foi ligado: `/_vercel/insights/script.js` responde **200** com 3.106 bytes, contra o 404 de 06 e 07/09. Com o `3e060fc` no ar, o **Passo 9 fecha**. O bundle publicado foi conferido **byte a byte** contra o build local, então o que está medindo é o código desta sessão |
| **Globo invertido** | D-56 entregue — oceano roxo e continentes claros com 3,47:1 entre si, malha custando 0,42 de contraste, anel de radar em duas faixas para cruzar as duas superfícies. Textura recolorida na paleta: 11.372 bytes, os mesmos |
| **Globo: giro livre depois do arrasto** | D-55 entregue — soltar suspende o assentamento; medido 6,6% de pixels mudando por 800 ms depois de 8 s sem rolagem, contra 0,18% depois de uma rolagem |
| **Globo: volta completa e zona de arrasto** | D-54 entregue — teto de ±60° removido com assentamento congruente no eixo X (sem ele o percurso chegava a 6π para trás), gesto só **começa** perto do planeta, e o `cursor-grab` que era inerte passou a funcionar |
| **Hierarquia na Stack e rodapé** | D-53 entregue — títulos em 20 px/700 com 20,12:1, uma ênfase por grupo **sem fundo** (5 em prosa com, 4 em grade sem), rodapé em 700 contra 400 |
| **Foco e 404** | RNF-06 auditado com **Tab real** em 1440 e 360 px — dois defeitos achados no `CopyEmail`, os **dois únicos controles do caminho de contato**: anel cortado pelo `overflow-hidden` e a 1,79:1. Depois: 0 de 23 e 0 de 25 paradas, contraste de 8,3 a 8,9:1. Página 404 própria, autocontida, 3,4 KB, respondendo **HTTP 404 de verdade** — soft-404 seria indexado como página válida |
| **Contraste entre blocos** | D-52 entregue — citação de abertura no About em 24 px, marca-texto com `box-decoration-clone` medido em **6,9:1**, subtítulo do hero em dois parágrafos. Máximo real de leitura: 75 caracteres por linha |
| **Ênfase e material** | D-38 e D-36 entregues — cinco destaques em `primary-deep`, um por parágrafo; grão a 2,8% com contraste medido em 7,48:1 no pior caso |
| **Fio roxo** | D-37 e M-25 entregues — caminho gerado da geometria medida dos rótulos, `ResizeObserver`, confirmado por A/B de pixel em três posições de scroll |
| **Ponto na ponta do fio** | M-28 entregue — posição lida do próprio `<path>`; erro de 0 px contra o comprimento desenhado em três posições de scroll. **Fecha o inventário de movimento** |
| **Links magnéticos** | M-27 entregue — medido: 0 px longe, 4 px a meio raio, 8 px no teto, 0 px ao sair; no toque o ímã nem é instalado |
| **Reveal nos títulos** | M-26 entregue — os `h2` revelam palavra a palavra pegando carona no observer do `useReveal`; medido: exatamente 1 observer por `h2`, nenhum novo |
| **Documentos** | D-47 e D-48 entregues — `WORKFLOW` vira método (520 → 112 linhas), `ARCHITECTURE` corrigido com o contrato de camadas escrito, `Thread` movido para `layout/` |
| **Setas decorativas** | D-46 entregue — os dez links com seta têm nome acessível limpo; zero setas lidas por leitor de tela |
| **Texto legível por máquina** | Varredura da página inteira: os 14 títulos (`h1`, `h2`, `h3`) leem como frases corretas; `<br />` da Location corrigido — `"Dublin, Ireland"` |
| **Texto de máquina** | D-45 resolvida — varredura completa: 14 títulos corretos, `<br />` da Location consertado (`2c96e11`); fita da Stack e irmãos por `gap` ficam como estão, com o motivo escrito |
| **`WordReveal`** | Espaço real fora do recorte — `textContent` do `h1` e da frase de posicionamento voltam a ter espaços; larguras caíram 5,6 px e 24,7 px |
| **Pin de Dublin** | M-23 e D-25 entregues — `Marker` em arquivo próprio, haste e cabeça, oclusão confirmada por arrasto real; `Globe.tsx` de 226 para 195 linhas |
| **Header** | D-42 entregue — nav centrada, `Get in touch` à direita, e a mesma largura de container do hero; tinta alinhada com 0 px à esquerda e −1 px à direita |
| **Alinhamento do hero** | D-41 entregue — palco em coluna própria com `flex-1`; gutter de 43 a 626 px e folga vertical de 101 a 169 px na cena 3D, zero colisão em seis larguras e três alturas |
| **Produção** | Deploy automático da `main` confirmado na prática (RNF-08). **O hash corrente sai de `git log origin/main -1`** — hash fixo aqui envelhece no commit seguinte, inclusive neste |

### 🔨 Em andamento

> **Há uma fila só, e é o *plano de encerramento* mais abaixo.** A fila numerada
> que vivia aqui (itens 1 a 13, de 05 a 07/09) tinha o mesmo item repetido em
> dois lugares — o domínio principal aparecia como "a fazer" aqui e como item 2
> lá. Duas listas do mesmo trabalho é duas verdades; ficou a de baixo, que tem
> dono e tempo. O histórico dos 13 itens está em `DECISOES-ARQUIVADAS.md`.

### 📊 Lighthouse e RNF-02 — provas de 07/09

| | Performance | Acessibilidade | Best Practices | SEO |
|---|---|---|---|---|
| **Desktop** | **99** | 100 | 100 | 100 |
| **Mobile** | **94** | 100 | 100 | 100 |

Página de **372 KB → 138 KB**; `three` e `r3f` **ausentes** da carga a 412 px,
que é o RNF-02 medido por **comportamento** e não por tamanho de chunk. As
tabelas completas, a lista de requisições e o histórico das três corridas
(68 → 82 → 94) estão em `DECISOES-ARQUIVADAS.md`; o que uma sessão nova precisa
saber cabe nestas seis linhas.

### 🎯 O plano de encerramento — revisto em 11/09, fim do dia

> **A fila de código fechou de novo.** D-72, D-73, D-74 e D-75 entregues e no
> ar (`8a0a6c1`, `9fa974c`, `1049e41`, `43f67c7`, `decec99`). Não há item
> bloqueante: o RNF-09 mudou de estado na D-74. **O que resta é o conteúdo novo
> de trabalho (D-66), que depende do Raul, e os cinco minutos de conferência
> dele.** A D-70 e a D-76 são dívidas pequenas, registradas, e nenhuma das duas
> segura publicação.

**Ritmo de trabalho, achado em 11/09 e vale para as próximas sessões:** o
Claude Code está rodando em **`xhigh effort`**, o modo mais lento. Para
**diagnosticar** — achar a paridade invertida na textura, isolar o arco por
eliminação — ele paga cada segundo e foi o que produziu os melhores achados do
projeto. Para **aplicar especificação já escrita**, é desperdício: ele
raciocina sobre decisões que já estão tomadas neste documento. **Alternar:
`xhigh` nas rodadas de investigação, normal nas de aplicação.** A sessão de 34
minutos de 11/09 misturava os dois tipos.


> **Dois itens de aparência voltaram à fila em 11/09** (D-72, o retângulo do
> campo de estrelas; D-73, o risco no globo). Nenhum deles trava o lançamento —
> o bloqueante continua sendo a revisão do inglês —, mas os dois são a primeira
> coisa que um recrutador vê.


**Não falta código. Falta conferir e soltar.** Esta tabela é a lista inteira;
quando ela zerar, o site publica.

| # | O quê | Dono | Tempo | Estado |
|---|---|---|---|---|
| ✅ | ~~D-65 — malha, clique e o aro~~ | — | — | **fechada em 07/09** (`8c6ca50`, `89ed004`). Resta o olho do Raul sobre a borda que sobrou no claro — **D-70** |
| 0.5 | **D-66 — conteúdo novo de trabalho**, em `src/data/` | Raul traz 08/09 à tarde · Claude Code aplica | 1 sessão | 🕐 08/09 |
| 0.7 | **D-64 — pacote de revisão**, gerado **depois** do conteúdo novo | Claude Code | 1 sessão | 🕐 depois da D-66 |
| ✅ | ~~Revisão do inglês (RNF-09)~~ | Cowork | — | **feita em 11/09** — `docs/REVISAO-INGLES.md`, 11 pontos, 3 erros de fato. O requisito mudou na **D-74**: não exige mais pessoa fluente. **Não há mais item bloqueante.** |
| ✅ | ~~Apex como domínio principal na Vercel~~ | — | — | **feito em 07/09** — apex em Produção, `www` com 307 para ele, casando com `canonical`, `og:url`, `sitemap` e `robots` |
| ❌ | ~~D-67 — globo no celular~~ | — | — | **descartada pelo Raul em 07/09**; o motivo e o caminho, se voltar, estão na decisão |
| ✅ | ~~D-68 — favicon novo, faísca B3~~ | — | — | **publicado em 11/09** (`e16a85d`) — o arquivo estava trocado no disco desde 07/09 e segurado fora da `main` de propósito, por ser mudança de identidade; o Raul autorizou e foi ao ar. 490 bytes, sem fundo, com `prefers-color-scheme` dentro do próprio SVG |
| ✅ | ~~D-69 — o fio roxo sai do celular~~ | — | — | **entregue em código em 11/09** — não montado abaixo de 640 px, e o limiar unificado no `LARGURA_MINIMA`. A conferência em navegador (360 px, 1440 px, Lighthouse) entra na passada do Raul, itens 4 a 6 |
| ✅ | ~~Arrastar o globo no desktop: soltar e ver se volta a Dublin~~ | — | — | **confirmado em 07/09** — volta em 5 a 6 s, e medido a 0,0 px do centro |
| 4 | Arrastar o globo **com o dedo**, celular real | Raul | 1 min | a fazer |
| 5 | Passada de `Tab` do topo ao rodapé (RNF-06 é anterior ao botão de tema) | Raul | 5 min | a fazer |
| 6 | Preview do link no WhatsApp e no LinkedIn | Raul | 2 min | a fazer |
| 7 | Link para 5 pessoas, ouvir e aplicar | Raul | — | depois do 1 |
| ✅ | ~~Zero cookies (RNF-10)~~ · ~~360 px sem scroll (RNF-05)~~ · ~~links externos~~ | — | — | **fechados em 07/09** |

**A ordem tem um motivo só: o item 1 é o único que não depende de você.** Ele é
o que tem prazo de espera, então é o primeiro a sair da sua mão. Os itens 2 a 6
somam **menos de dez minutos** e podem ser feitos hoje, em qualquer ordem.

**O que já está provado e não precisa ser refeito:** Lighthouse 99/94 em
produção, RNF-02 medido por carga real, zero cookies com o Analytics ligado,
360 px sem scroll horizontal com a cena 3D não montando, 404 de verdade, 12
links externos abertos, `cv.pdf` respondendo, `lang="en"`, um `<h1>`, nenhuma
imagem sem `alt`.

**O que fica para DEPOIS de publicar:** a **D-61** (site bilíngue) e o
**pé do pin** no tema claro, que é gosto e não defeito.

---

### ⏳ Fila depois disso

| Item | Depende de | Bloqueia |
|---|---|---|
| ~~**Revisão do inglês** por pessoa fluente (RNF-09)~~ | ✅ **estado mudado pela D-74 em 11/09** — o alvo deixou de ser "soar irlandês" e passou a ser "não ter erro", que é verificável sem revisor humano. Revisado e corrigido; **deixou de ser bloqueante** | — |
| ~~**Domínio** `raulrodrigues.dev` + HTTPS~~ | ✅ **feito em 07/09** — comprado pela Vercel, DNS e certificado automáticos | — |
| ~~**Ligar Web Analytics no painel** da Vercel (RF-09)~~ | ✅ **feito em 07/09** — o script responde 200, o Passo 9 fecha | — |
| **Domínio principal no painel**: o apex responde 308 para `www`, e `canonical`, `og:url`, `sitemap` e `robots` apontam para o apex | o Raul, no painel da Vercel | nada trava, mas é sinal torto para o buscador |
| ~~Lighthouse na URL de produção (RNF-01)~~ | ✅ **rodado em 07/09** — 7 das 8 notas passam | — |
| ~~**Performance mobile em 68**~~ | ✅ **94 desde 07/09** — fontes self-hospedadas (D-62 a) levaram 68 → 82, e a ordem dos grupos de chunk (D-63) levou 82 → 94. Meta era 80 | — |
| Teste do arrasto com **dedo** no celular (D-29) | o Raul, num aparelho real | — |
| ~~4º card de projeto: B2B com geolocalização (D-21)~~ | ✅ **entregue em 12/09** (D-81), junto com a grade de dois destaques | — |

*Saiu daqui em 06/09: "hero com o miolo oco" — resolvido pelo D-43 mais a faixa
do D-34 e o fio do M-25, e confirmado nas capturas do tema escuro.*

### Não esquecer

- O site é **em inglês**. Se abrir traduzido, é o Chrome — use "Mostrar original".
- Abaixo de 640 px a cena 3D não é montada, de propósito. O Memoji vira imagem.
- `prefers-reduced-motion` desliga o movimento. Hoje ele desliga também as
  cenas 3D inteiras, o que é excessivo — a revisão desse comportamento é a
  **D-26**, ainda aberta.

---

## 1. Por que este documento existe

A primeira tentativa do site 3D falhou. O motivo não foi falta de capacidade
técnica — foi falta de fronteira. Sem escopo escrito, cada sessão de código
adicionava um efeito novo, o projeto nunca chegava a um estado "pronto", e o
custo de manter o que já existia cresceu até travar.

Este PRD responde três perguntas antes de qualquer linha de código:

1. O que este site precisa fazer para ser considerado um sucesso?
2. O que ele explicitamente **não** vai fazer na v1?
3. Como eu sei que terminei?

**Regra do projeto:** nada entra no código se não estiver aqui. Ideia nova vai
para `docs/BACKLOG.md`, não para a branch atual.

---

## 2. Objetivo de negócio

O site não é uma peça de arte. É um **instrumento de conversão de carreira**.

**Objetivo primário.** Fazer com que um recrutador, fundador ou cliente B2B na
Europa — foco em Dublin — entre em contato em até 90 segundos de visita, mesmo
sem o Raul ter diploma concluído.

**Objetivos secundários.**

- Provar competência técnica sem credencial formal: o próprio site é a evidência.
- Posicionar o Raul como **AI + negócio**, não como "mais um dev front-end". O
  3D chama atenção; o conteúdo tem que falar de receita, custo e automação.
- Servir de vitrine para os projetos-âncora que virão (revenue-leak detector,
  agente em Python puro), além dos já entregues.
- Estar online e estável antes de 26/10/2026.

**Não-objetivos.** Virar referência de WebGL. Ganhar prêmio de Awwwards. Ser um
showcase técnico de Three.js. Impressionar outros desenvolvedores.

---

## 3. Público-alvo

| Persona | Quem é | O que procura em 30 s | O que a faz sair |
|---|---|---|---|
| **Recrutador tech (Dublin / UE)** | Recruiter ou hiring manager de startup ou consultoria | Stack, provas de entrega, se dá para contratar sem diploma, disponibilidade e situação de visto | Site lento, sem CV, sem contato claro, inglês ruim |
| **Fundador / cliente B2B** | Dono de empresa média (jurídico, odontologia, marketing digital, varejo) | "Esse cara resolve o meu problema? Já fez isso para alguém como eu?" | Jargão técnico sem resultado de negócio |
| **Par técnico / comunidade** | Devs, mentores, gente de hackathon e de programas como o EEML | GitHub, qualidade de código, se o projeto é real | Projeto que não abre, repositório vazio |

**Regra de desempate:** quando impressionar visualmente conflitar com converter o
recrutador, **o recrutador ganha**.

---

## 4. Princípios do produto

1. **Conteúdo primeiro, 3D depois.** O site tem que fazer sentido com o WebGL
   desligado.
2. **Uma cena 3D, bem feita.** Não várias medianas.
3. **Toda seção responde a uma pergunta do visitante.** Se não responde, é
   decoração — corta.
4. **Performance é requisito, não otimização futura.** O recrutador abre no
   celular, no 4G, entre duas reuniões.
5. **Terminar vale mais que perfeito.** V1 no ar vale mais que v2 na cabeça.

---

## 5. Escopo da v1

### 5.1 Arquitetura de informação

Uma página, sete blocos, nesta ordem:

| # | Bloco | Pergunta que responde | Conteúdo obrigatório | Passo |
|---|---|---|---|---|
| 1 | **Hero** | "Quem é?" | Nome, função em uma linha, localização, cena 3D com o Memoji. **Nada mais** (D-17) | 3, 4 |
| 1b | **Statement** | "O que ele resolve?" | A frase de posicionamento em tipografia grande, o parágrafo de contexto, os dois CTAs e a disponibilidade — revelados no scroll | 3 |
| 2 | **Sobre** | "Por que confiar nele?" | 3 a 4 frases: autodidata, freelance para empresas reais desde 2025, foco AI + negócio, mudança para Dublin | 2 |
| 3 | **Projetos** | "O que ele já entregou?" | 3 a 5 cards: problema de negócio → o que construiu → impacto → stack → links | 5 |
| 4 | **Stack** | "Ele sabe o que eu preciso?" | Agrupado por uso: AI & Agentes, Automação, Dados, Web. Sem barra de proficiência | 6 |
| 4b | **Location** | "Onde ele está?" | Globo 3D que gira e aproxima Dublin no scroll, coordenadas e relógio local ao vivo (D-19) | extra |
| 5 | **Contato** | "Como falo com ele?" | E-mail, LinkedIn, GitHub, download do CV. Sem formulário | 7 |
| 6 | **Rodapé** | "Como falo com ele, se rolei até aqui?" | Nome, função, disponibilidade, os quatro caminhos de contato (e-mail, LinkedIn, GitHub, CV), copyright e link do repositório | ✅ |

A seção **Serviços** foi cortada da v1 (ver 6 e log de decisões D-07).

**Adição de 04/09 (D-34):** entre o bloco 1b (Statement) e o bloco 3 (Projetos)
entra uma faixa curta com **um** número de impacto em tipografia grande. Não é
uma seção nova com rótulo e título — é uma linha, sem cabeçalho, cuja função é
dar um pico de hierarquia no ponto em que a página hoje é mais monótona.

### 5.2 A cena 3D do hero

**Decisão:** o Memoji do Raul como elemento central, dentro de uma cena
React Three Fiber real. Sem modelo `.glb`.

**Fundamento.** O Memoji só existe como imagem — a Apple não exporta modelo 3D.
Mas uma imagem dentro de uma cena WebGL continua sendo 3D: tem profundidade,
sombra, iluminação, reage ao mouse e ao scroll. Custa ~100 KB em vez de 3 MB, e é
a única rota que entrega "o Raul aparece no hero" dentro do prazo.

**Especificação.**

- Uma cena única, ocupando a metade direita da primeira dobra no desktop.
- O Memoji entra como textura em um plano dentro da cena (billboard), com sombra
  projetada e leve inclinação — não é um adesivo colado sobre a página.
- Em volta dele, geometria real na paleta roxa: anel, partículas ou placas
  flutuantes, com profundidade de verdade.
- Parallax suave seguindo o mouse; leve afastamento ao rolar a página.
- Textura trocável: se um dia houver um Memoji em vídeo ou uma captura de tela
  dos projetos, troca-se a textura e a cena continua a mesma.

**Restrições duras.**

| | |
|---|---|
| Asset | `public/images/memoji.png` — 694 × 781, 102 KB, fundo transparente. Versão `.webp` de 30 KB para o fallback. **Pronto.** |
| Carregamento | A cena inteira entra por `React.lazy`. Nada de `three` no bundle inicial |
| Ordem de renderização | O texto do hero aparece e é utilizável **antes** de a cena carregar |
| Degradação | Sem WebGL, sem suporte, ou com `prefers-reduced-motion`: o mesmo PNG aparece como `<img>` comum, e o site continua 100% funcional |
| Timebox | Duas sessões. Depois disso, congela do jeito que estiver |

### 5.2.1 Linguagem de movimento

*Adicionado em 03/09/2026. O que se aproveita da referência
eric-cole.framer.website não é a aparência — é a técnica.*

**Três princípios, nesta ordem de prioridade:**

1. **Tipografia grande com muito vazio em volta.** Custa zero: é espaçamento.
   Responde por metade da sensação de "site caro".
2. **Movimento amarrado ao scroll e ao mouse**, não animação que roda sozinha.
   O elemento reage à pessoa — é isso que prende.
3. **Contenção.** Uma ideia visual, repetida. Site amador tem cinco efeitos
   brigando entre si.

**Inventário de movimento da v1** — tudo com `transform` e `opacity` apenas,
que é o que a GPU compõe de graça. Nenhuma biblioteca nova.

| # | Animação | Onde | Passo | Estado |
|---|---|---|---|---|
| M-1 | Entrada das seções em cascata ao rolar | todas as seções | — | ✅ pronto (`useReveal`) |
| M-2 | Headline aparecendo palavra por palavra | Hero | 3 | ✅ pronto (`WordReveal`) |
| M-3 | Memoji com parallax no mouse | Hero | 4 | ✅ pronto (`HeroScene` → `Rig`) |
| M-4 | Memoji se afastando levemente no scroll | Hero | 4 | ✅ pronto (`useScrollProgress`) |
| M-5 | Anel / partículas girando devagar em volta do Memoji | Hero | 4 | ✅ pronto (`Backdrop`) |
| M-6 | Brilho seguindo o cursor dentro dos cards, e hover nas tags | Projetos, Stack | 5, 6 | ✅ pronto |
| M-7 | Frase de posicionamento palavra por palavra ao entrar na tela | Statement | 3 | ✅ pronto |
| M-8 | Globo girando continuamente; após 1,5 s em repouso assenta com Dublin de frente (D-28). A aproximação segue ligada ao scroll | Location | extra | ✅ pronto |
| M-9 | Fita de tecnologias rolando em loop sem emenda | Stack | 6 | ✅ pronto |
| M-10 | Tom do fundo da página mudando de forma quase imperceptível ao rolar | global | extra | ✅ pronto |
| M-11 | Relógio de Dublin ao vivo | Location | extra | ✅ pronto |
| M-12 | Header que esconde ao descer e volta ao subir | global | extra | ✅ pronto |
| M-13 | Item do menu marcando a seção que está sendo lida | global | extra | ✅ pronto |
| M-14 | Barra fina de progresso de leitura | global | extra | ✅ pronto |
| M-15 | Parágrafos do Sobre entrando em cascata | About | extra | ✅ pronto |
| M-16 | Botão de e-mail com feedback de "copiado" | Contato | 7 | ✅ pronto |
| M-17 | Setas do rodapé avançando no hover | Rodapé | 7 | ✅ pronto |
| M-18 | Rolagem com inércia (Lenis, 6 KB) | global | extra | ✅ pronto |
| M-19 | Cortina de entrada da página | global | extra | ✅ pronto |
| M-20 | Cards inclinando 3,5° com o mouse | Projetos | extra | ✅ pronto |
| M-21 | Cursor piscando dentro do ícone de laptop do header | global | extra | ✅ pronto |
| M-22 | Globo girando por arrasto do mouse, com inércia e retorno a Dublin | Location | extra | ✅ pronto — testado à mão em 04/09, com mouse |
| M-23 | Marcador de Dublin como pin com haste, ocultado pelo globo quando gira para trás | Location | extra | ✅ pronto |
| M-24 | Número de impacto contando até o valor final ao entrar na tela, uma vez só | Statement → Projetos | extra | ✅ pronto |
| M-25 | Fio roxo desenhando-se ao rolar, do hero até o globo, por `stroke-dashoffset` | global | extra | ✅ pronto |
| M-26 | Títulos de seção revelando palavra a palavra, reusando o `WordReveal` com stagger menor que o do hero | todas as seções | extra | ✅ pronto |
| M-27 | Links e botões magnéticos: o elemento se desloca até ~8 px na direção do cursor a ~60 px e volta ao sair | CTAs do Statement, copiar e-mail, baixar CV | extra | ✅ pronto |
| M-28 | Ponto de luz na ponta do fio, acompanhando o traço enquanto ele se desenha no scroll | global | extra | ✅ pronto |

**Regras duras do movimento.**

- Só `transform` e `opacity`. Animar `width`, `height`, `top` ou `left` obriga o
  navegador a recalcular layout a cada quadro e derruba o desempenho no celular.
  **Única exceção autorizada:** `stroke-dashoffset` no fio do M-25 — é uma
  propriedade de pintura de SVG, não dispara recálculo de layout, e é a única
  forma de desenhar um traço progressivamente sem redesenhar a geometria.
- Toda animação respeita `prefers-reduced-motion` (RF-08, RNF-06).
- Nada de animação que bloqueie a leitura: o texto do hero tem que estar legível
  mesmo se o JavaScript falhar.
- **Se um efeito não está nesta tabela, ele não entra na v1.** Ideia nova de
  animação vai para `docs/BACKLOG.md`.
- **Degradar para verdade, nunca para mentira.** Todo elemento cujo estado
  animado, interpretado ou parcial carrega significado precisa de um estado de
  repouso correto. Se a animação não rodar, se o parser não casar, se o dado
  vier torto — o que sobra na tela ainda tem que ser verdade. Um contador
  parado em zero e um parágrafo com metade do texto sumida não são falhas
  estéticas, são afirmações falsas. Origem: D-34 e D-38.
- **Medir o que a pessoa enxerga, não o que é conveniente de medir.** Já falhou
  três vezes neste projeto: `ch` entregando 100 caracteres onde a unidade
  prometia 68 (D-39); medição do `<img>` de fallback reportada como se fosse a
  cena 3D (D-41); e `getBoundingClientRect` medindo a caixa com padding em vez
  da tinta, o que quase provocou a "correção" de um alinhamento correto (D-42).
  Antes de aceitar um número, pergunte se ele descreve o que aparece na tela ou
  só o que a API devolve mais fácil. **Quarta e quinta ocorrências, no D-52:**
  `getBoundingClientRect()` num `<strong>` que quebra de linha devolve a
  **união** das caixas — 580 × 43 px de espaço quase todo vazio —, e amostrar
  ali media o fundo da página em vez do marca-texto, devolvendo o contraste
  errado; `getClientRects()`, que dá uma caixa por linha, resolveu. E o próprio
  medidor de leitura, em uso desde o D-39, calculava largura da caixa ÷ largura
  média de caractere — o que responde "quantos **caberiam**", não "quantos
  **há** por linha". Acusava três estouros inexistentes. O critério passou a
  dividir o texto pelo número de linhas que ele de fato ocupa; o máximo real da
  página é **75 caracteres**.
- **O que o olho lê e o que a máquina lê têm de ser a mesma frase.** Já
  apareceu por três caminhos diferentes, todos com build verde: no `h1`, onde a
  separação entre palavras era `margin` e não espaço, devolvendo
  `"RaulRodrigues"`; na frase do Statement (M-7), pela mesma causa, onde uma
  sentença de 56 caracteres virava **uma palavra só** — e é a frase da D-12,
  escrita pelo Raul, a que o Google indexa junto do `h1`; e no título da
  Location, onde um `<br />` sem espaço produzia `"Dublin,Ireland"`. Não é um
  bug, é uma classe: **separação visual não é separação textual.** `margin`,
  `<br>`, `position`, `flex gap` e pseudo-elementos separam pixels e não
  separam texto. Regra: qualquer componente que quebre uma frase em pedaços —
  para animar, para quebrar linha, para estilizar — é aceito só depois de
  conferir o `textContent` renderizado. Vale para leitor de tela, indexação e
  copiar-colar, ou seja, RNF-06 e RNF-07 ao mesmo tempo.
- **Simular o gatilho não é o gatilho.** No item de foco, `element.focus()` por
  script **não ativa `:focus-visible`** no Chrome — a medição estava lendo o
  anel padrão do navegador (3 px, offset 0), não o do site (2 px, offset 3 px).
  O estímulo chegou, mas era **outro** estímulo, e o site teria passado numa
  auditoria que nunca olhou para o que a pessoa vê ao apertar Tab. Refeito com
  `Input.dispatchKeyEvent` disparando Tab de verdade, apareceram dois defeitos
  reais. É o degrau seguinte da regra abaixo: não basta o estímulo chegar, ele
  tem de ser **o mesmo** que o do uso real.
- **Zero só prova alguma coisa se o estímulo tiver chegado.** Testar que um
  efeito está *desligado* exige duas medidas, não uma: que o gatilho ocorreu
  **e** que nada aconteceu. No M-27, o ímã sob `prefers-reduced-motion` mediu
  0 px de deslocamento — mas isso, sozinho, é indistinguível de "nenhum evento
  chegou". A prova veio de contar os `pointermove`: quatro chegaram, e o
  deslocamento continuou zero. Sem essa contagem, a guarda não estava
  verificada, estava só não contrariada. O contraexemplo apareceu no mesmo
  item: a primeira medição deu zero em tudo porque o botão estava a `top: -872`,
  fora da viewport, onde despachar mouse não gera evento nenhum — código certo,
  teste mudo.
- **Regex atravessando camadas de escape é a parte mais frágil do teste.** No
  M-28 o instrumento falhou três vezes seguidas e nas três o código estava
  certo: um regex escapado através de `bash` → Python → template literal do JS
  não casou com um atributo que estava ali no DOM; e `"\s"` virou `"s"` dentro
  de string JS, então `/\s+/` passou a dividir na letra **s** e o `split`
  devolvia um elemento só. Preferir `split(' ')` e comparação literal a regex
  em script que atravessa camadas — a elegância do regex não sobrevive ao
  escape, e o custo do erro é "consertar" código que não tem defeito.
- **Medição vazia não é prova de código quebrado — desconfie primeiro do
  instrumento.** Aconteceu duas vezes, do mesmo jeito: o teste devolvia nada e
  a conclusão fácil era "não renderiza". No fio (M-25), o recorte da captura
  estava em coordenadas erradas e fotografava outro lugar. No pin (M-23),
  `Page.captureScreenshot` com `clip` e sem `captureBeyondViewport` interpreta
  as coordenadas em espaço de **documento**, e a foto saiu do topo da página
  com a seção renderizada logo abaixo — além de `scrollIntoView` e
  `window.scrollTo` em chamadas separadas não rolarem nada, porque o rAF do
  Lenis desfaz a posição entre uma chamada e outra: a rolagem tem de acontecer
  dentro de um único script na página. Regra: antes de declarar que algo não
  renderiza, confirme pelo DOM — contexto WebGL vivo, `reveal` com
  `is-visible`, opacidade 1. **DOM íntegro e foto vazia = defeito do
  instrumento.**
- **Ninguém checa um mecanismo quando o resultado bate.** O `ARCHITECTURE.md`
  afirmou por semanas que "a pasta `three/` inteira entra por `React.lazy`".
  Não entra: o `Hero` importa o `HeroVisual` normalmente, porque é ele que
  decide, com o `useCanRender3D`, se monta a cena ou o `<img>` — e essa decisão
  tem de acontecer **antes** de qualquer chunk de 3D ser buscado. A frase
  sobreviveu porque a **conclusão** estava certa: o `three` fica em chunk
  próprio e nada fora da pasta o importa. Regra: quando o resultado bate,
  ninguém volta para conferir como ele foi obtido — então a descrição do
  mecanismo é onde a mentira se esconde por mais tempo. Vale para a regra 4 do
  `CLAUDE.md`, que está **certa** e não deve ser "corrigida" junto: ela fala de
  quem **importa** `three`, e o `HeroVisual` não importa.
- **Reduzir sem deixar ponteiro solto.** Tirar conteúdo de um documento não é
  apagar linhas: é remover o que saiu **e** o que apontava para ele. Na D-47,
  cortar os dez passos deixaria três resíduos — uma regra dizendo "todo prompt
  **aqui**" quando os prompts tinham acabado de sair, uma seção abrindo com
  "fora do fluxo dos dez passos", e um limiar duplicado com o que o substituiu.
  Referência que aponta para o vazio é exatamente o defeito que a redução
  existia para corrigir. Vale para código igual: remover uma função e deixar o
  comentário que a explicava é o mesmo erro.
- **Detector que acusa demais não achou nada — só fez barulho.** Na varredura
  da D-45, o primeiro detector marcou mais de 100 ocorrências, quase todas
  falso positivo, porque contava vãos sem distinguir irmãos de bloco
  (parágrafos, cards) de elementos inline dentro de uma frase. O sinal útil só
  apareceu na segunda passada, olhando **nome acessível** em vez de
  `textContent` cru. Regra: quando um teste acusa muito, o problema costuma ser
  o critério, não o código — refine o critério antes de sair corrigindo.

### 5.3 Requisitos funcionais

| ID | Requisito | Verificação |
|---|---|---|
| **RF-01** | Página única com navegação por âncora e scroll suave | Clicar em cada item do menu leva à seção certa, sem cortar o título sob o header |
| **RF-02** | Header fixo com versão mobile funcional | Abre e fecha em 360 px; fecha ao clicar num item |
| **RF-03** | Todo conteúdo vem de `src/data/`, nunca escrito no JSX | `grep` por texto visível nos componentes não retorna nada |
| **RF-04** | Cada card de projeto abre repositório e demo em nova aba | `target="_blank"` + `rel="noreferrer"` em todos |
| **RF-05** | Download do CV em PDF | `public/cv.pdf` existe e o botão baixa |
| **RF-06** | Cena 3D do hero conforme 5.2 | Ver restrições duras acima |
| **RF-07** | Tema claro como padrão, sem alternância | Um tema só na v1 |
| **RF-08** | Movimento conforme o inventário da seção 5.2.1 (M-1 a M-6) | Só `transform`/`opacity`; todos respeitam `prefers-reduced-motion` |
| **RF-09** | Medição de audiência sem cookies | Vercel Analytics ativo em produção (ver D-05) |

### 5.4 Requisitos não-funcionais

| ID | Requisito | Meta | Como medir |
|---|---|---|---|
| **RNF-01** | Lighthouse em produção, mobile | Performance ≥ 80 · Acessibilidade ≥ 90 · Best Practices ≥ 90 · SEO ≥ 90 | pagespeed.web.dev |
| **RNF-02** | Bundle JS inicial | ≤ 150 KB gzip, com `three` em chunk próprio carregado sob demanda | **Carga real em largura de celular, provando que `three` e `r3f` não aparecem na aba de rede** (D-62). A medição antiga — saída do `npm run build` — olhava o **tamanho** dos chunks e não **quem os baixa**, e por isso deixou passar 237 KB pré-carregados num aparelho que nunca monta a cena |
| **RNF-03** | Primeiro conteúdo visível | Texto do hero renderizado antes de qualquer JS de 3D | DevTools → Network, throttle 4G |
| **RNF-04** | Degradação sem WebGL | Site 100% utilizável; o Memoji aparece como imagem estática | Desligar WebGL no navegador |
| **RNF-05** | Responsivo | Sem scroll horizontal e sem texto cortado em 360, 768, 1024, 1440 e 1920 px. Nenhuma linha de texto corrido passando de ~75 caracteres em tela larga (D-31) | DevTools responsivo |
| **RNF-06** | Acessibilidade | Navegável só com Tab; contraste AA (4.5:1 em texto normal); canvas com `aria-hidden`; foco sempre visível | WAVE + navegação por teclado |
| **RNF-07** | SEO e preview social | `<title>`, meta description, Open Graph com imagem 1200×630, `robots.txt`, `sitemap.xml` | opengraph.xyz |
| **RNF-08** | Deploy | Push na `main` publica sozinho, em menos de 2 minutos | Painel da Vercel |
| **RNF-09** | Qualidade do inglês | Zero erro de gramática ou naturalidade no texto visível | Revisão por ferramenta **e** por uma pessoa fluente |
| **RNF-10** | Privacidade | Sem cookies, sem rastreador de terceiros, sem formulário — logo, sem banner de consentimento | Aba Application → Cookies vazia |
| **RNF-11** | Console limpo | Zero erro e zero warning em produção | DevTools → Console |

### 5.5 Suporte

| | |
|---|---|
| Navegadores | Chrome, Edge, Firefox e Safari — duas últimas versões |
| Sistemas | Windows, macOS, iOS, Android |
| Telas | 360 px a 1440 px |
| Sem WebGL | Suportado por degradação (RNF-04) |
| Internet Explorer | Não suportado |

---

## 6. Fora de escopo da v1

Isto **não** será construído agora. Não abra exceção; anote em `docs/BACKLOG.md`.

**3D**

- Mundo navegável, sala virtual, câmera controlada pelo scroll.
- Qualquer modelo `.glb` — nem notebook, nem avatar.
- Física, pós-processamento (bloom, DOF), shaders customizados.
- Memoji em vídeo ou avatar animado.

**Produto**

- Seção Serviços (D-07).
- Alternância de idioma PT/EN (D-06). ~~Alternância claro/escuro~~ — **saiu de
  fora de escopo em 05/09 por pedido explícito do Raul, repetido. Ver D-44**,
  que traz o custo escrito e a posição na fila. É o último item, e o primeiro a
  ser cortado se o prazo apertar.
- Página própria por projeto / estudo de caso.
- Blog, CMS, painel administrativo.
- Formulário de contato com backend, newsletter.
- Autenticação, área logada, banco de dados.

**Enfeite**

- Cursor customizado, preloader elaborado, transições entre páginas.
- **Emoji de bandeira** em qualquer lugar do site, e **foto da Irlanda** ou de
  Dublin (D-35). A origem é sinalizada em tipografia, não em imagem.

**Engenharia**

- Testes automatizados. O projeto não tem lógica de negócio que os justifique;
  a verificação é o `npm run build` mais a checagem visual de cada passo.

---

## 7. Inventário de conteúdo

O site vazio é um site morto. Nada disto é opcional para o lançamento.

| Item | Onde vive | Status |
|---|---|---|
| Frase de posicionamento (inglês, 1 linha) | `src/data/profile.ts` | ✅ pronto |
| Subtítulo do hero | `src/data/profile.ts` | ✅ pronto |
| Texto do Sobre (3–4 frases, inglês) | `src/data/profile.ts` | ✅ pronto |
| 3 projetos com impacto declarado | `src/data/projects.ts` | ✅ pronto |
| Stack agrupada por uso | `src/data/stack.ts` | ✅ pronto |
| Chamada final de contato | `src/data/profile.ts` | ✅ pronto |
| URL do LinkedIn | `src/data/profile.ts` | ✅ pronto |
| CV em PDF, em inglês | `public/cv.pdf` | ✅ pronto — versão corrigida (D-20) |
| Memoji recortado | `public/images/memoji.png` | ✅ pronto |
| Imagem Open Graph 1200×630 | `public/og.png` | ✅ pronto — gerada com o Memoji, 59 KB |
| Favicon próprio | `public/favicon.svg` | ✅ pronto — “R” na cor da marca |
| Capas dos projetos (opcional na v1) | `public/images/projects/` | ⬜ opcional |

**Projetos candidatos.** Cover Letter API (front estático + Cloudflare Worker +
OpenAI); automações B2B para clientes, descritas por setor sem quebrar sigilo;
Titanic no Kaggle (~0.78); e este próprio site.

**Regra do card de projeto.** Se você não consegue escrever o campo `impact`, o
projeto ainda não está pronto para entrar no site. "Usei React e Tailwind" não é
impacto. "Transforma uma tarefa de 30 minutos em menos de um minuto" é.

---

## 8. Métricas de sucesso

**Métrica de saída — a única que importa de verdade:** pelo menos **3 contatos
qualificados** (recrutador, fundador ou convite) originados do site nos três
primeiros meses no ar.

**Métricas de processo**

| Métrica | Meta | Fonte |
|---|---|---|
| Site publicado e estável | até 10/10/2026 | Vercel |
| Lighthouse performance (mobile) | ≥ 80 | pagespeed.web.dev |
| Visitantes que rolam até a seção Projetos | ≥ 60% | Vercel Analytics |
| Cliques no CTA de contato | ≥ 5% das visitas | Vercel Analytics |
| Erros no console em produção | 0 | DevTools |

---

## 9. Calendário

**Reescrita em 06/09.** O calendário original planejava os dez passos por
semana, de 04/09 a 08/10 — e **os dez foram executados em três dias**, entre 03
e 06/09. Um cronograma que descreve trabalho já feito é a mesma doença que a
D-47 diagnosticou no `WORKFLOW.md`: documento de plano fica falso no dia em que
o plano acaba. O que sobrou é curto e não é código.

| Data | O que | Depende de |
|---|---|---|
| **em aberto** | Revisão do inglês por pessoa fluente (RNF-09) | o Raul achar o revisor — **é o único item bloqueante do lançamento** |
| **07/09** | ✅ Domínio + HTTPS — `raulrodrigues.dev` | feito. Metadados conferidos no ar: `og:url`, `og:image` (200), JSON-LD, `robots.txt` e `sitemap.xml` **já apontam para o domínio novo** |
| ✅ **07/09** | Web Analytics ligado no painel (RF-09) | feito — o script responde 200 |
| ✅ **07/09** | Lighthouse rodado (RNF-01) | 7 das 8 notas passam |
| **em aberto** | **Performance mobile: 68 contra a meta de 80** (RNF-01) | as duas causas estão medidas na seção 0; falta decidir o conserto |
| **10/10** | Meta de publicação divulgável | os quatro acima |
| **26/10** | Embarque para Dublin | — |

**A ordem que importa agora é outra:** o que trava não é mais escrever código,
é o que depende de terceiros e de calendário. A revisão do inglês e o domínio
deveriam ter começado antes do modo escuro.

---

## 10. Definição de pronto para publicar

O site só é divulgado quando **todas** estas linhas estiverem marcadas. Sem
exceção, sem "depois eu arrumo".

**Conteúdo**

- [ ] Nenhum placeholder, nenhum "TODO", nenhum `EmptyState` visível
- [ ] Todos os itens da seção 7 marcados como prontos
- [ ] Todo card de projeto tem `impact` preenchido com resultado, não com feature
- [ ] Texto em inglês revisado por ferramenta **e** por uma pessoa fluente (RNF-09) — **as duas metades saem do pacote da D-64**: o corpo do arquivo é a passada da pessoa, e a passada de máquina fica no fim dele, depois do aviso para não ser lida antes

**Técnico**

- [x] `npm run build` passa sem erro e sem warning — conferido a cada commit desde 05/09
- [ ] Zero erro no console em produção (RNF-11)
- [x] Lighthouse mobile ≥ 80 / 90 / 90 / 90 (RNF-01) — **94 / 100 / 100 / 100** em 07/09, produção (desktop 99 / 100 / 100 / 100)
- [x] Testado em 360, 768, 1024 e 1440 px sem scroll horizontal (RNF-05) — ✅ remedido em produção a **360 px** em 07/09, depois do modo escuro e da troca de fonte: `scrollWidth` 360 contra `clientWidth` 360. Os elementos que passam da borda são os do `Marquee`, por construção, dentro de contêiner com `overflow` — e a cena 3D **não monta** (0 `canvas` a 360 px), como o RNF-04 manda
- [ ] Site inteiro navegável só com Tab, com foco visível (RNF-06) — ⚠ auditado em 05/09 (0 defeitos em 23 e 25 paradas) e o botão de tema medido em 06/09; **falta uma passada final**, porque desde a auditoria entraram o botão de tema e o `canonical`
- [ ] Testado com o WebGL desligado (RNF-04)
- [x] Nenhum cookie criado (RNF-10) — ✅ **fechado em 07/09**, lido em produção **com o Web Analytics já ligado e servindo**: `document.cookie` devolveu string vazia. Era a última dúvida do requisito

**Publicação**

- [x] Todos os links externos abrem e apontam para o lugar certo — ✅ conferidos em 07/09, os 12 do site: GitHub, LinkedIn, os dois repositórios, `mailto:` e `/cv.pdf` (responde 200). A página do **Cover Letter Assistant** foi aberta de verdade, não só pingada: carrega conteúdo real, não é o 404 do GitHub Pages
- [x] `og.png`, `favicon`, `robots.txt` e `sitemap.xml` no lugar (RNF-07) — conferidos **no ar** em 07/09, já no domínio novo; `og.png` responde 200
- [ ] Preview do link conferido no WhatsApp e no LinkedIn
- [x] Domínio apontado e HTTPS ativo — `raulrodrigues.dev`, 07/09
- [ ] Link enviado para 5 pessoas e feedback aplicado antes da divulgação ampla

---

## 11. Riscos

| Risco | Probabilidade | Por que acontece | Mitigação |
|---|---|---|---|
| **Escopo escorregando** | Alta | Foi o que matou a v1 | A seção 6 é lei. Ideia nova vai para `BACKLOG.md` na mesma hora |
| **Site bonito e vazio** | Alta | Escrever é mais difícil que codar | A seção 7 é bloqueante. Semanas 1 e 3 são inegociáveis |
| **Perfeccionismo na cena 3D** | Alta | Luz e posição nunca ficam "boas o bastante" | Timebox de 2 sessões no Passo 4. Depois, congela |
| **Inglês com erro no hero** | Média | Não é a língua nativa e o hero é a primeira coisa que o recrutador lê | RNF-09: revisão dupla, uma delas humana |
| **Retrabalho por prompt vago** | Média | Causa principal da falha da v1 | Prompts prontos no `WORKFLOW.md`, cada um citando RF/RNF |
| **Performance no mobile** | Média | Three.js é pesado por natureza | RNF-02 e RNF-03 conferidos a cada passo, não no fim |
| **Prazo da viagem** | Baixa | 26/10 é data fixa | Meta interna 10/10, com 16 dias de folga |
| **Perder o trabalho** | Baixa | Branch quebrada, arquivo sobrescrito | Um commit por passo; `main` só recebe merge do que buildou |

---

## 12. Decisões técnicas travadas

Mudar qualquer item exige uma razão escrita no log da seção 14 — não um impulso.

| Área | Decisão |
|---|---|
| **Stack** | Vite 8 · React 19 · TypeScript · React Three Fiber · drei · Tailwind CSS v4 |
| **Tema** | O tema mora em `src/index.css`, dentro de `@theme`. **Não existe `tailwind.config.js`** |
| **Paleta** | Fundo `#fbfbfe` · superfície `#ffffff` · texto `#000000` · secundário `#4a4a5a` · borda `#e7e4ef` · roxo suave `#8c62ac` (decorativo) · roxo forte `oklch(0.438 0.218 303.724)` = `#6e11b0` (links, botões, todo texto colorido) |
| **Tipografia** | Sansation via Google Fonts — pesos 300, 400 e 700 apenas. Mono do sistema para detalhes técnicos. **Nunca usar `font-medium` ou `font-semibold`**: a fonte não tem esses pesos e o navegador sintetiza um resultado feio |
| **Idioma** | Inglês. Sem versão em português na v1 |
| **Hero** | Memoji como textura em cena R3F, sem `.glb` |
| **Repositório** | `portfolio-3d-v2`, novo e limpo |
| **Branches** | **Uma `main` só.** Todo commit vai direto nela, e o build tem de passar antes do push. Este documento afirmou até 05/09 que havia "uma branch por passo" — era falso desde o primeiro commit; o histórico nunca teve outra branch. Corrigido pelo D-47 |
| **Deploy** | Vercel, automático a partir da `main` |
| **Analytics** | Vercel Analytics — sem cookies, sem banner |
| **Modo de trabalho** | Esqueleto e documentação montados com o Claude; cada passo executado pelo Raul no Claude Code, um por sessão |

---

## 13. Licenças e créditos

| Ativo | Origem | Situação |
|---|---|---|
| Sansation | Google Fonts | Livre para uso na web |
| Memoji | Apple, gerado pelo próprio Raul | Recurso da Apple, feito para uso dentro dos apps dela. Uso pessoal no próprio portfólio é comum, mas **não é um ativo que o Raul possui**. Se o site virar peça comercial, trocar por um avatar próprio é uma linha de código |
| Ícone da Apple no notebook do Memoji | Apple | Único logo de marca do site. Pode ser removido por retoque, se preferir |
| React, Three.js, R3F, drei, Tailwind | MIT | Livres |

---

## 14. Log de decisões

Toda mudança de rumo vive aqui, com data e motivo. É a memória do projeto.

> **O arquivo é CONGELADO.** Nada nele se edita, renumera ou corrige — ele só
> se lê. Se uma decisão arquivada precisar mudar de status ou ganhar adendo,
> isso acontece **aqui**, na linha que ficou. É o que impede os dois documentos
> de virarem duas verdades: um é vivo, o outro é fotografia.
>
> **D-01 a D-53 estão resumidas aqui e completas em `DECISOES-ARQUIVADAS.md`.**
> Todas foram entregues e nenhuma dirige trabalho pendente; os IDs continuam
> aqui, com a decisão inteira e o status, para que toda referência do projeto
> resolva. O **motivo** — que é a parte longa e é história — mora no arquivo.

| ID | Data | Decisão | Motivo | Status |
|---|---|---|---|---|
| **D-01** | 03/09 | Recomeçar do zero com PRD, em vez de consertar a v1 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-02** | 03/09 | Site clássico com toques 3D, não mundo navegável | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-03** | 03/09 | React + R3F em vez de Three.js puro | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-04** | 03/09 | Tema claro com roxo, tipografia Sansation | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-05** | 03/09 | Vercel Analytics entra na v1 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-06** | 03/09 | Sem PT/EN na v1 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-07** | 03/09 | Seção Serviços cortada | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-08** | 03/09 | Hero com notebook 3D em `.glb` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | **Revertida por D-10** |
| **D-09** | 03/09 | Referência eric-cole.framer.website inspecionada | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-10** | 03/09 | Hero com o Memoji como textura em cena R3F, sem `.glb` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-11** | 03/09 | Memoji extraído da versão de 1024 px, não da transparente de 512 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-12** | 03/09 | Headline: *"I build systems that take repetitive work off people's calendars"* | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-13** | 03/09 | E-mail do site: `raulrodrigues.mldev@gmail.com` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-14** | 03/09 | A v1 sobe com 3 projetos: 1 em destaque, 2 compactos | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-15** | 03/09 | Títulos de seção corrigidos para inglês | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-16** | 03/09 | Criada a seção 5.2.1 — linguagem de movimento, com os 6 efeitos permitidos na v1 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-17** | 03/09 | Hero reduzido a nome + função; posicionamento, CTAs e disponibilidade descem para uma seção `Statement` revelada no scroll | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-18** | 03/09 | Memoji com `alphaTest` e `depthWrite`, e órbitas centradas em z=0 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-19** | 03/09 | Segunda cena WebGL permitida: o globo da seção Location | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-20** | 03/09 | CV reescrito e corrigido | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Resolvida |
| **D-21** | 03/09 | O CV revelou material para um 4º card: "B2B Websites with Geolocation" | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | **REABERTA em 12/09 — e ela NUNCA foi entregue. Eu a fechei por engano.** Quando corrigi os quinze status errados na v9.4, fechei esta junto **por atacado, sem conferir**: exatamente a falha que eu tinha acabado de acusar o documento de ter. O site continua com **três cards** e nenhum deles é trabalho pago. **O que a travava deixou de existir.** A decisão de 03/09 dizia "falta só o número de impacto" — e o Raul deu o número em 12/09: **leads ficavam até duas semanas sem resposta**. **Por que este é o card mais importante do site, e não um quarto qualquer:** os três atuais — Cover Letter Assistant, Titanic e o próprio site — **são todos projetos pessoais**. Para alguém que se apresenta como **AI Solutions Engineer**, que resolve problema de negócio, o portfólio mostra três trabalhos de estudo e **zero prova de que alguém pagou**. O CV já carrega essa prova; o site não. **É a maior lacuna que sobrou, e ela é de conteúdo, não de código.** **O conteúdo do card, pronto:** `title` **AI automation for four companies**; `problem` *"Leads were sitting unanswered for up to two weeks. By the time anyone replied the enquiry had gone cold — the delay was costing the sale, not just the hours."*; `solution` *"AI automation across the channels the leads actually arrive on — email, WhatsApp Business, a chatbot and the spreadsheets the team already lived in — so an enquiry gets picked up, sorted and routed without anyone watching an inbox."*; `impact` *"Leads that used to wait up to two weeks are now picked up and routed automatically."*; `stack` Python, n8n, OpenAI API, WhatsApp Business. **Sem `repo` nem `demo`: é trabalho de cliente e o código não é público** — e isso precisa ler como escolha, não como card quebrado. **O que precisa ser MEDIDO antes de fechar, e é o risco real:** o **D-14** fixou "três projetos: um em destaque, dois compactos", e a grade dos compactos é de **duas colunas**. Com três compactos, **um fica sozinho numa linha** — exatamente o "item órfão" que o próprio projeto evita em toda parte. Conferir como a grade se comporta e, se sobrar órfão, dizer isso **antes** de escolher entre reorganizar a grade ou promover o card novo a destaque. | **Entregue e fechada em 12/09 — agora conferido na árvore, não no documento.** O card está no ar desde `78cf4d0` e a D-81 o promoveu a destaque. **Esta linha ficou dois dias discordando da §0**, que já o marcava entregue, e quem achou foi o Claude Code — não por ler o PRD, mas por comparar o PRD com o repositório. **Vale guardar a assimetria:** a v9.4 fechou o que não estava feito; esta deixou aberto o que estava. **O segundo erro é o seguro e ainda assim é erro** — status que não bate com a árvore faz a próxima sessão trabalhar duas vezes ou não trabalhar nenhuma. **A regra que sai daí, e ela vale mais que esta linha:** *status de decisão se confere contra o código, nunca contra outra parte do documento* — foi o documento concordando consigo mesmo que deixou o erro da v9.4 passar |
| **D-22** | 03/09 | Hero em layout de cantos: nome em cima à esquerda, função em cima à direita, cena no centro. Marca do header virou um ícone de laptop | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-23** | 03/09 | Container passa de 1024 px para 1280 px de largura máxima | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | **Ampliada pelo D-31** |
| **D-24** | 03/09 | Globo passa a ser arrastável com o mouse, com o usuário tendo prioridade sobre o scroll | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-25** | 03/09 | Marcador de Dublin vira pin com haste, no lugar do círculo rente à superfície | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-25a** | 04/09 | Nota de execução do M-23: o pin lê como **ponto** na pose de repouso | Consequência geométrica, não defeito: a haste sai perpendicular à superfície e o D-28 assenta Dublin **de frente para a câmera**, que é exatamente o ângulo onde a haste fica encurtada a zero. Girando o globo ela aparece inteira, com cabeça e sombra própria. **A oclusão foi confirmada por arrasto real** (eventos de mouse do CDP, 960 px ≈ 275°): a 180° a haste sai de trás da silhueta com a base cortada pela esfera — se o teste de profundidade estivesse falhando, o disco e o anel apareceriam por cima da face do globo. O pin **não some por completo** quando Dublin passa para trás, e isso também é geometria: por causa da inclinação em X que o D-28 aplica, o caminho de Dublin passa perto do topo, e um pin radial na borda projeta para fora da silhueta — como um pin de verdade num globo de verdade. Se um dia se quiser que ele leia como pin também em repouso, o caminho é inclinar a haste alguns graus fora da normal ou assentar Dublin ligeiramente fora do centro — os dois mexem no D-25 ou no D-28, e nenhum é conserto de bug | Ativa |
| **D-26** | 03/09 | `prefers-reduced-motion` hoje desliga as cenas 3D inteiras, não só o movimento | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-27** | 03/09 | O movimento ocioso do globo muda de **tipo** com a proximidade, não só de intensidade: giro ao longe, oscilação de perto | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | **Substituída por D-28** |
| **D-28** | 03/09 | O gatilho do "apontar Dublin" deixa de ser a POSIÇÃO do scroll e passa a ser o REPOUSO | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-29** | 04/09 | A captura do arrasto sai do `<canvas>` e passa a cobrir a área visível do globo; o critério de classificação do gesto é afrouxado | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-30** | 04/09 | A velocidade do gesto passa a sobreviver ao `pointerup`: soltar o globo o deixa girando e desacelerando | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-31** | 04/09 | O Container vai a **96 rem** (1536 px) **e** todo texto corrido ganha limite de leitura de 68 ch. Substitui o número do D-23 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-32** | 04/09 | Seções com metade direita vazia recebem uma coluna curta de metadados em mono | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-33** | 04/09 | Rótulos de seção ganham numeração: `01 / ABOUT`, `02 / WORK`, `03 / STACK` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-34** | 04/09 | Um número de impacto sai de dentro do card e vira tipografia grande entre o Statement e os Projetos | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-35** | 04/09 | Bandeira em emoji e foto da Irlanda **rejeitadas**; a origem é sinalizada por um marcador `BR → IE` em mono, na cor da marca | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-36** | 04/09 | Grão: uma textura de ruído sobre a página inteira, opacidade ~0,02, `pointer-events: none` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-37** | 04/09 | Um fio roxo único atravessa a página: nasce numa órbita do hero, desce pela lateral passando atrás dos números das seções, e termina junto ao globo.… | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-38** | 04/09 | O conteúdo passa a admitir ênfase tipográfica: um trecho por parágrafo pode ser destacado, e o destaque vive no dado, não no JSX | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-39** | 04/09 | O limite de leitura sai de `ch` e passa a ser declarado em `rem`, com aceite por medição. **O D-31 não está entregue** | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-40** | 04/09 | O primeiro parágrafo do About vira *lead*: corpo maior, o resto inalterado | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | **Absorvida pelo D-52** |
| **D-41** | 04/09 | O hero passa a ter uma linha de alinhamento compartilhada: o palco do Memoji vira coluna própria à direita a partir de `lg`, e um filho flex que ocupa a… | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-42** | 04/09 | O header vira três partes — marca, navegação centrada, ação — e ganha um `Get in touch` fixo apontando para `#contact` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-43** | 05/09 | O Memoji volta ao **centro** da área de conteúdo. O experimento de alinhá-lo à borda direita foi testado e **descartado** | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-44** | 05/09 | **Modo escuro com botão de alternância entra na v1**, e entra por último. Reverte a exclusão da seção 6 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-45** | 05/09 | Varredura de texto visual × texto de máquina concluída. **Corrigido:** o `<br />` da Location. **Decidido NÃO corrigir:** a fita da Stack e os irmãos… | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Resolvida |
| **D-46** | 05/09 | As setas decorativas dos links ganham `aria-hidden="true"` — quatro lugares | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-47** | 05/09 | O `WORKFLOW.md` perde o plano e vira **documento de método**. O `CLAUDE.md` deixa de apontá-lo como fonte do "próximo passo" | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-48** | 05/09 | O `ARCHITECTURE.md` é corrigido, e o **contrato de camadas** passa a ser documentado. O `Thread.tsx` muda de `ui/` para `layout/` | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-49** | 05/09 | **O contrato de camadas e o modo escuro colidem — e o D-44 é o gatilho mais provável da falha.** Antes de qualquer código de tema, releia a regra 5 do… | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-50** | 05/09 | O `h2` da Location fica **fora** do reveal do M-26, de propósito | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-51** | 05/09 | O fio (M-25) ganha um **ponto de luz na ponta** que acompanha o traço enquanto ele se desenha — M-28. **Fecha o inventário de movimento: nada entra depois** | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Concluída |
| **D-52** | 05/09 | **Contraste entre blocos**, em três movimentos. Absorve e substitui o D-40 | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-53** | 05/09 | **A hierarquia do D-52 desce para a metade de baixo da página**: Stack e rodapé. E uma regra nova separa onde o marca-texto entra | 📦 entregue e fechada — motivo completo em `DECISOES-ARQUIVADAS.md` | Ativa |
| **D-54** | 05/09 | O globo passa a girar **volta completa nos dois eixos**, inclusive de cabeça para baixo, e o arrasto só **começa** perto do planeta. **Escrita depois de ler o código, corrigindo uma especificação minha que estava errada** | O Raul pediu duas coisas. Antes de especificar, li `Globe.tsx`, `useGlobeDrag.ts` e `Location.tsx` — e a leitura mudou o item inteiro. **(b) A rotação nos dois eixos JÁ EXISTE e já funciona.** O hook define `allowVertical = e.pointerType === 'mouse'` e acumula `offset.x`; o `Globe` aplica `g.rotation.x`, transfere a inércia de inclinação na soltura (`tiltVel`) e assenta o eixo X no repouso. Nada disso precisa ser construído. **O que impede a volta completa é uma constante:** `MAX_TILT = Math.PI / 3` — ±60°, com o comentário dizendo literalmente "para o globo nunca virar de cabeça para baixo" — e o bloco de clamp que a aplica sobre o valor composto. Liberar é remover o teto. **Mas removê-lo quebra o assentamento**, e essa é a parte que não pode ser esquecida: o repouso do eixo X persegue `target.x` **cru**, então depois de algumas cambalhotas ele desenrolaria várias voltas para trás — exatamente o defeito que o D-28 resolveu no eixo Y com o ângulo congruente mais próximo (`Math.round((base.y − target.y) / 2π)`). O eixo X passa a precisar do mesmo tratamento. **Consequência assumida:** `rotation.x` e `rotation.y` são ângulos de Euler; passando de ±90° em X, o arrasto horizontal **inverte a sensação**, porque o "cima" do globo virou. Isso é inerente a rotação estilo turntable e só se resolve com acumulação por quaternião (trackball de verdade). **Decisão: aceitar a inversão nesta versão** — objeto real de cabeça para baixo se comporta assim, e trocar por quaternião é reescrever o núcleo de rotação que custou D-28 e D-30. Se incomodar na tela, vira item próprio. **(a) Onde o arrasto começa.** Os listeners estão na `<section>` (D-29) e continuam lá — mexer nisso reintroduz o defeito do canvas menor que a área aparente. O que muda é a **origem**: um `pointerdown` que nasce fora da caixa do globo é ignorado. Restringe-se onde o gesto **começa**, nunca onde ele continua — soltar o globo porque o mouse saiu da bola no meio do arrasto seria pior que o problema original. A caixa sai do `<div>` que embrulha a cena, que já é bem menor que a seção (56% da largura no desktop), aproximada a um quadrado centrado com margem. **Achado extra da leitura:** esse `<div>` tem `cursor-grab` **e** `pointer-events-none` ao mesmo tempo — o cursor de mão nunca aparece, porque o elemento não recebe ponteiro. Hoje só existe `grabbing`, durante o arrasto. Com a zona menor, mostrar `grab` ao entrar nela deixa de ser enfeite e vira o que **ensina** onde pegar. **Limite duro:** nada pode desfazer D-28, D-29 ou D-30 | Ativa |
| **D-55** | 05/09 | **Soltar o globo depois de girá-lo deixa de assentar em Dublin: quem arrastou quer brincar, quem rolou quer ler.** O repouso passa a distinguir o TIPO de interação, não só o tempo desde a última | Depois do D-54 o Raul girou o globo e reclamou: solta e ele para. **Não é defeito — é o D-28 executando o que está escrito.** A inércia decai em ~0,7 s (`0.25^delta`) e, a 1,5 s sem interação, `resting` assenta em Dublin e trava. "Mostrar Dublin parado" e "mundinho girando" são **incompatíveis por construção**, e nenhum ajuste de número concilia os dois — mesma armadilha do D-27, que tentou resolver com intensidade um conflito que era de regime. A saída é a mesma do D-28, um degrau acima: aquele unificou rolagem e arrasto num **carimbo** só; este os separa por **intenção**. Rolar é ler a página, e o leitor merece ver Dublin de frente. Arrastar é brincar, e quem brinca não quer que o brinquedo se arrume sozinho. Regra: **um arrasto suspende o assentamento automático**; o globo mantém o giro livre indefinidamente, e volta a apontar Dublin **na próxima rolagem** — que é exatamente o instante em que a mensagem "eu moro em Dublin" volta a importar. Implementação esperada: um sinalizador ligado na soltura do arrasto e desligado no `scroll`, lido junto de `resting`. O `lastInteraction` do D-28 continua existindo e continua único; o que se acrescenta é *de onde* veio a última interação. **Não** mexer no decaimento da inércia nem no `REST_DELAY`: o problema não é a velocidade, é o destino  **Adendo de 07/09 — o `livre` precisa de prazo, e sem ele o D-55 come o D-28.** Relato do Raul: *"o mundinho quando para não volta para Dublin"*. Lido no código, não é regressão nova: `resting = !dragging && !livre && agora - lastInteraction > REST_DELAY`, e **a flag `livre` só é desligada por um evento de rolagem**. Quem arrasta e continua olhando a seção nunca rola — então `livre` fica ligada para sempre, o globo gira indefinidamente e **o assentamento do D-28 nunca roda**. Os dois pedidos do Raul são compatíveis; o que faltou foi um prazo. **Regra:** `livre` **expira sozinha** depois de uma janela generosa sem interação — ordem de **10 s**, contra os 1,5 s do `REST_DELAY` —, e aí o globo volta a apontar Dublin sem a pessoa fazer nada. A rolagem continua limpando na hora, como hoje. O número tem de ser grande o bastante para o giro livre ainda ser sentido como "ele continua rodando" (era o pedido original do D-55) e pequeno o bastante para a pessoa ver a volta antes de sair da seção. **Aceite:** arrastar, soltar e **não tocar em mais nada** — o globo gira por ~10 s e depois assenta em Dublin; medido, não olhado. **ENTREGUE em `4ffa808`.** Entrou `LIVRE_TIMEOUT = 10_000`, e a decisão de execução que vale registro é **como** o prazo é contado: ele se mede contra o mesmo `lastInteraction` que já governa o repouso do D-28, e **não** por um `setTimeout`. O resultado hoje seria o mesmo, mas o temporizador criaria o relógio paralelo que o D-28 evitou de propósito — aquela decisão diz textualmente "um carimbo só governa os dois repousos; não existe um segundo" — e ainda precisaria ser cancelado e rearmado a cada gesto, que é superfície para o mesmo tipo de bug do D-59. Segunda escolha: quando o prazo vence, a flag é **apagada de verdade** em vez de apenas ignorada na conta do `resting`, para o estado não passar a mentir para quem o ler depois. A rolagem continua limpando na hora, como o adendo pediu. | Ativa |
| **D-56** | 06/09 | **O globo inverte: oceano roxo, continentes cinza-claros.** Mais uma malha de triângulos por cima da esfera. Vem **antes** do D-44 | Referência trazida pelo Raul: um loop 3D do Envato Elements (`3d-globe-seamlessly-rotating-purple`, autor PixoArt). **É referência, não ativo** — é um vídeo por assinatura, e vídeo como textura mataria o arrasto do M-22 e do D-54: não se gira um vídeo. Mas a geometria já é nossa; o que faz a referência parecer o que parece é **material e textura**, e os dois estão sob nosso controle. **A inversão.** Hoje: esfera quase branca (`#fbfafd`) com continentes em pontos roxos. Passa a ser: **esfera em roxo** e **continentes em cinza claro**. Exige regerar o `world-dots.png` com os continentes claros, pelo mesmo caminho que o criou — contornos do Natural Earth (`land-110m`, domínio público) rasterizados, 11 KB, sem foto de satélite (D-19). **A malha.** Uma esfera *wireframe* de baixa contagem de polígonos, um fio maior que o corpo, em roxo mais claro e opacidade baixa: é ela que dá o ar técnico da referência, custa ~10 linhas e zero KB. **O que precisa ser reavaliado junto:** a casca de atmosfera (`BackSide` + `AdditiveBlending`) foi calibrada para brilhar sobre um corpo **claro** — sobre um corpo roxo ela pode sumir ou saturar; e o pin de Dublin (M-23) e seu anel foram escolhidos para contrastar com o branco. **Correção de um risco que eu levantei e estava errado:** eu disse que um globo roxo brigaria com texto por cima no mobile. Não brigaria — abaixo de 640 px a cena **não é montada** (RNF-04), então não existe texto sobre o globo em nenhuma largura. O risco real é outro e é de gosto: um corpo saturado grande numa página `#fbfbfe`. Mitigação: usar o `primary` suave (`#8c62ac`), não o `primary-deep`, e julgar na tela. **Ordem:** este item vem **antes** do D-44, porque o modo escuro vai repintar esta cena e fazer na ordem inversa é retrabalho garantido. No tema escuro o mesmo esquema tende a ficar ainda melhor — o oceano roxo escurece e os continentes claros ganham contraste —, então o D-44 **adapta** este desenho em vez de inventar outro. **Medir:** contraste dos continentes contra o oceano, e que a malha não empaste a leitura dos continentes. **O que sobrevive intacto, verificado no código (06/09):** giro, arrasto, inércia e assentamento são `g.rotation` no grupo — material não os toca, então D-28, D-30, D-54 e D-55 seguem iguais; e a **oclusão do pin continua de graça**, porque ela depende da esfera opaca escrever profundidade e a esfera continua opaca, só muda de cor. **O que NÃO sobrevive, e é preciso:** o `Marker` inteiro é `#6e11b0` — roxo escuro sobre esfera quase branca, hoje com contraste altíssimo. Depois da inversão, **o pin fica bem**, porque Dublin está na Irlanda e a Irlanda vira continente cinza-claro; **o anel de radar é que quebra**: ele cresce 2,2× para fora, **atravessa a costa** e vai pulsar no oceano roxo, onde roxo escuro sobre roxo médio some. O anel precisa de valor próprio — claro, ou com contorno —, decidido contra **as duas** superfícies que ele cruza, não só contra a terra. **Detalhe da malha:** o *wireframe* não pode cruzar por cima do marcador, senão embaralha a única coisa da cena que carrega informação; ou fica atrás dele, ou é sutil o bastante para não competir. **⚠ O D-56 é PRÉ-REQUISITO da frente 2 do D-44, e dissolve o problema dela.** O inventário do modo escuro achou que os continentes são um PNG com a cor assada dentro, desenhado com `<meshBasicMaterial map={texture} />` sem `color` — e cor de material **multiplica** a textura: dá para escurecer, nunca para clarear. Com a textura roxa atual, o tema escuro só teria a saída de gerar um **segundo PNG** (+11 KB e um par de ativos a manter) ou aceitar continentes apagados. **A textura clara do D-56 elimina a escolha:** partindo de cinza quase branco, multiplicar chega a qualquer valor nos dois temas com **uma textura só**. E o oceano deixa de ser textura e vira `color` sólida, que é trivialmente temável. Fazer o D-44 antes do D-56 é escolher entre duas saídas ruins de um problema que o D-56 apaga. **Entregue em `653b66b`, e a textura NÃO foi regerada — a execução achou um caminho melhor.** O `world-dots.png` é PNG **de 1 bit, indexado**, com paleta de duas entradas: `[0]` transparente e `[1]` o roxo. Os contornos do Natural Earth já estavam lá dentro; a cor eram **três bytes num chunk `PLTE`**. Trocar esses três bytes e recalcular o CRC deu 11.372 bytes — **exatamente os mesmos**, geometria byte a byte idêntica. Por que isso importa mais do que economia: **regerar teria de reproduzir a mesma projeção equirretangular, e meio grau de deslocamento poria Dublin no meio do Atlântico sem quebrar build, lint nem teste.** Trocar a paleta tem risco zero disso. O caminho óbvio foi tentado antes e falhou: recolorir por canvas e reexportar deu **155 KB contra 11 KB**, porque o encoder do canvas escreve RGBA cru e perde o indexado — teria destruído o "custo marginal quase zero" que justificou a segunda cena no D-19. **Lição geral: mudar a apresentação do dado é mais seguro que regerar o dado.** **O anel virou duas faixas concêntricas, e a medição provou que precisava:** nenhuma cor única serve — a faixa escura dá 7,32:1 sobre o continente e 2,11:1 sobre o oceano; a clara dá 4,20:1 sobre o oceano e 1,21:1 sobre o continente. Com duas faixas, onde uma some a outra aparece, em qualquer ponto do pulso. **Medições finais:** continentes `rgb(237,231,244)` contra oceano `rgb(154,103,183)` = **3,47:1**, ou 3,89:1 sem a malha — o custo da malha é 0,42 e o resultado segue acima do limiar de 3:1 para gráfico não textual. A malha ficou em `RADIUS × 1.003`, entre continentes e marcador, com `depthWrite: false`, então **nunca cruza por cima do pin por construção**, não por ajuste; afinada de 24×16 para 18×12 porque os meridianos convergindo no polo criavam um estrelado denso. A atmosfera sobreviveu: mudou de sentido — era brilho roxo sobre corpo claro, virou halo pálido em volta de bola roxa — e continua lendo como atmosfera. **Ponto em aberto, para o olho do Raul:** nesta escala o pin é **maior que a Irlanda**, então cobre a ilha em vez de pousar sobre ela. A geometria não mudou; ficou aparente porque o continente agora é claro. Se incomodar, é reduzir cabeça e pé do pin — número, não estrutura | Ativa |
| **D-57** | 06/09 | **⚠ BLOQUEADOR: o `useAmbientTint` (M-10) impede o tema escuro de pintar.** É a primeira frente do D-44, antes de qualquer token | Achado pelo Claude Code no inventário, antes de escrever código — exatamente o que a exigência de "traga o plano antes" existe para produzir. O hook escreve `--ambient` com um `rgb()` **absoluto** (`rgb(251 251 254)` e companhia), e o `<html>` usa `background-color: var(--ambient, var(--color-canvas))`. **O fallback só vale se a variável não existir** — e com o hook rodando ela sempre existe. Consequência: o M-10 sobrescreve o fundo a cada quadro de rolagem, em claro, para sempre. Isso **não** é "o tom ambiente fica esquisito no escuro"; é **"o modo escuro não funciona"** — e seria descoberto no meio da implementação, com meia paleta pronta e ninguém entendendo por que a página continua branca. O hook passa a conhecer o tema e a ter jogo próprio de paradas. **Parentesco com a D-49:** as duas são a mesma família — algo pintando o fundo por um caminho que o tema não controla. A D-49 avisou sobre o `body { background-color }` que alguém *ia* escrever; esta é a versão que **já está no código**. **Entregue em `d17ee23`.** Medido: com `data-theme="dark"`, o `<html>` segue as paradas escuras — `rgb(28 20 37)` no meio e `rgb(20 17 25)` no fim —, e o tema claro ficou byte a byte como antes, sem regressão. **⚠ Achado que atravessa frentes:** o `apply()` só roda em evento de **scroll**, então alternar o tema **parado no topo não repinta** até a primeira rolagem. Na carga normal o script inline da frente 6 resolve; mas **o botão da frente 7 tem de forçar um reapply do tint ao trocar de tema**, senão a pessoa clica no sol e a página só muda quando ela rola. Anotado também em comentário no `useAmbientTint.ts`, para não virar "bug misterioso" duas frentes adiante | Ativa |
| **D-58** | 06/09 | Duas correções do Raul olhando o tema escuro na tela: **(a) os continentes do globo passam a ser SÓLIDOS, não pontilhados** — a textura é regerada de verdade; **(b) as partículas do hero ficam brancas no escuro** | **(a) O D-56 entregou metade do pedido.** A textura sempre foi um mosaico de pontos — está escrito no D-19, "textura pontilhada" — e trocar os três bytes do `PLTE` manteve os pontos, mudando só a cor deles. O pedido original era **continente branco**, massa sólida, e é o que a referência do Envato mostra. Agora a regeração é inevitável, e o risco que o D-56 evitou volta: reproduzir a projeção equirretangular e não deslocar a geografia. **Mitigação obrigatória, e é verificável:** depois de gerar, conferir que **o pin de Dublin cai sobre a Irlanda** e que a linha de costa fecha — se o pin cair no mar, a projeção está errada, e isso não quebra build, lint nem teste. Manter o PNG **indexado de 1 bit**, como o atual, para não repetir os 155 KB do caminho por canvas; terra preenchida comprime bem, e o teto continua sendo a promessa de custo marginal quase zero do D-19. **Consequência a remedir:** com massa sólida em vez de pontos, o contraste contra o oceano roxo **sobe**, e o pin, o anel de duas faixas (D-56) e a malha *wireframe* precisam ser reconferidos sobre a superfície nova. **(b) Partículas brancas.** No escuro o roxo decorativo se perde contra o fundo escuro — o Raul viu e está certo. **Mas é decisão POR TEMA, não troca global:** partícula branca sobre a página quase branca do tema claro simplesmente some. Então roxo no claro, branco no escuro, e a intensidade **cai** no escuro em vez de subir, porque `AdditiveBlending` sobre fundo escuro estoura (já registrado no adendo do D-44). Isto pertence à **frente 2** do D-44 e é feito lá, não antes. **Reforço de 06/09:** o pedido é o desenho da referência do Envato — **massa branca sólida**, não pontilhado recolorido. **(a) ENTREGUE em `2894e85`.** Regerado do mesmo Natural Earth land-110m, mesma projeção, mesmo PNG indexado de 1 bit: 15,6 KB contra 11,4 KB. **A mitigação exigida foi cumprida por medição:** os 4.740 pontos do PNG antigo tiveram o **centro** comparado com a terra nova — 96,90% em terra cravada, 99,56% dentro de meia célula do próprio retículo; o resto é franja de costa, porque o gerador antigo marcava a célula e o centro dela podia cair na água. Mais 28 de 30 cidades e mares de controle. E o aceite literal, com o marcador **escondido** para poder olhar embaixo dele: o centro do globo assentado é continente, na borda leste de uma ilha de 20×22 px, com o Mar da Irlanda e a Grã-Bretanha a leste. **A armadilha real não era a prevista.** Não houve deslocamento de projeção; o que quase estragou o mapa foi a **Antártida**: ela circunda o polo, e o anel dela vai de -180° para leste até 178,3° e volta ao início por um salto que atravessa o mapa inteiro em -84,5°. Fechado assim, o preenchimento par-ímpar repartia o continente em faixas — a costa em -70° saindo água e o polo também. O fecho tem de ser **pelo polo**. Registrado no `README.md` de `public/images/`, porque não quebra build, lint nem teste: só faz o mapa mentir. Medição extra: o filtro de linha do PNG fica em `None`, porque com `Up` o arquivo sobe para 17,6 KB — o deflate já casa a linha inteira com a de cima e o filtro destrói esse casamento. **Consequências remedidas:** continente × oceano 3,65:1 sem malha e 3,49:1 com ela (custo da malha caiu de 0,42 para 0,16); pin 7,70:1 sobre o continente; as duas faixas do anel continuam se revezando. Nada a ajustar no pin, no anel ou na malha. **(b) ENTREGUE dentro da frente 2 do D-44** (`51d6964`), como este item mandava | Concluída |
| **D-59** | 06/09 | **⚠ REGRESSÃO: o globo parou de voltar a apontar Dublin.** Diagnosticar antes de mexer | Relato do Raul na tela: quando o globo para, ele **não volta mais a assentar em Dublin**. Pelo D-55 o comportamento correto é: **arrastar** suspende o assentamento e o globo gira livre indefinidamente; **rolar** limpa a suspensão e o globo volta a apontar Dublin. Se ele nunca mais assenta, alguma coisa está deixando a suspensão ligada para sempre — ou ligando-a sem arrasto. **Suspeitos, em ordem, e cada um verificável:** (1) o sinalizador do D-55 é ligado na soltura mas **não é limpo** no `scroll`, ou é limpo num caminho que não roda; (2) alguma coisa **liga o sinalizador sem arrasto** — vale conferir se o `pointerdown` que o D-54 passou a ignorar fora da zona ainda assim marca estado; (3) o `resting` do D-28 depende de `lastInteraction`, e se algo carimba esse valor a cada quadro o globo nunca alcança os 1,5 s de repouso — **candidato forte: o `tema:mudou` ou o próprio tom ambiente**. **Não corrigir por tentativa.** Instrumentar e me dizer, em três instantes após soltar o globo sem rolar: o valor do sinalizador do D-55, o `resting` do D-28, e `performance.now() − lastInteraction`. O que estiver errado aparece nesses três números. **ENTREGUE em `6c13caf`, e não era nenhum dos três suspeitos — os três ficam verificados:** o `livre` É limpo no `scroll`; o `pointerdown` fora da zona sai antes de marcar estado; e o `tema:mudou` **não toca** em `lastInteraction`. O culpado era um quarto, no cruzamento de duas decisões antigas: pelo **D-29** o mouse pula a classificação de gesto e `mode` já nasce `'drag'` no `pointerdown`, e o `onEnd` ligava o `livre` do **D-55** só de ver `mode === 'drag'` — ou seja, **um clique parado sobre o globo era tratado como arrasto** e suspendia o assentamento para sempre. Entrou um sinalizador `moveu`: o `livre` só liga se o gesto girou alguma coisa. Medido na caixa de 773 px, dois quadros a 800 ms: sem tocar em nada 0,15%; um clique sem mover **15,47% antes e 0,14% depois**, com o marcador saindo de 161,6 px fora do centro para Δ 0,0 px; arrasto de verdade 12,42%, ou seja o D-55 continua de pé. **A lição é sobre o método:** instrumentar em vez de chutar valeu mesmo com os três palpites errados — foi reproduzindo a regressão com o conserto revertido que a causa ficou provada, e não suposta | Concluída |
| **D-60** | 06/09 | **O globo inverte por tema** — claro: oceano branco + continentes roxos; escuro: oceano roxo + continentes brancos. **Uma textura só serve os dois.** O **pin ganha duas polaridades**. Azul descartado: a matiz continua 303.724 | Pedido do Raul olhando os dois temas na tela. Ele chegou a escrever "continente azul" e **corrigiu: foi erro de digitação, é roxo**. Fica o motivo escrito para ninguém reabrir: o site é **uma matiz só**, e o D-44 já travou que ela não muda em nenhum dos dois temas — uma segunda matiz no globo não seria adaptar a cor, seria trocá-la, no único objeto grande e saturado da página. **A inversão é barata por causa do D-56:** depois dele o oceano é `color` sólida (trivialmente temável) e os continentes são **textura multiplicada pela cor do material**. Multiplicação só escurece, então, partindo de uma textura **clara**, `color` branco devolve continentes claros e `color` roxo devolve roxos — **com o mesmo arquivo**. Zero KB novos, nenhum segundo PNG para manter. Pré-requisito duro: a textura clara e sólida do **D-58a**. **O pin não é uma cor só por tema, e essa foi a parte contraintuitiva.** O `Marker` tem quatro peças em **duas situações**: **anel** e **pé** ficam rentes à superfície, sobre continente ou oceano; **haste** e **cabeça** sobem pela normal e são vistas contra o **oceano e o fundo**, nunca contra a terra. Todas eram `#6e11b0`. No escuro, cabeça roxa contra oceano roxo é roxo sobre roxo — foi o que o Raul viu. Mas pintar o pin inteiro de branco **troca o problema de lugar**: o pé branco cairia sobre a Irlanda branca. Regra: **a peça escolhe a cor pela superfície que cruza, não pelo tema** — acima da superfície segue o oposto do **oceano**, na superfície segue o oposto do **continente**. É a lógica das duas faixas do anel (D-56) estendida ao marcador inteiro. **Restrição de execução, e valeu mais que a paleta:** trocar de tema **não pode remontar a cena** — o globo guarda estado vivo em refs (`base`, `spinVel`, `tiltVel`, o carimbo de repouso), e remontar zeraria tudo, matando o giro livre do D-55 a cada clique no botão. A cor troca no material que já existe, sem tocar `g.rotation` e sem trocar `key`; o tema chega pelo `data-theme` e pelo evento `tema:mudou` da frente 7, sem criar um segundo canal. **Entregue dentro da frente 2 do D-44 (`51d6964`)**, e a medição confirmou que a restrição foi respeitada: depois de duas trocas de tema, **0,07% dos pixels mudam em 800 ms** — o globo não acorda. **Adendo de 07/09 — auditoria: DUAS das três partes desta decisão não chegaram ao código, e a causa está identificada.** O Raul viu na tela que *"o pin no modo escuro não está branco"*, e a leitura de `paleta.ts` e `Marker.tsx` mostrou mais do que ele viu. **(a) A inversão por tema não existe.** Os dois temas têm **oceano roxo**: `claro.oceano = '#8c62ac'` com `continentes = '#ffffff'`, e `escuro.oceano = '#754ba3'` com `continentes = '#e6e0ee'`. O tema escuro é o claro um pouco mais escuro, não o inverso dele. O que esta decisão pede para o tema **claro** é o contrário: **oceano quase branco, continentes roxos**. **(b) O pin tem uma cor só por tema.** `paleta.escuro.pin = '#5a0d91'`, e há um comentário no arquivo dizendo que *"o pin continua escuro nos dois temas"*, deliberadamente, porque pousa sobre continente claro. Esse raciocínio está certo **para o pé e para a faixa escura do anel** e errado para a **haste e a cabeça**, que sobem pela normal e são vistas contra o **oceano**, nunca contra a terra — é exatamente a distinção que esta decisão criou, e ela não foi aplicada: as quatro peças compartilham `paleta.pin`. **(c) O que chegou certo:** a estrutura por tema (`PALETAS` com `claro`/`escuro`, cores fora dos componentes), a textura única multiplicada, e a restrição de não remontar a cena — o que era a parte difícil. **A causa é documental, não técnica, e é a lição:** esta decisão **não estava no PRD** quando a frente 2 foi executada — ela viajou só dentro do prompt daquela sessão, e o log pulava do D-59 para o backlog. Sem o texto na pasta, quem executou reconstruiu a regra de memória e escolheu uma saída plausível e diferente. **Foi escrita no log só em 07/09** (v6.6), depois de implementada. Registrado aqui porque é a demonstração mais limpa do motivo da regra de manutenção no topo deste documento: **prompt não é contrato; o PRD é.** **(a) e (b) ENTREGUES em `885881f`**, no mesmo commit, porque uma muda a resposta da outra: inverter o tema claro troca a superfície que o pé do pin pisa, e portanto troca a cor que ele precisa ter. **A inversão usa o par que existia antes do D-56** — oceano `#fbfafd`, continentes `#8c62ac` — de propósito: não é número novo, é o estado que o Raul já tinha visto e aprovado, que é exatamente o que esta decisão pede de volta. **O pin virou dois tokens:** `pinNaSuperficie` (o pé) e `pinAcimaDaSuperficie` (haste e cabeça). **O que esta decisão não tinha previsto, e a execução obrigou a resolver:** o **anel não pode seguir nenhuma das duas regras**. Ele está rente à superfície, então pela letra do texto acima seguiria o continente — mas o pulso cresce 2,2× e **atravessa a costa**, então cada faixa cruza as duas superfícies dentro do mesmo ciclo. Amarrar a faixa escura à cor do pé deixaria as **duas faixas claras** no tema claro, e o anel sumiria inteiro sobre o oceano — desfazendo o D-56. Por isso entrou `anelEscuro` próprio, e a regra do anel fica sendo a do D-56: **o par é sempre uma escura e uma clara, nos dois temas**, e a garantia é ao menos uma acima de 3:1 em cada superfície. **Efeito colateral consertado junto, e ele só existia por causa do acaso que esta decisão denuncia:** a atmosfera lia `paleta.oceano`, o que passava despercebido porque os dois temas tinham oceano roxo; com o claro invertido ela viraria halo branco somado sobre página branca, ou seja sumiria. Voltou a ter token próprio, como tinha antes do D-56. **Medido, mínimo 3:1, pior caso 3,79:1**, com os números na seção 0 — e com o método declarado, porque muda a confiança: é contraste WCAG calculado das cores de material, com a malha composta analiticamente, e **não** leitura de pixel de captura como no D-56 e no D-58a, porque não havia navegador na sessão. **Fica apontado e não corrigido:** no tema claro o pé do pin é claro sobre a Irlanda roxa, e projeta ~16,6 px contra uma ilha de ~20×22 px — a pegada já estava anotada na seção 0, e com a inversão ela fica mais visível, porque agora o pé clareia a ilha em vez de escurecê-la. | Ativa |
| **D-61** | 07/09 | **O site passa a ser bilíngue — inglês e português, com troca manual.** O texto em português é **escrito**, não traduzido por máquina. **Vai por último**, depois de tudo | Pedido do Raul: *"quero em dois idiomas [...] quando traduz pelo Google tá indo tudo feio, só quero corrigir isso pra ficar bonito nas duas línguas — mas isso deixa por último"*. **O problema real não é falta de português, é o português errado que já existe.** Hoje o site é `lang="en"` e qualquer visitante brasileiro cai na tradução automática do navegador, que quebra justamente o que este site tem de melhor: o `WordReveal` recorta palavra a palavra, os títulos foram medidos em caracteres por linha (D-39, 74 no máximo), e o fio do M-25 é gerado da **geometria medida dos rótulos**. Texto trocado por baixo por um tradutor muda largura, quebra de linha e contagem de palavras — o layout medido deixa de bater com o texto exibido. Ou seja: **o site não fica só mal escrito em português, ele fica torto.** **Decisões que já dá para travar:** (1) **texto escrito, não traduzido** — a voz do Raul em português não é a tradução da voz dele em inglês; expressões como "I work where things are ambiguous" viram outra frase, não a mesma frase em português; (2) **duas URLs de verdade**, `/` e `/pt`, não só um botão que troca strings na memória — sem URL própria o Google não indexa a versão portuguesa e metade do ganho se perde; (3) **`<html lang>` acompanha a página**, e cada versão declara a outra com `hreflang` — é isso que faz o navegador parar de oferecer tradução automática, que é a origem da reclamação; (4) **`og:locale`, `sitemap.xml` e o JSON-LD** passam a ter as duas; (5) **o seletor de idioma é irmão do botão de tema** no header (D-42, D-44), com o mesmo padrão de foco auditado no RNF-06; (6) **a escolha persiste em `localStorage`**, mesmo mecanismo do tema, **mas a primeira visita segue a URL, não o idioma do navegador** — quem chega em `/` vindo de um recrutador tem de ver inglês, mesmo com o Windows em português. **O que precisa ser remedido, e não é óbvio:** as medidas de leitura do D-39 e do D-52 foram feitas **no texto inglês**; português é tipicamente 15–25% mais longo, então **a contagem de caracteres por linha e a altura das seções precisam ser remedidas na versão portuguesa** — o mesmo `max-width` não dá o mesmo resultado. E o `WordReveal` e o fio do M-25 precisam ser conferidos com o texto português no ar, pela mesma razão. **Custo honesto:** é a maior mudança de conteúdo desde o Passo 7, mexe em todos os textos e no roteamento, e **não é bloqueante do lançamento** — o site em inglês publica em 10/10 sem ela. **Posição na fila: última, depois do Lighthouse.** Se o calendário apertar, esta é a primeira a sair, e sai inteira: **meio bilíngue é pior que monolíngue** — página em inglês com header em português é a cara de site inacabado. **Pré-requisito de qualidade:** a revisão do inglês (RNF-09) vem **antes**, senão a versão portuguesa seria escrita a partir de um inglês ainda não revisado | Aberta |
| **D-62** | 07/09 | **A performance mobile de 68 tem conserto, e são dois — cada um ataca uma causa medida.** (a) **A fonte passa a ser self-hospedada**, saindo do Google Fonts; (b) **o `modulePreload` do Vite para de pré-carregar as cenas 3D**, que o celular baixa e nunca monta | O Lighthouse de 07/09 deu 95 no desktop e **68 no mobile**, com sete das oito notas passando. As duas causas foram medidas, não deduzidas, e cada uma tem um conserto de baixo risco. **(a) A folha do Google Fonts bloqueia a renderização por 825 ms, contra 155 ms do CSS próprio.** Os `preconnect` que já existem no `index.html` não resolvem: eles encurtam o *handshake*, e o problema é a folha ser **render-blocking** — o navegador não pinta nada enquanto ela não chega, e ela mora em outro domínio. Hoje o `index.html` pede `family=Sansation:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700`, ou seja **seis faces** para uma página que usa 400 e 700 em peso e quase nada em itálico. **O conserto:** baixar os `.woff2` (a Sansation servida pelo Google está sob OFL, então redistribuir é permitido — **confirmar o arquivo de licença junto com os binários**), guardar em `public/fonts/`, declarar `@font-face` **dentro do CSS que já existe** com `font-display: swap`, e dar `<link rel="preload" as="font" crossorigin>` **só nas faces realmente usadas**. Isso remove um domínio terceiro do caminho crítico, elimina uma requisição bloqueante inteira e, de brinde, tira do site a única chamada a um serviço do Google — o que combina com a promessa de zero cookies do D-05. **Já existe `public/fonts/README.md` prevendo este passo.** Ele deixa de ser "depois" e vira agora. **Cortar as faces é parte do conserto, não um extra:** seis faces em vez de duas ou três é peso que ninguém vê. **(b) 237 KB de 3D que o celular baixa e nunca monta.** O `useCanRender3D` recusa a cena abaixo de 640 px e **recusa certo** — o LCP do celular é o `memoji.webp`. Mas o `index.html` construído traz `modulepreload` de `three` (183 KB) e `r3f` (54 KB), então **o navegador baixa antes de o React decidir**: 202 KiB de JS não usado numa página de 372 KiB, mais 466 ms de avaliação do r3f numa tela que não tem nada 3D. **O conserto é de configuração, não de código de produto:** `build.modulePreload.resolveDependencies` no `vite.config.ts`, filtrando os chunks `three` e `r3f` da lista de pré-carga. O `React.lazy` continua igual; some só o palpite do empacotador. **⚠ Isto conserta uma contradição do próprio documento, não só uma nota.** O RNF-02 exige `three` "em chunk próprio **carregado sob demanda**". O chunk próprio existe desde o Passo 4; **a demanda nunca foi respeitada** — e a medição escrita no RNF-02 (`npm run build`) jamais pegaria, porque ela olha o **tamanho** dos chunks e não **quem os baixa**. **A medição do RNF-02 muda junto:** passa a ser uma carga real em largura de celular provando que `three` e `r3f` **não aparecem na aba de rede**. Critério que falha quando o defeito volta é o único que vale. **Ordem e risco:** (a) antes de (b) — a fonte está no caminho crítico da primeira pintura e o 3D não. Nenhum dos dois toca conteúdo, layout ou as cenas; se um dos dois estourar o custo, o outro entrega sozinho. **Aceite:** Lighthouse mobile **≥ 80** (meta do RNF-01) rodado em produção **depois** do deploy, com as outras sete notas sem regressão. **ACEITE CUMPRIDO em 07/09: mobile 82, desktop 98, as outras seis em 100 sem regressão** — mas o crédito é todo da (a), e as duas partes terminaram em lugares muito diferentes. **(a) ENTREGUE em `9d8d99c` e funcionou como previsto.** O bloqueio de renderização caiu de 825 ms do Google Fonts mais 155 ms do CSS próprio para **150 ms do CSS próprio e mais nada**; FCP de 4,6 s para 2,7 s. **Duas faces, não seis, e a escolha veio de varredura em vez de palpite**, como esta decisão exigiu: `font-bold` 12 vezes, `font-normal` 4, **nenhum itálico** e **nenhum uso real de 300** — a única ocorrência de `font-light` estava dentro de um comentário do `index.css`, ou seja o peso era anunciado pela documentação e não usado por ninguém. Só o subset `latin`, porque os outros três não têm caractere nenhum deste site. Licença conferida no `METADATA.pb` do `google/fonts` em vez de presumida: OFL 1.1, © 2011 Bernd Montag, e o `OFL.txt` viaja ao lado dos binários, que é a condição. **O `404.html` foi junto**, porque ele também chamava o Google Fonts e deixá-lo assim faria a promessa de "nenhuma chamada a serviço do Google" ser falsa justamente na página de erro. **(b) ENTREGUE em `fc96578` e NÃO atingiu o objetivo — a premissa desta decisão estava incompleta.** O `resolveDependencies` está lá e as duas dicas sumiram do HTML, mas `three` e `r3f` **continuam baixando 237,5 KB a 412 px**. A dica de `modulepreload` nunca foi a amarra: ela apenas **antecipava** um download que aconteceria de qualquer forma. A amarra é um **import estático** do chunk de entrada para o `r3f`, que por sua vez importa o `three` — e import estático o navegador tem de resolver antes de executar o módulo. **E a razão de a entrada depender do `r3f` não é o 3D:** aquele chunk **não contém só `@react-three`**, ele carrega também **`react-dom` e `scheduler`**, de que o `main.tsx` precisa para o `createRoot`. O agrupador varreu a família do React para dentro do grupo do `@react-three`. O `React.lazy` e o `useCanRender3D` estão certos e não foram tocados. **Conserto apontado e não feito**, porque mexeria no desenho dos grupos de `codeSplitting` — fronteira de chunk e cache do site inteiro — e não em filtrar preload, que foi o que esta decisão autorizou; é **decisão nova**. **A lição, e ela é sobre medição:** o defeito passou pelo `npm run build`, pelo lint, pelo TypeScript e **pelo próprio Lighthouse**, que deu 82 com os 237 KB ainda sendo baixados. Só a medição nova do RNF-02 — carga real em largura de celular — reprovou. **Nota boa não é prova de contrato cumprido**, e um critério que não falha quando o defeito existe não é critério | Concluída |
| **D-63** | 07/09 | **O grupo `r3f` engoliu a família do React, e é por isso que 237 KB de 3D baixam num celular que nunca monta a cena.** Os três pacotes do React ganham grupo próprio, antes dos grupos 3D | A prova de rede do RNF-02, medida a 412 px em produção depois da D-62 (b): `three` (183,4 KB) e `r3f` (54,2 KB) **continuam baixando**. A D-62 (b) funcionou no que prometia — as dicas de `modulepreload` sumiram do HTML — mas **a premissa dela estava incompleta**: o `modulepreload` só **antecipava** um download que aconteceria de qualquer jeito. **A amarra real é um import estático da entrada.** O `codeSplitting.groups` do `vite.config.ts` declara `three` e `r3f` por regex sobre `node_modules`, e o agrupador varreu **`react-dom` e `scheduler` para dentro do grupo do 3D**. Como o `main.tsx` precisa de `react-dom` para o `createRoot`, a **entrada passa a depender estaticamente do chunk `r3f`** — que importa o `three`. O `React.lazy` e o `useCanRender3D` **estão certos e não são o problema**: a cena de fato nunca monta a 412 px, e o LCP é o `memoji.webp`. O que baixa é a biblioteca, por uma aresta de empacotamento. **O conserto:** um grupo explícito para `react`, `react-dom` e `scheduler`, **declarado antes** dos grupos `three` e `r3f`, para que a família do React tenha destino próprio e o grupo 3D volte a conter só 3D. **O que isto mexe e por que não é trivial:** fronteira de chunk muda o nome com hash de **todos** os arquivos afetados, então invalida o cache do site inteiro. Aqui o custo é aceitável e vale registrar por quê: **o site ainda não tem público** — invalidar cache antes do lançamento não custa nada, depois custaria uma visita lenta para todo mundo. Fazer agora é mais barato do que fazer em novembro. **Aceite, e ele é o mesmo critério que reprovou:** carga real a 412 px em produção **sem `three` nem `r3f` na aba de rede**; no desktop, as duas cenas ainda montam (Memoji e globo), e o Lighthouse não regride em nenhuma das oito notas. **A lição, e ela é a maior da semana:** este defeito passou pelo `npm run build`, pelo lint, pelo TypeScript **e pelo próprio Lighthouse**, que deu 82 com os 237 KB ainda descendo. Só o critério novo do RNF-02 reprovou — o critério **justificou a si mesmo na primeira vez que rodou**. Um requisito medido pelo artefato ("o chunk existe e tem tal tamanho") não é o mesmo que medido pelo comportamento ("ninguém baixa esse chunk"), e a diferença entre os dois escondeu 237 KB por quatro passos. **ENTREGUE em `8833165`, e o aceite passou inteiro.** O grupo `react` entrou antes dos grupos 3D, e a **ordem é o conserto** — não a existência do grupo. **Conferido no grafo do build, como esta decisão exigiu, e não só na configuração:** a entrada passou a importar `rolldown-runtime` e `react`, e mais nada; `r3f` importa `react` e `three`; `HeroScene` e `GlobeScene` importam `three`, `r3f` e `paleta`. Ou seja os dois chunks 3D **só são alcançáveis pelas cenas**, que é o que o `React.lazy` sempre quis dizer e nunca conseguiu entregar. **A prova de rede, a 412 px em produção:** `three` e `r3f` **ausentes**; peso total de 372 KiB para **138 KiB**; JS não usado de 202 KiB para **28 KiB**; o teto de 150 KB gzip do RNF-02 fica em **80 KB**. **Lighthouse: mobile 94, desktop 99**, as outras seis notas em 100 — o percurso da semana no mobile foi 68 → 82 → 94. **A regressão obrigatória não aconteceu:** a 1350 px o `HeroScene`, o `GlobeScene`, o `paleta` e a textura do globo continuam sendo buscados, então as duas cenas montam. **E o método de verificar isso foi calibrado antes de valer como prova** — a corrida de 07/09, no mesmo Chrome headless, já baixava as duas cenas no desktop e nenhuma no mobile, então ausência no desktop significaria regressão real e não limitação da ferramenta; sem essa calibração, "não apareceu" não distinguiria conserto de cena quebrada. **O que não foi reverificado, dito na cara:** arrasto e assentamento do globo não foram medidos de novo, porque não há como dirigir a página pelo Lighthouse. O argumento é outro e é conferível: o commit **tocou um arquivo só**, o `vite.config.ts`, então `useGlobeDrag.ts` e `Globe.tsx` seguem byte a byte como nas medições do D-59 e do adendo do D-55 — mudou o pacote em que o código viaja, não o código. **Fecha o Passo 10 e a fila de código do projeto** **RESOLVIDA A PARTE (a) — a malha saiu (`09e5abe`), e o contraste subiu nos dois temas:** claro 3,77 → **4,53:1**, escuro 4,13 → **4,94:1**. Sem flag, sem código morto: as chaves `malha` e `malhaOpacidade` saíram do tipo `Paleta` e dos dois temas. **PARTE (c) — as duas hipóteses que eu escrevi estavam ERRADAS, e a causa é uma terceira.** Fica registrado com o nome certo, porque o padrão se repete: eu especulei de longe, a medição derrubou as duas. **(i) O Lenis não carimba nada em repouso** — 0 eventos de `scroll` depois de soltar, `lastInteraction` imóvel em `14462.1` pelos 15 s inteiros. **(ii) O alvo em X centraliza sim, e é falso por construção duvidar** — `Rx(target.x)·Ry(target.y)` leva Dublin a `(0,0,r)` exatamente; medido, o pin para a **0,0 px do centro do quadro**. O aceite que eu tinha escrito **já passava**. **A causa real: o `onDown` carimba `lastInteraction` incondicionalmente**, antes de saber se o gesto vira arrasto. O D-59 impediu que um clique parado **ligasse** o `livre`, mas não impediu que ele **re-carimbasse o relógio contra o qual o `livre` vence** — e o clique também não limpa a flag. Cada clique parado no globo adia o assentamento em **10 s inteiros, sem nenhum retorno visual**. Medido com dois cliques, em 3,2 s e 6,1 s: assentou aos **16,1 s** em vez de 10,1, e `6,1 + 10,0 = 16,1` fecha exato. **Isto explica o relato inteiro, e a ironia é o ponto:** o Raul arrasta, espera, se impacienta, **clica no globo para acordá-lo** — e é o clique que empurra a volta. O instinto dele era o que impedia a cura. **DECISÃO: o clique parado LIMPA o `livre`, e o carimbo FICA.** Duas opções estavam na mesa; a outra era não carimbar em clique que não moveu. A escolhida é melhor por dois motivos. **(1) Semântica:** pelo D-55, **rolar** já significa "volta a apontar Dublin"; **clicar** passa a significar a mesma coisa — duas formas de dizer a mesma frase, em vez de uma que ajuda e outra que atrapalha. O instinto do Raul vira a cura em vez da causa. **(2) O carimbo deixa de ser defeito no instante em que o clique tem significado:** mantido, o globo assenta **1,5 s** depois do clique, pelo `REST_DELAY`, e essa folga faz a volta parecer decidida. Tirando o carimbo junto, o retorno começaria **no mesmo quadro do clique** — seco. Por isso **não** se aplicam as duas. **Nota de escopo:** os ouvintes moram na `<section>` (D-29), então um clique em qualquer lugar da seção limpa o `livre`, não só sobre o globo. É coerente e é de propósito: a seção inteira já responde ao arrasto pelo mesmo motivo. **Aceite:** arrastar, soltar, e (a) não tocar em nada → gira ~10 s e o pin para a 0,0 px do centro; (b) clicar aos 3 s → assenta ~1,5 s depois do clique, não 10 s depois. | Concluída |
| **D-64** | 07/09 | **A revisão do inglês (RNF-09) passa a ter um pacote: `docs/REVISAO-INGLES.md`** — todo o texto visível do site **extraído do código**, em ordem de leitura, com **um identificador estável por frase**. O revisor devolve correções citando o identificador, sem abrir o repositório, sem instalar nada e sem tocar em código | **O RNF-09 é o único bloqueante do lançamento e é justamente o item que menos anda — e o motivo não é preguiça de ninguém, é de desenho.** Hoje o pedido é "revise o inglês do meu site": a pessoa tem de abrir o site, rolar, caçar o texto no meio do movimento, anotar em algum lugar e torcer para não ter pulado seção. É uma hora de trabalho sem formato, não cabe num favor de amigo, e **por isso fica sempre para depois**. Uma lista numerada é **quinze minutos e dá para fazer no celular** — e a distância entre uma hora e quinze minutos é a distância entre o revisor aceitar e o revisor adiar. **O que fica travado no desenho do pacote:** **(1) extraído do código, nunca redigitado.** Se o texto do pacote divergir do texto do site a revisão vira lixo, porque as correções chegariam em frases que não existem; cada entrada carrega o caminho do arquivo de onde saiu. **(2) Identificador estável por frase** (`HERO-01`, `ABOUT-03`): é o que torna a **viagem de volta** barata. O revisor escreve `ABOUT-03: "graduaded" → "graduated"` e quem aplica não precisa caçar a string. Sem isso o retorno vem como prosa solta e alguém tem de reconstruir a que frase cada observação se referia — que é exatamente onde uma revisão se perde. **(3) Ordem de leitura, não ordem de arquivo.** Metade do RNF-09 é *naturalidade*, e naturalidade depende do que veio antes; uma lista em ordem alfabética de componente mede gramática e não mede voz. **(4) Entra também o que não é prosa na tela:** `<title>`, `meta description`, `og:title` e `og:description` — que são o texto do **preview do link no WhatsApp e no LinkedIn**, uma linha ainda aberta da §10 — mais a página `404.html`. Num bloco à parte e marcado como tal entram `aria-label` e `alt`: são inglês **lido em voz alta** por leitor de tela, mas não são "texto visível" na letra do RNF-09, então vão separados e o revisor decide se olha. **(5) A passada de máquina fica no MESMO arquivo, no fim, sob um aviso para não ser lida antes.** O RNF-09 pede revisão por ferramenta **e** por pessoa, e são duas passadas independentes de propósito: se a pessoa ler as suspeitas da máquina primeiro, ela deixa de fazer a segunda passada e passa a **conferir a primeira** — o valor da leitura fresca some. Um arquivo só, as duas metades, e a ordem garantida pelo layout em vez de pela boa vontade de quem lê. **(6) Nada em `src/` é tocado.** A fila de código está encerrada; isto é documento. As correções voltam como decisão nova e só então viram código — e cada uma passa pelo julgamento do Raul, porque revisor de inglês corrige gramática mas não conhece a voz de quem escreveu. **O risco, escrito para não ser esquecido:** é um documento gerado a partir de código, então ele **envelhece no instante em que qualquer string mudar**. Hoje esse risco é quase zero, e o motivo é a fila de código ter fechado — **este é o momento mais barato do projeto para fazê-lo**; feito uma semana atrás já teria desatualizado três vezes. **De brinde:** se a D-61 acontecer depois do lançamento, este arquivo já é a coluna inglesa do site bilíngue, e o trabalho de extração não se repete **SUBSTITUÍDA pela D-74 em 11/09.** O pacote nunca chegou a ser gerado: o Raul dispensou o revisor humano, e a revisão foi feita direto pelo Cowork contra `src/data/`, saindo em `docs/REVISAO-INGLES.md` com 11 pontos. O pacote existia para **entregar texto a uma pessoa que não abre o código** — sem essa pessoa, ele perdeu a função. **Fica registrada e não apagada** porque a ideia volta se um revisor aparecer depois de publicar. | Substituída |
| **D-65** | 07/09 | **A malha *wireframe* do globo sai.** Ela é a listra que atravessa o planeta e o estrelado no polo. E o assentamento em Dublin é reaberto: **medir onde o pin para, não olhar** | Duas reclamações do Raul na tela, e a captura mostrou que **são o mesmo objeto**. **(a) A listra.** A malha é uma `sphereGeometry(RADIUS × 1.003, 18, 12)` em `wireframe`. Uma esfera de arame tem anéis de latitude; quando o globo assenta em Dublin ele inclina ~53° em X, e **o anel de latitude que fica na altura do olho é visto exatamente de perfil — um círculo de perfil é uma reta.** É a listra. Ela não é defeito de material nem de textura: é geometria, e aparece **sempre que o globo assenta**, que é justamente o estado de repouso que a pessoa mais vê. **(b) O estrelado no polo.** Os 18 meridianos convergem no polo, e a mesma inclinação de 53° que cria a listra **vira o polo norte para a câmera** — então o adensamento que o D-56 já tinha reduzido de 24×16 para 18×12 voltou a ficar no centro do quadro, sobre o Ártico, como um disco riscado. **A decisão é remover, não afinar, e o motivo é o custo/benefício:** a malha entrou no D-56 para dar "ar técnico" copiando a referência do Envato, e desde então o terreno mudou — o **D-58a** trouxe continentes **sólidos**, que era o que dava o desenho que o Raul queria, e a malha passou a **custar 0,42 de contraste** (3,89:1 → 3,47:1 medidos no próprio D-56) para entregar dois artefatos visuais. Enfeite que tira contraste e cria listra não é enfeite, é ruído. **Some também a linha de `paleta.ts` (`malha`, `malhaOpacidade`) e a mesh inteira** — não deixar código morto atrás de uma flag. **(c) O assentamento, reaberto.** O adendo de 07/09 do D-55 deu prazo de 10 s ao `livre` e o código está lá (`LIVRE_TIMEOUT = 10_000`, com `d.livre = false` quando vence). Mesmo assim o Raul continua vendo o globo não voltar. **Não chutar de novo — instrumentar:** medir, ao longo de ~15 s depois de soltar sem tocar em mais nada, os valores de `livre`, `lastInteraction`, `resting` e a posição do pin na tela. Duas hipóteses a testar antes de qualquer conserto: **(i)** algo continua carimbando `lastInteraction` sem o usuário fazer nada — o `useSmoothScroll` (Lenis) roda `rAF` contínuo e pode emitir `scroll` em repouso, e o carimbo é compartilhado; **(ii)** o assentamento roda, mas o alvo em X (`asin`, ~53°) leva Dublin à **altura** do olho sem trazê-la ao **centro**, e o que o Raul chama de "não voltou" é o pin parando fora do meio do quadro. As duas têm conserto diferente, e escolher sem medir é o erro que o D-59 já cobrou uma vez **RESOLVIDA A PARTE (a) — a malha saiu (`09e5abe`), e o contraste subiu nos dois temas:** claro 3,77 → **4,53:1**, escuro 4,13 → **4,94:1**. Sem flag, sem código morto: as chaves `malha` e `malhaOpacidade` saíram do tipo `Paleta` e dos dois temas. **PARTE (c) — as duas hipóteses que eu escrevi estavam ERRADAS, e a causa é uma terceira.** Fica registrado com o nome certo, porque o padrão se repete: eu especulei de longe, a medição derrubou as duas. **(i) O Lenis não carimba nada em repouso** — 0 eventos de `scroll` depois de soltar, `lastInteraction` imóvel em `14462.1` pelos 15 s inteiros. **(ii) O alvo em X centraliza sim, e é falso por construção duvidar** — `Rx(target.x)·Ry(target.y)` leva Dublin a `(0,0,r)` exatamente; medido, o pin para a **0,0 px do centro do quadro**. O aceite que eu tinha escrito **já passava**. **A causa real: o `onDown` carimba `lastInteraction` incondicionalmente**, antes de saber se o gesto vira arrasto. O D-59 impediu que um clique parado **ligasse** o `livre`, mas não impediu que ele **re-carimbasse o relógio contra o qual o `livre` vence** — e o clique também não limpa a flag. Cada clique parado no globo adia o assentamento em **10 s inteiros, sem nenhum retorno visual**. Medido com dois cliques, em 3,2 s e 6,1 s: assentou aos **16,1 s** em vez de 10,1, e `6,1 + 10,0 = 16,1` fecha exato. **Isto explica o relato inteiro, e a ironia é o ponto:** o Raul arrasta, espera, se impacienta, **clica no globo para acordá-lo** — e é o clique que empurra a volta. O instinto dele era o que impedia a cura. **DECISÃO: o clique parado LIMPA o `livre`, e o carimbo FICA.** Duas opções estavam na mesa; a outra era não carimbar em clique que não moveu. A escolhida é melhor por dois motivos. **(1) Semântica:** pelo D-55, **rolar** já significa "volta a apontar Dublin"; **clicar** passa a significar a mesma coisa — duas formas de dizer a mesma frase, em vez de uma que ajuda e outra que atrapalha. O instinto do Raul vira a cura em vez da causa. **(2) O carimbo deixa de ser defeito no instante em que o clique tem significado:** mantido, o globo assenta **1,5 s** depois do clique, pelo `REST_DELAY`, e essa folga faz a volta parecer decidida. Tirando o carimbo junto, o retorno começaria **no mesmo quadro do clique** — seco. Por isso **não** se aplicam as duas. **Nota de escopo:** os ouvintes moram na `<section>` (D-29), então um clique em qualquer lugar da seção limpa o `livre`, não só sobre o globo. É coerente e é de propósito: a seção inteira já responde ao arrasto pelo mesmo motivo. **Aceite:** arrastar, soltar, e (a) não tocar em nada → gira ~10 s e o pin para a 0,0 px do centro; (b) clicar aos 3 s → assenta ~1,5 s depois do clique, não 10 s depois. **PARTE (d) — a listra saiu e SOBROU UMA LINHA, esta rodeando o globo. Isolar por eliminação, não teorizar.** Relato do Raul depois do `09e5abe`: *"a linha embaixo do globo rodeando ele ainda está lá"*. A malha era mesmo uma das causas — a listra reta que atravessava o planeta —, mas há um segundo traço, e ele contorna a esfera em vez de cortá-la. **Sobraram exatamente três camadas**, então a resposta sai por eliminação em minutos: desligar uma de cada vez e capturar. **Isto é ordem, não sugestão**, e o motivo está escrito duas linhas acima nesta mesma decisão: nesta decisão eu escrevi duas hipóteses e a medição derrubou as duas. **Os dois suspeitos, nomeados como suspeitos:** **(1) a atmosfera** — `sphereGeometry(RADIUS × 1.14, 48, 48)` com `BackSide` e `AdditiveBlending`. Casca renderizada por dentro acumula brilho onde a superfície fica de perfil, ou seja **na silhueta**, e o raio 1,14 dá a essa concentração um contorno próprio, **maior que o planeta e concêntrico a ele** — que é exatamente a forma descrita. O D-56 já registrava que ela mudou de sentido na inversão: era brilho roxo sobre corpo claro, virou halo pálido em volta de bola roxa, e ninguém remediu depois do D-58a e do D-60. **(2) a costura entre as duas esferas** — o corpo opaco está em `RADIUS × 0.995` e os continentes em `RADIUS`. São 0,5% de folga, e no perfil a esfera de textura ultrapassa a opaca: a borda transparente vista de raspão pode fechar um aro. **Método:** capturar o globo em quatro estados — completo, sem atmosfera, sem a esfera de continentes, e só o corpo —, **nos dois temas**, e dizer **qual captura perde a linha**. Só então propor conserto. **Se for a atmosfera**, o conserto tende a ser opacidade e raio, não remoção: ela é o que dá volume à bola, e tirá-la deixa o planeta chapado — diferente da malha, que era enfeite puro. **Se for a costura**, é aproximar os dois raios. **Aceite:** o globo sem nenhum aro concêntrico em volta, nos dois temas, **mantendo a sensação de atmosfera** — e o contraste continentes × oceano não pode cair dos 4,53:1 (claro) e 4,94:1 (escuro) medidos depois da saída da malha. **Também confirmado pelo Raul nesta rodada: o assentamento voltou a funcionar** — "demora uns 5, 6 segundos, mas de boa". É o `livre` vencendo mais o `REST_DELAY`, exatamente o desenhado. **FECHADA em 07/09 (`8c6ca50`, `89ed004`), e a parte (d) rendeu o melhor achado de mecanismo do projeto.** **(c) O clique parado limpa o `livre` — medido:** clique aos 3,2 s → a flag cai no mesmo quadro, `resting` vira verdadeiro em 4,8 s, ou seja **1,8 s depois do clique**, e o pin para a **0,0 px do centro**. Antes seria 13,0 s. A folga de ~1,5 s que motivou manter o carimbo apareceu na medição, como previsto. **(d) O aro é a ATMOSFERA — provado por perfil radial, quatro estados × dois temas.** O patamar de luminância aparece nos dois estados que têm a casca e some nos dois que não têm, nos dois temas; a **costura 0.995/1.0 fica descartada com prova**, porque o estado "sem atmosfera" tem as duas esferas e lê **0,00** fora da silhueta. **O mecanismo, e é aqui que eu estava errado:** eu escrevi que uma casca `BackSide` "acumula brilho onde a superfície fica de perfil". **Não acumula — `meshBasicMaterial` não tem queda com o ângulo de visão, não existe fresnel.** Ela pinta um **disco chapado de borda dura**; como o planeta é opaco e escreve profundidade, sobra o anel entre 1,0 e 1,14 com **luminância constante terminando num degrau**. Patamar plano + borda dura = contorno concêntrico. **O suspeito estava certo no objeto e errado no mecanismo** — e a diferença não é acadêmica: acúmulo na silhueta se conserta com opacidade, disco recortado se conserta com **raio**. **E a parte contraintuitiva, que o modelo previu antes de a medição confirmar:** no tema claro a atmosfera **escurece** em vez de clarear. `AdditiveBlending` soma cor, **mas o canal alfa também acumula**, e o navegador compõe o canvas sobre a página usando esse alfa — somar 6% de roxo cobrindo 11% de uma página quase branca dá **saldo negativo**. Previsto −0,120; medido **−0,1165**. É por isso que o aro gritava no claro e quase não aparecia no escuro. **Conserto aplicado:** raio 1,14 → **1,035**, opacidade 0,11 → 0,06 no claro e 0,07 → 0,05 no escuro. Degrau no claro −116,5 → **−67,4**; no escuro +3,07 → **+1,82**, invisível. Faixa de ~43 px → **~12 px**. Contraste continentes × oceano **intocado** (só `atmosferaOpacidade` mudou no diff), então 4,53:1 e 4,94:1 seguem valendo por construção. **O aceite passou no escuro e NÃO passou inteiro no claro**, e está registrado assim de propósito: sobrou um degrau de **6,7% de contraste de Weber numa faixa de 12 px colada na silhueta**. Deixou de ser aro e virou borda do planeta, mas continua sendo borda dura — e **isso é o teto do que "opacidade e raio" alcançam com material chapado**. Ver **D-70**. **Achado de método, e ele quase produziu uma conclusão falsa:** o `clip` do `captureScreenshot` do CDP **devolve o quadro sem a camada WebGL** — a primeira corrida saiu 100% uniforme, o que teria "provado" que não havia aro nenhum. A saída foi capturar a viewport inteira e recortar depois. **Fica como regra: instrumento novo se calibra contra um resultado conhecido antes de valer como prova**, que é o mesmo cuidado que o D-63 já tinha tomado. | Concluída |
| **D-66** | 07/09 | **O conteúdo é atualizado ANTES do pacote de revisão, não depois.** O Raul traz informação nova de trabalho em 08/09 à tarde, e a D-64 passa para depois dela | Aviso do Raul: *"amanhã à tarde vou mandar as novas informações sobre o trabalho e já corrijo o inglês"*. Isso reordena a fila, e a ordem importa mais do que parece: **a D-64 gera um pacote a partir do texto que está no ar**. Gerar hoje e trocar o conteúdo amanhã joga o pacote fora — e, pior, cria a chance de o revisor receber a versão velha. **Ordem nova: D-66 → D-64 → revisão.** **Onde se mexe:** o texto vive em `src/data/` (`profile.ts`, `projects.ts`, `stack.ts`), e é só ali — a regra 1 da arquitetura. O **`CONTENT.md` não é atualizado junto**: ele levou tarja de histórico em 07/09 e serve para registrar de onde cada texto veio, não para ser a fonte. **O que texto novo obriga a remedir, e não é óbvio:** (1) a **largura de leitura** do D-39 e do D-52, medida em caracteres por linha, com máximo de **74** — texto mais longo estoura isso sem quebrar nada visível; (2) o **`WordReveal`** recorta palavra a palavra, então frase nova muda a contagem e o ritmo da revelação; (3) o **fio do M-25** é gerado da **geometria medida dos rótulos**, então mexer em texto de seção move o caminho do fio; (4) a **ênfase do D-38 e do D-53** é **uma por parágrafo**, marcada com `**…**` — parágrafo novo sem ênfase quebra o padrão, com duas quebra a regra; (5) se mudar cargo, cidade ou disponibilidade, mudam junto o **JSON-LD**, o **`og:description`** e o **`<title>`** — três lugares **fora** do `src/data/` que descrevem a mesma pessoa e envelhecem calados. **Sobre "já corrijo o inglês":** correção própria é bem-vinda e reduz o trabalho do revisor, **mas não cumpre o RNF-09**. O requisito pede pessoa fluente porque o que ela detecta não é erro de gramática — é o que **soa estrangeiro** para um irlandês lendo três segundos antes de decidir se responde. Texto sem erro nenhum pode soar traduzido, e é exatamente isso que o RNF-09 existe para pegar. Se o Raul decidir publicar sem essa revisão, é decisão dele e é legítima — mas então **o RNF-09 muda de estado explicitamente aqui**, em vez de ser dado por cumprido por omissão | **CONCLUÍDA em 12/09, e não do jeito que estava previsto.** Ela esperava "informações novas de trabalho" como um bloco a ser inserido. O que chegou foi **correção do que já estava lá**, e mais valiosa: os clientes são **quatro** — advocacia, odontologia, marketing digital e varejo —, **logística nunca existiu** (era erro herdado do CV antigo e estava publicado), e o resultado real é que **leads ficavam até duas semanas sem resposta**. Tudo aplicado no site e no CV. **A lição:** pedir "o que mudou" trouxe menos do que pedir **"me diga os clientes e um número"** — a pergunta específica destravou em uma mensagem o que a pergunta genérica não destravou em cinco dias. | Concluída |
| **D-67** | 07/09 | **O celular ganha uma imagem estática do globo.** A cena 3D continua desligada abaixo de 640 px — o que muda é a seção deixar de ficar meio vazia | Reclamação do Raul: *"no celular o globo nem aparece"*. Ele está certo no sintoma e o código está certo no comportamento: o `useCanRender3D` recusa a cena abaixo de 640 px (RNF-04), e **é essa recusa que comprou os 94 de Lighthouse no celular** — foi medido no D-63 que os 237 KB de `three` e `r3f` desciam sem nunca montar nada. Ligar o 3D no celular desfaz a conquista mais cara da semana. **Mas a seção Location no celular hoje mostra texto de um lado e nada do outro**, e "nada" não é uma escolha de desenho, é um buraco. **A solução já existe no projeto e é só repetir o padrão:** o hero, sem WebGL, troca a cena por `memoji.webp` — um `<img>` de 30 KB. A Location faz o mesmo: **uma captura do globo já assentado em Dublin**, exportada em WebP, servida abaixo de 640 px e para quem não tem WebGL. **Detalhes que fazem diferença:** (1) a captura tem de sair do globo **no estado de repouso**, com o pin sobre a Irlanda — é a única pose que carrega a informação da seção; (2) precisa de **duas versões, uma por tema**, porque as cores invertem (D-60), e a troca segue o `data-theme` como todo o resto; (3) `alt` descritivo de verdade, não "globo" — algo como *"Globe showing Dublin, Ireland"*, porque num leitor de tela essa imagem é a seção; (4) `loading="lazy"` e `width`/`height` explícitos, senão ela reintroduz deslocamento de layout numa página que hoje tem **CLS 0**. **Aceite:** Lighthouse mobile **não cai de 94**, o CLS continua **0**, e os chunks 3D continuam ausentes da rede a 412 px — a imagem entra no lugar da cena, não além dela **DESCARTADA no mesmo dia, por decisão do Raul: *"esquece o globo no celular"*.** Fica registrada em vez de apagada, porque a pergunta volta: **por que a seção Location no celular não tem globo?** Resposta curta — de propósito, e é o que comprou os 94 de Lighthouse. **Nota de honestidade:** a reclamação nasceu de uma contradição minha. Eu tinha posto "arrastar o globo com o dedo no celular" na lista de conferências de lançamento, e o globo **não existe** no celular desde o RNF-04. O Raul não achou um defeito — ele foi executar um item que eu escrevi errado. **Se um dia voltar**, o caminho registrado é o terceiro que apareceu na conversa e é melhor que os dois óbvios: **imagem estática que carrega a cena real ao toque** — nada de 3D desce no carregamento, e quem quiser girar toca e recebe o planeta, com o mesmo `React.lazy` disparado por gesto em vez de por largura. | Descartada |
| **D-68** | 07/09 | **A marca: o favicon sai do "R" em quadrado roxo, e o nome FICA no cabeçalho.** Duas propostas de símbolo, para o Raul escolher olhando | Pedido do Raul: tirar o nome do canto superior esquerdo e deixar uma estrela roxa, e trocar o "R" branco que aparece na aba. **Concordo com metade e discordo da outra, com motivo escrito para a decisão não ser refeita depois.** **(a) O favicon atual é genérico, e trocar é certo.** Ele é um quadrado roxo arredondado com um "R" branco — a mesma forma que todo serviço de internet usa, e que não diz nada sobre este site. **(b) Estrela de cinco pontas é o pior substituto**, e é preciso dizer por quê: a 16 px, que é o tamanho real de um favicon na aba, as cinco pontas empastam e viram um borrão; e a leitura cultural de uma estrela cheia é **"favorito" ou "avaliação"**, não identidade. **As duas propostas, em ordem de preferência:** **(1) anel com um ponto de luz na borda** — é a assinatura do próprio site, presente nas órbitas do Memoji e no ponto na ponta do fio (M-28); duas formas simples, sobrevive a 16 px, e não se parece com o de mais ninguém. **(2) faísca de quatro pontas**, roxa — é o que o Raul pediu, sem o defeito da estrela: quatro pontas ficam nítidas a 16 px e conversam com as partículas do hero. **(c) Tirar o nome do cabeçalho: NÃO, e esta é a parte em que discordo.** O cabeçalho é fixo — é a única coisa que permanece na tela enquanto a pessoa rola a página inteira. Um símbolo abstrato ali não comunica nada a um recrutador que caiu no meio do site por um link compartilhado, e **o objetivo do site é que ele lembre o nome**. O padrão que parece desenhado de verdade é **símbolo + nome**, não um dos dois. **(d) Trocar o nome por `raulrodrigues.dev`: NÃO.** A URL já está na barra do navegador; usá-la como marca gasta o único espaço permanente da tela repetindo o que o navegador já mostra. **O que fica combinado:** o desenho atual da marca do cabeçalho é um **laptop em SVG com um cursor piscando** — ele pode ser substituído pelo símbolo novo, para símbolo e favicon serem a mesma coisa, que é o que dá sensação de identidade. O nome ao lado permanece. **Restrições:** SVG, nunca emoji (mesma razão do D-22, do D-35 e do botão de tema); usa `currentColor` para acompanhar os dois temas; e o favicon precisa **ser conferido a 16 px de verdade**, não julgado ampliado na tela — é o erro clássico, e é o que produziu o "R" atual **RESOLVIDA em 07/09, e a renderização em tamanho real derrubou a minha própria ordem de preferência.** Desenhei as três e renderizei a 16, 20, 32 e 64 px, em claro e escuro, e dentro de uma aba simulada. **O que a medição visual mostrou: a 16 px, MASSA VENCE LINHA.** O "R" atual sobrevive não por ser bom, mas por ser um bloco sólido; o **anel + ponto** e a **órbita + planeta** — que eu tinha posto como primeira opção, por serem a assinatura do site — **viram fiapo**, porque são desenhos de traço fino e o traço some. A **faísca**, que era a segunda opção e era o pedido do Raul, é a única das três com massa suficiente. **Ele estava certo e eu estava errado, e o erro tem nome:** eu escolhi pelo significado (as órbitas, o ponto do M-28) em vez de escolher pelo tamanho em que a peça é vista. **Escolhida a variante B3**, depois de quatro tentativas: a faísca original era fina demais dentro da caixa, a B2 só maior, a **B3 mais gorda** é a que segura a 16 px, e a **B4 — com uma segunda faísca menor — foi rejeitada porque a segunda vira sujeira** nesse tamanho. **Ela acompanha o tema pelo `prefers-color-scheme` dentro do próprio SVG**: `#6e11b0` no claro, `#bf81ff` no escuro — os mesmos roxos já medidos no D-44, matiz 303.724 nos dois. 490 bytes. **CORREÇÃO DA ALÍNEA (c) DESTA MESMA DECISÃO: o laptop CONTINUA no cabeçalho.** Eu tinha escrito que símbolo e favicon deviam ser o mesmo desenho. A renderização mostrou por que isso está errado aqui: **o laptop não sobrevive a 16 px** — é desenho de linha com um cursor piscando —, e a faísca, no tamanho do cabeçalho, onde há espaço para significar algo, **não significa nada**. O laptop significa: é o Memoji do hero, na mesma pose, e amarra cabeçalho e cena. Então **duas peças, cada uma no tamanho em que funciona** — laptop + nome no cabeçalho, faísca na aba —, que é o oposto de descuido: é escolher por escala. O nome fica, como já estava decidido. **Nota de 07/09, para o Claude Code:** o `public/favicon.svg` apareceu modificado sem ter sido tocado por ele, e ele fez certo em **deixar fora do commit e perguntar** — mudança de identidade não entra de carona. **Foi o Cowork**, e é esta decisão: a faísca de quatro pontas, sem fundo, com `prefers-color-scheme` trocando o preenchimento, escolhida depois de renderizar as três propostas a 16 px. **Pode entrar.** | Ativa |
| **D-69** | 07/09 | **O fio roxo (M-25 / D-37) não é montado no celular.** Some junto o ponto de luz da ponta (M-28). O grão (D-36) fica | Pedido do Raul: *"aquela animação da linha roxa descendo, tire do celular apenas; a tela já é pequena, se ficar uma linha roxa ali perde o clima"*. **Ele está certo, e o motivo é de desenho, não de gosto:** o fio foi construído a partir da **geometria medida dos rótulos das seções** (D-37, M-25) — ele existe para costurar uma coluna larga, serpenteando na margem que sobra ao lado do texto. **Num viewport de 360 px essa margem não existe**, então a curva se achata contra a coluna e o que era um traço passando por trás do conteúdo vira um risco vertical atravessando o texto. O mesmo desenho, em outra largura, deixa de ser o mesmo desenho. **Regra: não montar abaixo de 640 px** — o **mesmo limiar do `useCanRender3D`**, de propósito. O projeto passa a ter **uma definição de "tela pequena", não duas**; dois números para o mesmo conceito é como nascem telas em que o 3D já saiu e o fio ainda está lá. **Não montar, não esconder por CSS.** `display: none` deixaria de pé o `ResizeObserver`, o `rAF` e o cálculo do caminho a partir da geometria — trabalho contínuo no aparelho mais fraco, para desenhar algo invisível. É a mesma lição do D-62 e do D-63: **o custo não é o que aparece, é o que roda.** **Duas armadilhas registradas, porque quem mexer nisso vai passar perto das duas:** (1) o fio e o grão dividem a camada `z-index: -1` atrás do `<body>` transparente — mexer aqui **não** pode devolver `background-color` ao `<body>`, ou **o grão some junto, sem erro nenhum** (regra 5 da arquitetura); (2) o **M-28**, o ponto de luz, lê a ponta do próprio `<path>` — sem fio ele não tem de onde ler, então sai junto e **não vira um ponto solto flutuando**. **O que NÃO muda:** o grão continua no celular (é textura, não movimento, e não disputa espaço), e o `prefers-reduced-motion` continua desligando o fio em qualquer largura — esta regra é **adicional** àquela, não a substitui. **Aceite:** a 360 px, nenhum `<svg>` do fio no DOM e nenhum `rAF` dele rodando; a 1440 px, fio e ponto idênticos ao de hoje; o grão presente nas duas; e o Lighthouse mobile **não cai de 94**. **ENTREGUE EM CÓDIGO em 11/09.** O gate ficou no `App.tsx` — `{telaLarga && <Thread />}` — e **não** dentro do `Thread`: componente que não monta **não roda effect nenhum**, então o `ResizeObserver`, o `resize` da janela, o `rAF` do scroll e o `useScrollProgress` deixam de existir no aparelho estreito **por construção**, e não por checagem espalhada pelo componente. **O limiar não virou número novo:** o `640` que estava escrito dentro do `useCanRender3D` saiu de lá e virou `LARGURA_MINIMA` em `src/hooks/useTelaLarga.ts`, que o `useCanRender3D` agora importa — o comportamento dele não mudou, só o lugar do número. **Uma diferença deliberada entre os dois, e ela está escrita no arquivo:** o `useTelaLarga` acompanha o resize por `matchMedia`, o `useCanRender3D` continua decidindo uma vez na montagem. O limiar é o mesmo; o que muda é **quando** cada um pergunta. O fio se remede a cada resize, então a decisão de existir tem de chegar pelo mesmo caminho — arrastar a janela de 1440 para 360 px precisa desmontá-lo, senão ele segue se redesenhando numa largura em que a margem que ele costura não existe mais. Uma cena 3D não se reconstrói a partir do texto e não tem esse problema; mudar o critério dela seria outra decisão. **A armadilha (1) não chegou a ser tocada:** o `index.css` não mudou em uma linha, então o grão segue em `body::before` e o `<body>` segue sem `background-color`. A armadilha (2) se cumpre de graça, porque o ponto do M-28 é filho do mesmo `<svg>` que deixou de ser montado. **Medido:** o chunk inicial vai de 63,94 para 64,17 KB cru e de 19,45 para **19,51 KB gzip** — 60 bytes —, com build limpo e nenhuma advertência nova no `oxlint` (as duas que aparecem são pré-existentes, no `vite.config.ts`). **O que NÃO foi verificado nesta sessão, e está registrado assim de propósito:** os três itens de aceite que exigem navegador — nenhum `<svg>` no DOM a 360 px, fio e ponto idênticos a 1440 px, e o Lighthouse mobile. Esta sessão não tinha navegador. O que existe no lugar é a **garantia estrutural** de que React não roda effect de componente desmontado, e isso **não é a mesma coisa que medição** — é exatamente a distinção que o D-60 já teve de registrar. Fica para a passada do Raul | **MEDIDO PELO COWORK em 11/09, no navegador, fechando os aceites que a sessão do Claude Code não tinha como rodar.** Ele entregou (`fcb7e4d`) e foi honesto: escreveu que tinha **garantia estrutural** — React não roda efeito de componente desmontado — e que isso **não é medição**. Estava certo em não marcar como provado. **Carga limpa a 360 px, em produção:** nenhum `<svg>` grande no DOM (o fio tem 6247 px de altura quando existe, então é inconfundível), **0 `canvas`**, **grão presente** (`body::before` com opacidade 0,02 e a textura em `data:`), **`background-color` do `<body>` transparente** — a regra 5 da arquitetura intacta —, e **sem rolagem horizontal**. **Carga limpa a 1440 px:** o fio **volta**, com 6247 px e `-z-[1]`, e as **duas cenas 3D montam** (2 `canvas`). Os dois lados do limiar conferem. **O que NÃO foi possível medir, e quase virou um defeito falso:** o caminho de **arrastar a janela** de 1440 para 360. Na primeira leitura o fio continuou no DOM a 360 px depois do redimensionamento, o que parecia contradizer a promessa do `useTelaLarga` de acompanhar o `resize`. **Antes de acusar o código, instrumentei o instrumento** — e a emulação de viewport do painel **não dispara evento nenhum**: `innerWidth` foi de 360 para 1200 com **0 eventos de `resize` e 0 mudanças de `matchMedia`**. O código nunca foi avisado de que a largura mudou; o defeito era da ferramenta. **É a terceira vez que esta armadilha aparece** — depois do `clip` do `captureScreenshot` que devolvia quadro sem WebGL (D-65) e da calibração do headless no D-63 — e por isso vira regra do documento, não anedota: **instrumento novo se calibra contra um resultado conhecido antes de valer como prova, e um resultado que contraria o código é suspeito do instrumento antes de ser defeito.** **Fica em aberto, para o Raul:** arrastar a janela de verdade entre larguras, e o **Lighthouse mobile ≥ 94** — os dois pedem navegador real. | Concluída |
| **D-70** | 07/09 | **A borda dura que sobrou no tema claro só sai com gradiente — e isso é decisão do olho do Raul, não da medição.** Nada a fazer até ele olhar | O `89ed004` levou o degrau do aro de −116,5 para **−67,4** no claro e tornou-o invisível no escuro. Sobrou, colado na silhueta, um degrau de **6,7% de contraste de Weber numa faixa de 12 px**. **Por que não dá para ir além por opacidade e raio:** `meshBasicMaterial` é chapado — não tem queda com o ângulo de visão. Toda atmosfera visível feita com ele é **uma faixa de borda dura**; baixar mais a opacidade não suaviza o degrau, apenas **apaga a atmosfera inteira**, e a atmosfera é o que dá volume à bola (foi por isso que o D-65 mandou consertar em vez de remover, ao contrário da malha). **O que apagaria o degrau sem apagar a atmosfera:** um **gradiente** — fresnel num `ShaderMaterial`, ou um *sprite* com gradiente radial atrás do planeta. **Custo honesto:** shader próprio é a primeira linha de GLSL do projeto, e passa a ser código que ninguém mais no repositório se parece; o sprite é mais simples mas acrescenta um ativo e não gira com a esfera. **Decisão: não fazer agora.** O item entra **só se o Raul ainda enxergar a linha na tela** depois do `89ed004`. Se ele não enxergar, esta decisão fecha como descartada e o assunto morre — perseguir 6,7% de Weber que ninguém vê é gastar o prazo de 10/10 com o que não muda a percepção de ninguém | **DESCARTADA em 12/09, por ausência de reclamação.** Ela existia só se o Raul ainda enxergasse a borda da atmosfera depois do `89ed004`. Passaram-se cinco dias em que ele olhou o site diariamente, reclamou do retângulo das estrelas, do risco no globo, do cargo e dos setores — **e nunca mais da borda**. O que sobrou é o degrau de 6,7% de Weber em 12 px já registrado, e perseguir isso custaria a primeira linha de GLSL do projeto. **Silêncio de quem olha todo dia é evidência**, e é a evidência que esta decisão pedia. | Descartada |
| **D-71** | 11/09 | **⚠ RISCO SILENCIOSO: existem cópias VELHAS dos documentos soltas em `Documents\Recomeço`, um nível acima do projeto. Elas saem.** E fica a regra do diretório de trabalho | Achado numa captura de tela do Raul em 11/09: o terminal do Claude Code estava aberto em `C:\Users\USUARIO\Documents\Recomeço` — ele mesmo respondeu *"not a git repo"* —, e a pasta ao lado mostra **`PRD.md` (24 KB, 02/09)**, **`WORKFLOW.md` (18,6 KB, 03/09)**, `CONTENT.md` e `RESOURCES.md` soltos na raiz. **O PRD de verdade tem 173 KB e vive em `portfolio-3d-v2\docs\`.** O que está solto é a **primeira semana do projeto**: descreve um site que não existe mais, sem modo escuro, sem domínio, com uma fila de dez passos que já foram todos executados; e o `WORKFLOW.md` solto é a versão de 520 linhas que a **D-47 substituiu por 112**. **Por que isto é pior que um arquivo duplicado comum:** alguém — pessoa ou sessão — abre o Claude Code naquela pasta, pede "leia o PRD.md", e recebe **um documento coerente, bem escrito e nove dias errado**. Não há erro, não há aviso, o build continua verde. É a mesma família do `background-color` no `<body>` (regra 5 da arquitetura) e dos 237 KB do RNF-02: **falha que não grita**. E o custo é maior aqui, porque o PRD é o que decide o que se faz em seguida. **Decisão: os quatro soltos saem da raiz** — `git rm` não serve, porque estão **fora** do repositório; é mover para `Recomeço\_arquivo-antigo\` ou apagar. O `portfolio-3d-v2.zip` e o `memoji.png` podem ficar, não são documentos de decisão e não competem com nada. **Regra que entra no `.claude/CLAUDE.md`, e é para o Claude Code escrever:** *o diretório de trabalho é sempre `portfolio-3d-v2`; se o `pwd` terminar em `Recomeço`, pare e avise, porque a pasta acima contém cópias obsoletas dos documentos.* **É a segunda vez que esta pasta causa problema** — a primeira foi o `npm run dev` falhando com `Missing script: "dev"`, que é o mesmo erro com sintoma barulhento. Agora ela apareceu com sintoma mudo. **ENTREGUE em 11/09, e a própria sessão pisou na armadilha uma vez.** Os quatro — `PRD.md` de 24 KB, `WORKFLOW.md` de 18,6 KB, `CONTENT.md` e `RESOURCES.md` — foram **movidos** para `Recomeço\_arquivo-antigo\`, não apagados, porque apagar é decisão do Raul. **Conferido antes de mover que nada os referencia:** nenhum caminho relativo de `src/`, `vite.config.ts`, `tsconfig*.json`, `index.html` ou `public/` sai da pasta do projeto — todos os `../` morrem dentro de `src/` —, e as únicas menções a `RESOURCES.md` no repositório inteiro estão no texto desta própria decisão. O `.zip` e o `memoji.png` ficaram, como a decisão previa. **A regra entrou no `.claude/CLAUDE.md` como seção própria, logo depois da lista de "documentos que mandam" e antes da Stack** — é ali que se decide qual PRD é o verdadeiro, então é ali que a dúvida tem de ser levantada. Entrou **com o motivo escrito**, não só a regra: a comparação entre a mordida barulhenta e a muda, e a frase de que falha barulhenta custa minutos enquanto falha muda custa a sessão inteira. **Achado da sessão, e ele vale mais que a arrumação:** ao mover os arquivos, o `cd` para a pasta de cima **mudou o diretório de trabalho da sessão para `Recomeço`** — ou seja, a armadilha desta decisão foi disparada por quem estava consertando-a, dois minutos antes de a regra existir. Voltou na hora e nenhum documento errado chegou a ser lido, mas fica registrado: **a conferência do `pwd` não é uma vez no começo da sessão, é a cada passo que sai da pasta** | Concluída |
| **D-72** | 11/09 | **O `<canvas>` do hero desenha o próprio fundo, e por isso o campo de estrelas aparece dentro de um RETÂNGULO visível.** A cena passa a cobrir o hero inteiro, com fundo transparente e as bordas sumindo | Reclamação do Raul: *"as estrelas no hero parecem estar dentro de um quadrado e a estética fica feia"*, com a sugestão de *"cobrir praticamente o hero todo com essas estrelas"*. **Confirmado na captura dele:** existe uma **aresta horizontal dura** onde o brilho roxo e as estrelas terminam, e abaixo dela a página fica chapada. Não é opinião, dá para apontar o pixel. **A causa é estrutural, não de gosto:** o WebGL pinta **dentro dos limites do canvas**, e o `Backdrop` põe brilho e partículas no fundo da cena. Onde o canvas acaba, o desenho acaba — no meio de um degradê, isso vira uma linha. **Duas saídas, e a segunda é a que o Raul pediu:** (a) canvas transparente, jogando brilho e estrelas fora do 3D — mata a caixa, mas mata também a profundidade, porque as partículas têm paralaxe e um `background` de CSS não tem; (b) **o canvas cobre o hero inteiro e o seu conteúdo decai até o fundo da página nas bordas** — o retângulo continua existindo, mas **deixa de ter aresta**, porque não há degrau onde ele termina. **É (b).** O desenho decai; o limite some porque não há salto, não porque não há limite. **O que precisa ser medido, e é o que impede isto de virar regressão:** o custo. O hero já é o LCP (`memoji.webp`) e o mobile está em **94** — canvas maior é mais *fragment shader* por quadro. **Aceite:** nenhuma aresta perceptível em nenhuma borda do hero, nos dois temas; **Lighthouse mobile não cai de 94**; e abaixo de 640 px nada muda, porque a cena continua não sendo montada (RNF-04) | **MEDIDA em 11/09, e a medição mudou o plano — por isso não foi implementada.** Três achados, e o primeiro derruba a minha descrição do problema. **(1) A aresta NÃO é um degrau de luminância.** Em trecho limpo o salto é de **1,19% de Weber** — praticamente invisível. Eu escrevi que era "uma aresta dura onde o degradê termina"; **não é o degradê, é o campo de estrelas cortado em retângulo**. O olho não vê um degrau de brilho, vê **partículas que acabam numa linha reta**. Isso muda o conserto: suavizar degradê não resolve nada. **(2) Aumentar o canvas mexe no enquadramento**, e o número está medido: o `fov` é **vertical**, então passar a altura de 567 para ~846 px aumenta o Memoji em **1,49×**. A compensação **não pode ser escala de grupo** — ela quebra para objetos fora do plano `z=0`, como o halo em `z=−1,2`; o certo é `position.y` da câmera, que é exato em qualquer profundidade. **(3) O aceite do Lighthouse já está satisfeito por construção:** a 412 px a cena **não é montada** (RNF-04), então a D-72 não tem como derrubar os 94 — o aceite continua escrito, mas não é risco. **Consequência da (1), e é uma saída melhor que a minha:** se o problema é o campo **cortado**, e não o fundo, então a primeira tentativa deve ser **fazer as partículas desaparecerem antes da borda** — opacidade caindo com a distância ao centro do canvas —, e **não** aumentar o canvas. Assim o campo perde a borda reta **sem tocar em enquadramento, sem risco de mexer no Memoji e sem `position.y` nenhum**. Só se, com a queda aplicada, o campo parecer pequeno demais, é que se aumenta o canvas — e aí a compensação já está estudada. **Ordem: queda de opacidade primeiro, medir, e só então decidir se cresce.** | **QUEDA DE OPACIDADE APLICADA em 11/09 — e a borda reta acabou.** Feita na ordem que o adendo mandou: **queda primeiro, canvas depois**, e o canvas **não** foi aumentado. **A queda é em espaço de TELA, e a escolha tem motivo geométrico, não preguiça.** Uma queda **radial em 3D** seria mais barata — dava para assar o fator uma vez só, porque `|p|` é invariante à rotação — e **não funciona aqui**: a nuvem é uma esfera e o canvas é um retângulo **2,5× mais largo que alto** (1440×567). Esfera inscrita no retângulo deixa os lados vazios; esfera que cubra os lados é **cortada em cima e embaixo**. Não existe raio que faça as duas coisas. Em NDC, `max(|x|,|y|)` é um **quadrado** — a forma exata do canvas —, então a partícula chega à borda **já em zero, por construção**, em qualquer proporção de tela. Custo medido do laço: **0,0092 ms por quadro** com 1000 partículas, ou **0,06% do orçamento de 60 Hz**, mais 11,7 KB de buffer de cor por quadro. **Medição da borda, que é o aceite:** na fronteira do canvas (y=279), o degrau de mediana entre a linha 278 e a 279 é **0,000000** no tema claro e **0,000429** no escuro. Não há aresta — e o maior degrau da janela fica em y=294, que é o **degradê CSS do `HeroHalo`**, contínuo por construção. Isso **confirma o achado (1)**: nunca foi degrau de luminância; era o campo cortado, e cortar deixou de acontecer. **Arquivo novo `three/Dust.tsx`:** a poeira saiu da `Backdrop.tsx`, que estava em 190 linhas, acima do teto da regra 8 — ficou em 152. **O que mudou de número:** `count` 380 → 1000 e `radius` 3,2 → 4,6, porque com a queda o campo precisa **exceder** o canvas para morrer dentro dele em vez de acabar antes. A cor continua vindo do material; o atributo de vértice carrega só o **fator em cinza**, então este arquivo não sabe que existem dois temas. **PENDENTE, e é decisão do Raul:** o campo agora cobre a **largura inteira**, mas o canvas continua ocupando **67% da altura do hero** — ele começa em y=279, abaixo do bloco do nome, então o terço de cima **não tem estrela nenhuma**. Se "cobrir praticamente o hero todo" exigir isso, aí sim o canvas cresce, e a compensação por `fov` e `position.y` já está estudada no achado (2) | **ENTREGUE em `1049e41`, e o canvas NÃO foi aumentado — decisão registrada.** A queda de opacidade é em **espaço de tela**, e a geometria não deixava alternativa: a nuvem de partículas é uma **esfera**, o canvas é um **retângulo 2,5× mais largo que alto**. Esfera inscrita deixa os lados vazios; esfera que cubra os lados é cortada em cima. `max(|x|,|y|)` em NDC é um **quadrado — a forma do canvas** —, então a partícula chega à borda **já em zero**. **Medido na fronteira (y=279): degrau de 0,000000 no tema claro.** Custo **0,0092 ms por quadro**, 0,06% de um quadro de 60 Hz. **A parte que ficou de fora, e é decisão e não pendência: o canvas continua ocupando 67% da altura do hero**, começando abaixo do bloco do nome — então o terço de cima não tem estrela. **Decidido NÃO crescer, por três motivos.** (1) O terço de cima é onde mora **"Raul Rodrigues"**, o texto mais importante da página: partícula atrás dele **compete** e derruba contraste, e a página inteira foi calibrada em contraste medido. (2) **A reclamação original era "as estrelas estão dentro de um quadrado", e ela está resolvida** — 0,000000 de degrau; "não tem estrela em cima" é outro pedido, não o mesmo. (3) O custo é real e o ganho não está medido: crescer exige compensação de `position.y`, faz o Memoji crescer **1,49×** e pede dois ou três ciclos de build e captura para afinar a borda nos dois temas. **Se um dia for feito, o estudo já está pronto** e está escrito acima — não se recomeça do zero. | Concluída |
| **D-73** | 11/09 | **O risco atravessando o globo VOLTOU a ser relatado — e o método de captura da rodada anterior pode ter produzido parte dele. Isolar com captura de UMA viewport, nunca de página inteira** | Relato do Raul depois do `89ed004`: *"o planeta terra ainda tá com um risco no meio"*. Na captura dele há **duas coisas diferentes**, e confundi-las custaria uma rodada: **(1) um arco fino e claro atravessando o globo de lado a lado**, e **(2) uma descontinuidade horizontal** onde o globo parece cortado e deslocado. **A (2) é fortemente suspeita de ser artefato da ferramenta, e o motivo é específico deste projeto:** pelo **D-19** o globo **cresce com o progresso da rolagem**, e uma captura de página inteira é montada em **faixas** — cada faixa pega o planeta num tamanho diferente, e a emenda vira um degrau. **Regra para a próxima medição: capturar UMA viewport parada, nunca a página inteira**, e só então afirmar que o degrau existe na tela. **A (1) é outra coisa e precisa de isolamento próprio.** A malha *wireframe* — que era a explicação natural para um anel — **saiu no `09e5abe`**, então sobrou um objeto que ninguém identificou ainda. **Não especular:** nesta mesma família de problemas eu errei três vezes seguidas — o mecanismo da atmosfera (disse acúmulo na silhueta, era disco chapado), e duas hipóteses sobre o assentamento que a medição derrubou. **Método obrigatório, o mesmo que funcionou no D-65:** perfil radial **e** perfil ao longo da linha suspeita, com o globo **parado e assentado**, capturando uma viewport só, desligando uma camada por vez — corpo opaco, continentes, marcador (incluindo o **anel de radar**, que pulsa a 2,2× e é o único objeto circular que sobrou na cena), atmosfera. **Dizer qual estado perde o arco antes de propor qualquer conserto.** **Relação com a D-70:** aquela decisão trata da **borda concêntrica** da atmosfera, que contorna o planeta; esta trata de um traço que **cruza** o planeta. São formas diferentes e podem ter causas diferentes — se a medição mostrar que é a mesma coisa, as duas se fundem e isso fica escrito. **MEDIDO em 11/09, e o arco tem nome: é a TEXTURA, não um objeto da cena.** **Instrumento:** Chrome 152 headless dirigido por CDP, **uma viewport parada de 1440×900 a dpr 1**, `captureScreenshot` **sem `clip`** — a regra que a D-65 deixou escrita. Calibrado antes de valer como prova: o bundle servido foi conferido como `index-BUkIrnqO.js`, o mesmo do build local (havia **outro** servidor ocupando a porta 4173, e medir nele teria sido medir o código de outra pessoa); o WebGL é real (ANGLE/SwiftShader, não fallback 2D); e a viewport saiu exatamente 1440×900. **(1) O ARCO EXISTE.** Está na tela, com o globo parado e assentado, **nos dois temas** — claro sobre continente roxo no tema claro, e com a polaridade invertida no escuro. Não é artefato de captura. **(2) O DEGRAU NÃO FOI REPRODUZIDO, e isto está escrito como "não reproduzido", não como "não existe".** Não há degrau na viewport parada; e a tentativa de reproduzir o artefato falhou: o `captureBeyondViewport` do Chrome 152 renderiza a página **numa passada só**, não em faixas, então este instrumento **não consegue** fabricar a emenda que a decisão suspeitava. A hipótese da ferramenta continua plausível e **continua sem prova** — o que se sabe é que o degrau não aparece aqui. **ELIMINAÇÃO, uma camada por vez, mesma cena e mesma rotação:** com a **atmosfera desligada** o arco **fica**; sem o **marcador inteiro** (anel de radar incluído) o arco **fica**; **sem os continentes** o arco **SOME**; só o oceano, **some**; só os continentes, **fica**. **O estado que perde o arco é o sem-continentes.** **O anel de radar foi eliminado duas vezes, e a primeira nem precisou de captura:** ele cresce `1 + t·2,2` sobre um raio externo de `0,044`, ou seja chega a **0,141** unidades de mundo contra **1,6** do globo — **8,8% do raio**, um círculo pequeno em volta de Dublin, geometricamente incapaz de cruzar o planeta de lado a lado. A captura confirmou. **CONSEQUÊNCIA PARA A D-70: as duas NÃO se fundem.** O arco sobrevive com a atmosfera desligada, logo não é a atmosfera; a D-70 trata da borda concêntrica da atmosfera e segue sendo outra coisa, ainda dependendo do olho do Raul. **O QUE HÁ NA TEXTURA** (`world-dots.png`, 2048×1024), medido linha a linha pela fração de pixels opacos contra a mediana da vizinhança: **três cicatrizes horizontais** — linhas **105–112** (71,5°N a 70,2°N) com **+32 pontos** de terra a mais que a vizinhança (risco escuro); linhas **128–141** (67,4°N a 65,1°N) com **−49 pontos**, ou seja terra **faltando** (é este o risco CLARO que o Raul vê); e linhas **604–606** (−16,3° a −16,6°) com **+52 pontos** (risco escuro, hoje escondido abaixo do horizonte visível na rotação assentada, mas que aparece com o globo girado). Há ainda um **quarto defeito, menor**: as linhas 0–35 (89,9° a 83,8°N) têm 0,1% de terra constante — uma coluna de ~2 px que no polo vira o **espigão radial fino** visível na captura. **PROVA DO MAPEAMENTO, e é o que fecha o caso:** pintei essas faixas na textura — vermelho em 105–112, verde em 128–141, azul em 604–606 — e renderizei. Os anéis vermelho e verde caem **exatamente** sobre o arco, e o verde traça a linha clara reclamada. Uma linha horizontal numa textura equirretangular é **um paralelo completo na esfera**, e um paralelo visto de viés é precisamente "um arco fino atravessando o globo de lado a lado". **MECANISMO, e ele já estava documentado ao lado:** o `public/images/README.md` registra que a rasterização foi feita por **varredura por linha com regra par-ímpar**. É a falha clássica desse algoritmo — quando a varredura passa **exatamente por um vértice** do polígono, a contagem de cruzamentos erra por um e **a paridade inverte do vértice em diante**, produzindo um risco que começa numa longitude e segue pela linha inteira. É a mesma família da armadilha (1) que aquela README já paga com a Antártida. **CONSERTO PROPOSTO, NÃO APLICADO** (a decisão manda dizer qual estado perde o arco **antes** de propor, e é o que se fez): reparar as **25 linhas** afetadas copiando das linhas limpas imediatamente acima e abaixo, preservando o formato documentado — PNG indexado de 1 bit, filtro None, deflate Z_FILTERED. São 25 linhas em 1024, e nas latitudes atingidas um preenchimento vertical de ~2,5° é imperceptível ao lado de um paralelo inteiro. **A alternativa é regerar a textura com o rasterizador corrigido, e ela tem um custo honesto: o gerador não está no repositório** — a README descreve o método, não o script. **Conferência obrigatória em qualquer dos dois caminhos, e ela já está escrita na README:** o pin de Dublin tem de continuar caindo sobre a Irlanda; se cair no mar, nem build, nem lint, nem teste acusam | **RESOLVIDA em 11/09, e a causa é a melhor descoberta do projeto: o arco NÃO é objeto da cena — é a TEXTURA.** Eliminação com o globo parado, uma viewport só, sem `clip`: o arco **sobrevive** com a atmosfera desligada, **sobrevive** com o marcador inteiro desligado, e **some** com os continentes desligados. O **anel de radar** caiu duas vezes, e a primeira nem precisou de captura: por geometria ele chega a **0,141 contra 1,6 de raio do globo — 8,8%**, incapaz de cruzar o planeta. **Não se funde com a D-70**, porque o arco existe sem a atmosfera. **O mecanismo, medido na imagem:** o `world-dots.png` tem **três cicatrizes horizontais** — linhas **105–112** (+32 pontos percentuais de terra), **128–141** (−49 pp, que é o risco claro que o Raul enxerga) e **604–606** (+52 pp). Pintadas de vermelho, verde e azul e renderizadas, **caem exatamente sobre o arco**. E o motivo é geométrico e vale guardar: **uma linha horizontal numa textura equirretangular é um paralelo inteiro na esfera** — um defeito de 13 pixels de altura vira uma faixa dando a volta no mundo. **A explicação estava escrita ao lado o tempo todo:** o `public/images/README.md` registra que a rasterização foi feita por **varredura por linha com regra par-ímpar**, e **vértice exatamente em cima da linha de varredura inverte a paridade dali em diante** — terra virando mar e mar virando terra até o próximo cruzamento. **Lição que atravessa o projeto:** o defeito nasceu na **geração do dado** (D-58a), não na renderização, e três rodadas de investigação olharam para a cena — material, geometria, camadas — porque o sintoma aparecia lá. **Quando o sintoma é visual, o dado também é suspeito.** **O conserto está PROPOSTO e não aplicado, como esta decisão manda:** reparar as 25 linhas copiando das vizinhas limpas, mantendo o PNG **indexado de 1 bit** (o caminho por canvas dá 155 KB contra 15,6 — já medido no D-56). O gerador original **não está no repositório**; só o método, no README. **Aceite:** as três faixas somem do perfil por linha, o arco some da tela nos dois temas, **o pin continua caindo sobre a Irlanda** (é o aceite do D-58a e regerar mexe em projeção) e o arquivo continua indexado de 1 bit. **Sobre o fenômeno (b), o degrau horizontal: NÃO reproduzido.** O `captureBeyondViewport` do Chrome 152 renderiza numa passada só, não em faixas, então este instrumento **não consegue** produzi-lo. Fica registrado como **não reproduzido, não como inexistente** — a distinção importa, porque a hipótese original era justamente de artefato de captura em faixas. | **CONSERTADO em 11/09 — e o conserto corrigiu o meu próprio diagnóstico.** Eu havia escrito "terra faltando em 25 linhas". **Está errado.** Um detector melhor — a diferença de pixels entre linhas **vizinhas**, cuja mediana em geografia real é **0,684%** — mostrou **uma fronteira de inversão total na linha 142: 98,10% dos pixels trocados**, e apenas **1,90%** de diferença contra o **inverso**. Os dois lados são complementares. Não é falta de terra: é **PARIDADE INVERTIDA**. Confirmado por 17 pontos de controle geográficos — nas linhas 125 e 131 a Sibéria e a Groenlândia liam **oceano** e o Mar da Noruega lia **terra**; nas 102 e 159, tudo certo. **O reparo são 43 linhas, não 25:** as **110–141** (70,6° a 65,1°N) estavam invertidas por inteiro e foram **invertidas de volta** — isso devolve a geografia **exata**, não é remendo; as **105–109** e **603–608** têm inversão **parcial**, com o trecho trocado crescendo ao longo da longitude (12% → 84% das longitudes), e essas 11 foram **copiadas das linhas limpas vizinhas**, porque ali não há inversão limpa para desfazer. **Medido:** pontos de controle errados **6 → 0** — o único que ainda acusava era **erro meu**, porque 66°N/20°W fica *dentro* da Islândia, não entre ela e a Groenlândia; maior salto entre linhas vizinhas **98,10% → 17,77%**, e o que sobra é a emenda das copiadas, não um paralelo. **O arco sumiu nos dois temas**, por captura. **Aceite do D-58a cumprido e verificado no pixel, não só na textura:** o pin cai sobre a **costa leste da Irlanda**. Formato preservado — PNG **indexado de 1 bit**, PLTE e tRNS idênticos, filtro None, `Z_FILTERED`: **15.634 → 15.329 bytes**. **O que NÃO foi consertado, de propósito:** as linhas **0–35** têm ~2 px de terra numa coluna só, que no polo vira um **espigão radial fino** — defeito separado, fora do aceite desta decisão, e **continua na tela**. **O gerador não foi corrigido porque não está no repositório:** se a textura for regerada, o bug da varredura volta inteiro — por isso a armadilha entrou como item **(3)** do `public/images/README.md`, ao lado das duas que já estavam pagas | **ENTREGUE em `9fa974c`, e o conserto corrigiu o próprio diagnóstico — vale mais que a correção.** A leitura era "terra faltando em 25 linhas". **Errada.** Um detector melhor — diferença entre linhas vizinhas, cuja mediana em geografia real é **0,684%** — achou uma **fronteira de inversão na linha 142**, com **98,10% dos pixels trocados** e apenas 1,90% na direção contrária. Os dois lados são **complementares**: não é terra faltando, é **paridade invertida** — exatamente o que a varredura par-ímpar do README previa, e a prova de que o mecanismo estava certo. Os pontos de controle geográficos fecharam a conta: **Sibéria e Groenlândia liam oceano, o Mar da Noruega lia terra.** São **43 linhas, não 25** — 110–141 invertidas de volta (geografia exata, sem inventar costa), 105–109 e 603–608 copiadas das vizinhas limpas. **Resultado:** pontos de controle errados **6 → 0**; maior salto entre linhas **98,10% → 17,77%**; **arco sumiu nos dois temas**; **pin cravado na costa leste da Irlanda**, conferido no pixel — o aceite do D-58a sobreviveu. Formato preservado, 15.634 → **15.329 bytes**. **Lição a somar à que já estava escrita:** o primeiro diagnóstico acertou **onde** (a textura) e errou **o quê** (falta, não inversão) — e a diferença não era acadêmica, porque "copiar das vizinhas" teria apagado geografia real em 32 linhas que só precisavam ser **invertidas de volta**. | Concluída |
| **D-74** | 11/09 | **O RNF-09 muda de estado, por escrito: a revisão do inglês deixa de exigir uma PESSOA fluente e passa a ser feita pelo Cowork. Entregue em `docs/REVISAO-INGLES.md`** | Decisão do Raul: *"não precisamos de um fluente"* — e, depois, o alvo dito com precisão: *"não precisa ser exatamente como o irlandês; estamos vindo do Brasil, eu tenho que fazer bonito, mas não precisa estar 100%"*. **Isto muda um requisito, então muda aqui e não por omissão** — a D-66 já tinha registrado que, se essa revisão caísse, ela cairia explicitamente. O RNF-09 dizia "revisado por ferramenta **e** por uma pessoa fluente" e era **o único item bloqueante do lançamento**. Passa a ser: **revisado pelo Cowork contra o texto que está em `src/data/`**, com a revisão escrita, item a item, com o motivo de cada mudança. **Por que isto é defensável e não um atalho:** o alvo mudou junto. Não se está buscando um texto que passe por escrito por um irlandês; busca-se **um texto que não denuncie tradução** — que é um problema de idiomatismo, e é justamente o que se pega lendo. O que continua fora do alcance está escrito no próprio arquivo: **convenção local do mercado de Dublin**, se alguma expressão soa datada ou corporativa demais ali especificamente. **Risco aceito, e é pequeno e reversível:** portfólio não é contrato — trocar uma frase depois de publicado custa cinco minutos, e o site já está no ar. **Resultado da revisão de 11/09, para o registro:** 11 pontos em todo o texto, dos quais **apenas 3 são erro de fato** — `dental` usado como nome de setor (é adjetivo, não existe "in dental"), `turns a task into under a minute` (transforma uma tarefa numa duração, a frase não fecha) e `the website` sem antecedente. Os outros 8 são frase que soa traduzida, com destaque para **`I work where things are ambiguous`**, que é literal de "trabalho onde as coisas são ambíguas" e aparecia **duas vezes**, sendo que a segunda — `"My job is to work where things are ambiguous"` — ainda era circular. **Um achado que não é de inglês e importa mais que os outros:** o card "This site" afirma **70 KB** de JavaScript, número anterior à D-62 e à D-63; o build de 11/09 dá **64,17 KB crus / 19,51 gzip** no chunk inicial e **138 KB** na carga completa. **Um portfólio que cita um número precisa que ele esteja certo — é o tipo de coisa que um entrevistador confere.** **O que NÃO foi tocado, e está escrito no arquivo:** `headline`, `aboutQuote`, as quatro linhas da Stack e as duas melhores frases do site | **APLICADA em 11/09 — os dez primeiros itens. O item 11 não, porque é decisão de conteúdo e está com o Raul.** Tudo em `src/data/`, sem uma linha de JSX tocada (RF-03). **Itens 1 a 9** entraram com a redação **escrita no pacote**, sem invenção. **Item 10 (contrações):** os exemplos citados foram corrigidos, e a varredura por formas não contraídas devolveu **exatamente duas** que sobreviveram — `The team is not slow, the process is` e `a portfolio that is itself a working product` —, que são precisamente as duas que o pacote manda **não tocar**. A regra e a exceção não se contradizem: bateram. Onde o pacote deu o princípio mas não a frase (`I have worked` → `I’ve worked`), a aplicação foi **mecânica**, e está dito aqui para não passar por invenção. **Conferências do D-66, todas rodadas depois da troca:** (a) **nenhuma frase alterada aparece no `<head>`** — `title`, `description`, `og:description` e o JSON-LD saem da `headline`, que não mudou, então nada a atualizar; (b) o `h1` continua lendo **"Raul Rodrigues"**, com espaço, ou seja o recorte do `WordReveal` está intacto; (c) o fio do M-25 continua existindo e atravessando a página — comprimento 5.647 → **5.618 px**, e a diferença é só o texto novo mexendo na geometria dos rótulos, que é o fio funcionando como projetado; (d) as **cinco ênfases seguem em pé**, uma por parágrafo, nenhuma perdida na troca. **Um número que precisa de contexto:** a largura de leitura mediu **82 caracteres por linha** — acima dos 74 do D-31 —, **mas o controle mostrou 82 TAMBÉM no texto antigo**, num parágrafo diferente (o `contactPitch`). **Não é regressão desta mudança.** O que existe é uma divergência entre este método de medição e o que produziu os 74/75 do D-31 e do D-52, e ela fica **registrada e não resolvida** — perseguir isso agora seria trocar o item do Raul por uma auditoria de método | **APLICADA em `decec99`** — itens 1 a 10 em `src/data/`. A varredura de contrações devolveu **exatamente duas formas não contraídas sobreviventes, e são precisamente as duas que a revisão manda não tocar** (`The team is not slow` e `a portfolio that is itself`), o que é a confirmação de que a regra 10 foi aplicada sem atropelar as exceções. As cinco ênfases do D-38 sobreviveram. Nada do que mudou aparece no `<head>`: a `description` e a `og:description` saem da `headline`, que não foi tocada. **Item 11 decidido pelo Raul e pelo Cowork — opção A, com a frase apertada:** `138 KB over the wire on a phone: the 3D bundle never downloads there. Fully usable with WebGL switched off.` **Por que A e não B nem C:** a métrica passa a ser **o que o visitante de fato baixa**, é a conquista da D-63, qualquer entrevistador confere no devtools em dez segundos, e **não envelhece a cada commit** — que é exatamente o defeito do `70 KB` atual. B anunciaria um número pior que o anterior sem explicar por quê; C cita nota de Lighthouse, que muda quando a ferramenta muda. **A frase original proposta tinha dois travessões seguidos e travava a leitura; os dois pontos e o ponto final resolvem.** | Concluída |
| **D-75** | 11/09 | **Um corretor gramatical leu o site e provou que os RÓTULOS das seções colam no texto: `ProblemJob seekers…`, `BuiltA static front end…`, `JavaScriptOpenAI APICloudflare Workers`.** O D-45 tinha aceitado isso; a evidência nova reabre | O Raul passou um corretor de gramática na página e colou o resultado. **A maior parte dos apontamentos é falso positivo — e o motivo dos falsos positivos É o defeito.** Itens como `workGet`, `ProblemJob`, `BuiltA` e `JavaScriptOpenAI APICloudflare` não existem no texto escrito: são **duas strings vizinhas sem separador no `textContent`**. O rótulo `PROBLEM` do card cola na primeira palavra do problema; as tags de stack colam umas nas outras. **Por que isto importa mais do que um corretor reclamando:** quem lê `textContent` não é só o corretor — é o **leitor de tela**, é o **Google**, e é **qualquer ferramenta que a pessoa do RH passar no site**. O D-45 varreu isto em 05/09, corrigiu o `"Dublin,Ireland"` e **decidiu deixar a fita da Stack e os irmãos separados por `gap`**, com o motivo escrito. **A decisão era defensável e a evidência nova a derruba:** naquela época era teoria sobre como uma ferramenta leria; agora **uma ferramenta real leu e errou**, e o dono do site viu o erro. **O conserto não é mexer no visual:** o espaço precisa existir **no texto**, não no CSS — separador invisível para o olho e presente para quem lê a árvore, no mesmo padrão que o D-45 usou no `"Dublin, Ireland"`. **Escopo a varrer:** rótulos dos cards de projeto (`Problem`, `Solution`, `Impact`), as listas de tags de stack dos cards e da seção Stack, a coluna de metadados do D-32 (`Marketing · Legal · Transport · Dental`) e a fita do Marquee. **Aceite:** copiar o texto da página inteira e colar num editor **não produz nenhuma palavra grudada**. **O que NÃO é defeito, e está na lista do corretor:** `"The team is not slow, the process is"` é vírgula de contraste, deliberada e boa — o corretor quer ponto e vírgula e está errado sobre o registro; `C++`, `C#` e os travessões estão certos | **ENTREGUE em 11/09, com o espaço no texto e zero mudança de visual — e a medição separou duas coisas que a decisão tratava como uma.** **O aceite escrito é "copiar a página inteira e colar", e ele JÁ PASSAVA antes do conserto.** Medido de verdade, selecionando `document.body` e lendo `getSelection().toString()`: os únicos trechos que o detector acusa são **`3D` e `n8N`**, falsos positivos do próprio regex. O motivo é que o **serializador da seleção insere quebra entre blocos** — ele não é o `textContent`. **O defeito real está no `textContent`, e é lá que ele importa**, que é exatamente o que a decisão argumenta: quem lê `textContent` é o corretor, o leitor de tela e o buscador. Medido: **79 trechos colados antes, 39 depois**. **O que foi consertado, o escopo pedido:** rótulos dos cards (`ProblemJob seekers` → `Problem Job seekers`), tags dos cards e da seção Stack (`pandasCode` → `pandas Code`, `assistantsAutomation` → `assistants Automation`), a coluna de metadados do D-32 e a fita do Marquee. Tudo pelo padrão do D-45 — **espaço explícito no texto, nunca no CSS**. **Visual conferido pixel a pixel, não no olho:** a seção Work deu **0 pixels diferentes em 1.296.000**; a Stack acusou 5.138, e o **controle derrubou a suspeita** — capturar o **mesmo build duas vezes** dá 5.632 pixels diferentes na **mesma faixa de 9 px** (y 725–733), que é a fita do Marquee animando. Diferença de relógio, não de código. **Espaço no fim de um trecho em linha não é desenhado** — ele fica pendurado no fim da linha —, e é por isso que isto não mexe em nada. **O que SOBRA e está fora do escopo desta decisão:** os 39 restantes são **junções entre blocos** — a nav (`AboutWorkStackContact`), os títulos de seção (`02 / WorkSelected work`), os botões do hero (`See the workGet in touch`), os títulos dos grupos da Stack (`AI & AgentsTurning language models`) e os rótulos da Location (`local time06:36 IST`). **A seleção do navegador separa todos eles**, então nenhum aparece ao copiar; só o `textContent` os cola. Limpar isso também é uma varredura maior e **não foi pedida** — fica registrado com a lista pronta. **Um achado de método que não é defeito:** `document.body.textContent` também devolve o conteúdo das tags `<style>` embutidas do hero, o que enche o detector de `@keyframes`. Não é texto do site e nenhuma ferramenta de leitura o consome | **ENTREGUE em `43f67c7`, e o Claude Code achou um erro NO MEU CRITÉRIO DE ACEITE — isso vale registrar mais que a correção.** Eu escrevi o aceite como *"copiar o texto da página inteira e colar num editor não produz nenhuma palavra grudada"*. **Medido: esse aceite já passava ANTES do conserto.** O motivo é técnico e eu não sabia: o serializador de `getSelection()` **quebra entre blocos por conta própria**, então ele nunca mostraria o defeito — ele não é `textContent`. **O defeito real estava onde a decisão argumentava, e não onde o aceite media:** no `textContent`, que caiu de **79 para 39 trechos grudados**. E `textContent` é justamente o que o corretor gramatical, o leitor de tela e o Google leem. **Ou seja: o argumento estava certo e a régua estava errada** — se ele tivesse seguido a régua ao pé da letra, teria fechado a decisão sem tocar em nada. **Regra que isto deixa:** um aceite precisa medir **a mesma coisa que o argumento afirma**; "selecionar e colar" e "ler o `textContent`" parecem a mesma medição e não são. **Resultado:** os quatro alvos consertados, e o visual conferido pixel a pixel — seção Work com **0 de 1.296.000 pixels diferentes**. A Stack acusou 5.138 e **o controle derrubou**: o mesmo build capturado duas vezes dá 5.632 na mesma faixa de 9 px — é o Marquee animando, não regressão. **Ficam 39 junções fora do escopo** (nav, títulos de seção, botões do hero), com lista pronta. | Concluída |
| **D-76** | 11/09 | **A largura de leitura tem DOIS números medidos por dois métodos, e o PRD não diz qual vale: 74/75 (D-39, D-52) contra 82 (11/09). Fixar o método** | Ao aplicar a D-74, o Claude Code mediu **82 caracteres por linha** e parou para checar se tinha causado regressão. **Não causou** — o controle no texto **antigo** também deu 82, num parágrafo diferente (o `contactPitch`). **Então o problema não é o texto, é o documento:** o D-31 e o D-39 registram um máximo de **74**, o D-52 registra **75**, e uma medição honesta hoje dá **82**. Três números para a mesma grandeza. **Um deles está errado, e enquanto não se souber qual, o critério não serve para aceitar nem para reprovar nada** — foi exatamente essa ambiguidade que fez uma sessão gastar tempo provando que não tinha quebrado algo que nunca esteve no valor declarado. **O que precisa ser fixado, e é uma escolha, não uma descoberta:** o que conta como "caractere por linha" — a linha renderizada de maior largura de **cada parágrafo** ou da **página inteira**; se entram espaços, pontuação e ênfase; e em **qual largura de viewport** se mede, já que o valor muda com ela. **O método do D-39 existe e funcionou** — foi ele que achou que o `ch` mentia —, então a saída é **reencontrá-lo e escrevê-lo ao lado do número**, não inventar um terceiro. **Enquanto isso, o 82 fica registrado como observação, não como reprovação.** **Custo real disto, para dimensionar:** nenhum leitor reclamou de linha longa; é dívida de documento, não defeito de produto. **Prioridade: baixa, e depois de publicar** | Aberta |
| **D-77** | 12/09 | **O CV e o site contam histórias diferentes, e o CV é a versão pior. Alinhar os dois — e o CV nem sequer traz o endereço do site** | Auditoria do `public/cv.pdf` contra o texto que está no ar, pedida pelo Raul junto com a atualização do LinkedIn. **Oito divergências, e nenhuma é de gosto.** **(1) O site não está no CV.** O documento lista LinkedIn e GitHub e **não lista `raulrodrigues.dev`** — o site foi construído para ser a prova de que ele entrega, e o CV, que é o que chega primeiro num processo, não aponta para ele. É o defeito mais caro da lista. **(2) Cargo em três versões:** o CV diz `AI Automation Developer`, o site diz `AI & Automation Developer`, e o LinkedIn é uma terceira. Quem procura o nome dele precisa achar **um** título. **(3) O perfil do CV é a versão corporativa do que o site diz melhor.** O CV abre com *"Automation developer specialising in AI-powered workflows that remove repetitive manual work"*; o site abre com *"I build systems that take repetitive work off people's calendars"* — mesma ideia, e a segunda é de quem fala, não de quem preenche formulário. **O CV deve herdar a voz do site, não o contrário.** **(4) O CV manteve `Ireland's tech ecosystem`** — exatamente a palavra que a D-74 tirou do site por soar de sala de reunião. **(5) Setores divergentes:** CV `marketing, legal, transport and dental sectors`, site `marketing, law, logistics and dentistry`. **(6) A experiência não tem um único número.** Três marcadores de "design and deliver", "build", "earned repeat business" — todos verbos, nenhum resultado. O site tem `Cuts a 30-minute writing task to under a minute` e `0.78 on Kaggle`. **Currículo sem número lê como afirmação; com número lê como fato.** **(7) `English — A2, currently studying`.** É a linha que mais pode custar entrevista em Dublin, e é provavelmente **desatualizada** — foi escrita em 03/09 e ele estuda desde então. **Não se inventa nível:** é preciso o nível real de hoje, e se for A2 mesmo, fica A2. **(8) Certificações ilegíveis para quem contrata:** `AI Automation — Bradesco`, `— Escola Guanabara`, `— Growth` não dizem o que foi cursado nem quem emitiu. **Também falta o próprio site como projeto** — ele tem engenharia real para mostrar (138 KB na carga do celular, 3D em chunk separado, uso sem WebGL) e está no site, não no CV. **Ordem correta, e importa:** o CV e o LinkedIn **saem do site**, não o contrário — o site é o texto que passou por revisão e por medição, e é o único dos três que está provado | **PARCIALMENTE ENTREGUE em 12/09.** Fechados: o **site no CV** (topo, em negrito), **cargo único** nos três lugares (D-78), o perfil herdando a voz do site, o `ecosystem` removido, os setores alinhados, **o primeiro número real da experiência** (os leads parados por duas semanas) e o próprio site como projeto. CV reescrito em **uma página, padrão europeu**. **Continuam abertos, e os dois dependem só de informação:** **(1) as três certificações** — o Raul não localiza os certificados, e `AI Automation — Growth` não diz nada nem pode ser verificado, então **saíram do CV** em vez de ocupar linha; voltam quando ele achar os nomes reais. **(2) O CAMP SBC.** Pesquisado: é o **Programa de Aprendizagem da Lei 10.097** — dois anos, formação teórica no CAMP mais **colocação em empresa parceira**. Ou seja, **é experiência de trabalho formal, não curso** — mas sem saber **em qual empresa** e **fazendo o quê**, ele só pode ficar em Educação. Com essa informação, o Raul passa a ter **dois blocos de experiência em vez de um**, que é o que mais pesa para quem ainda não tem diploma. | Aberta — depende de informação |
| **D-78** | 12/09 | **O site deixa de dizer "Developer". O cargo passa a ser `AI Solutions Engineer`, nos três lugares onde ele aparece — site, CV e LinkedIn** | O Raul já tinha decidido isto e **o site nunca soube**: os títulos que ele persegue são **AI Solutions Engineer** e **Growth Engineer**, e ele registrou explicitamente que **não quer "developer"** — quer negócio + IA, com projeto medido por impacto em receita e não por funcionalidade. O site inteiro foi escrito antes dessa virada e ainda se apresenta como `AI & Automation Developer`. **Escolhido `AI Solutions Engineer` como o título único**, com o lado de growth carregado no perfil e na disponibilidade. **Motivo da escolha, e é de mercado, não de gosto:** é o título que recrutador procura em Dublin; **`Growth Engineer` na Irlanda costuma cair em *martech*** e descreve menos do que ele faz. Um título só nos três lugares também conserta a divergência que a D-77 achou — hoje são três versões diferentes do mesmo cargo. **As trocas exatas, todas em `src/data/profile.ts` salvo indicação:** (1) `role` → `AI Solutions Engineer`; (2) `subheadline[0]` → `Self-taught, working where AI meets the business side. I do my best work where the problem isn't defined yet.` — a frase atual tem a palavra `developer` dentro dela, então trocar só o `role` deixaria o site se contradizendo dois parágrafos abaixo; (3) `availability` → `Open to AI Solutions and Growth Engineer roles in Dublin, and freelance projects`; (4) em `index.html`, o `<title>`, a `description`, a `og:description` e o **`jobTitle` do JSON-LD** — este último é o que o Google usa para dizer o que ele é, e é o mais fácil de esquecer porque não aparece na tela. **Aceite:** nenhuma ocorrência de `Developer` sobra no texto visível nem no `<head>`, e o `jobTitle` do JSON-LD bate com o `role`. **Ficou de fora de propósito:** acrescentar um quinto grupo `Growth` à seção Stack. Ele casaria com o CV novo, mas a seção é uma grade **2×2** e um quinto item a quebra — é decisão de layout, não de conteúdo, e fica para o Raul pedir se quiser | **ENTREGUE em 12/09 (`e4141a6`), e o Claude Code achou TRÊS lugares que esta decisão não listou.** Em produção: `<title>`, `og:title`, `jobTitle` do JSON-LD, `description` e `og:description` com o cargo novo; setores corretos na prosa e no rótulo; **zero ocorrências** de `developer`, `logistics` e `transport` no texto da página. **O que ele achou além da especificação, e cada um importa:** **(1) `og:title`** também dizia `AI & Automation Developer` e **não estava nas minhas quatro trocas** — é o título do card que aparece quando o link é colado no LinkedIn, ou seja, **exatamente onde o recrutador olha primeiro**. Publicar o cargo novo no site e o velho no card seria o pior dos dois mundos. **(2) `sectionMeta.about`** dizia `Marketing · Legal · Transport · Dental` — um **segundo** lugar com os setores, fora do parágrafo. E ali dentro sobrevivia o `Dental` usado como substantivo, **o mesmo erro que a D-74 mandou sair da prosa em 11/09 e que se escondeu no rótulo por um dia inteiro**. **(3) A seção 3 deste próprio documento**, a persona "Fundador / cliente B2B", listava `transporte` entre os setores atendidos — **seção viva, que define para quem o site escreve**, não linha de log. Ele corrigiu essa e **deixou as outras cinco menções intactas de propósito**, porque reescrevê-las apagaria o registro de onde o erro entrou. É a distinção certa entre documento que orienta e documento que lembra. **REGRA QUE ISTO DEIXA, e é a terceira vez que a mesma coisa morde:** quando um **fato** muda — cargo, setor, número —, **não se enumera de memória onde ele aparece: procura-se.** A D-74 tirou `dental` da prosa e ele sobreviveu no rótulo; a D-78 listou quatro lugares e existiam seis. **Um fato mora em mais lugares do que qualquer decisão consegue listar**, e a varredura leva segundos. **Achado que dói e fica escrito:** a **seção 2 deste documento diz, desde a primeira versão, "posicionar o Raul como AI + negócio, não como mais um dev front-end"**. O objetivo estava certo desde o começo; o site foi ao ar como `Developer` e ficou assim por nove dias. **Não foi falta de decisão — foi decisão escrita e não lida.** | Concluída |
| **D-79** | 12/09 | **Duas arestas que só aparecem onde as strings se encostam: o rodapé repete "AI Solutions" e o rótulo de setores quebra em duas linhas no celular** | Os dois foram achados pelo Claude Code depois da D-78, e nenhum é erro das strings — **as duas estão certas isoladas**. **(a) O rodapé.** O `Footer.tsx` concatena `role · availability`, e com os valores da D-78 sai: *"AI Solutions Engineer · Open to AI Solutions and Growth Engineer roles in Dublin, and freelance projects"* — **"AI Solutions" duas vezes na mesma linha**. É o tipo de defeito que nenhuma das duas decisões conseguiria prever, porque ele **não existe em nenhuma das duas pontas, só na junção**. **Conserto: um campo próprio**, `availabilityShort`, com `Open to roles in Dublin, and freelance projects`, usado **só pelo rodapé** — em vez de o rodapé recortar a string grande, que seria lógica de apresentação decidindo conteúdo. O hero continua com a `availability` inteira, onde o "Growth Engineer" precisa aparecer. **(b) O rótulo no celular.** `Law · Dentistry · Digital marketing · Retail` mede **392 px** contra ~342 px úteis numa tela de 390, então passa a ocupar duas linhas. **Não há transbordo horizontal** — é só uma linha a mais. **Conserto: `Digital marketing` → `Marketing` no rótulo**, e só nele: a prosa ao lado continua dizendo `digital marketing` por extenso, então a precisão não se perde — ela muda de lugar. **Por que vale corrigir algo que não quebra nada:** a coluna de metadados do D-32 existe para ser lida de relance; uma linha que dobra deixa de ser relance. **Nenhum dos dois é bloqueante** | **ENTREGUE (`c5a557d`, `67267e1`) — e a alínea (b) estava construída sobre uma MEDIÇÃO RUIM, que é o achado que fica.** **(a) O rodapé:** `availabilityShort` criado como campo próprio e consumido pelo `Footer.tsx`; confirmado em produção — *"AI Solutions Engineer · Open to roles in Dublin, and freelance projects"*, sem repetição. **(b) O rótulo:** voltou a uma linha a 390 px, com a coluna medindo 323 px úteis. **Mas o controle derrubou a premissa:** o rótulo **anterior** à D-78 (`Marketing · Legal · Transport · Dental`) **já ocupava duas linhas**. Ou seja, **a quebra nunca foi causada pela troca de setores** — o número que sustentava a decisão (339 px, "cabia em uma linha") veio de uma medição ruim, e a D-79 **herdou esse número sem questioná-lo**. O conserto é bom e melhora o estado anterior; **o motivo escrito estava errado**. **Por que isto importa mais que a correção:** é a primeira vez neste projeto que uma decisão nasceu de um número errado **em vez de** de uma hipótese errada. Hipótese errada a medição derruba; **número errado a medição confirma**, porque ninguém remede o que já foi medido. **Regra: um número herdado de outra rodada é dado de segunda mão — antes de virar justificativa de decisão, ele se remede.** **Varredura de `AI Solutions`:** oito lugares distintos, três na tela e cinco no `<head>`, **nenhum par encostado**. **E mais uma armadilha de instrumento, a quarta da série:** a busca precisou ser **insensível a caixa**, porque `innerText` respeita `text-transform: uppercase` e o `role` do hero é renderizado como `AI SOLUTIONS ENGINEER` — uma busca sensível a caixa teria "provado" que o cargo não estava no hero. | Concluída |
| **D-80** | 12/09 | **O CAMP SBC é formação certificada, não emprego — e o CV estava certo. Confirmado com o Raul antes de escrever** | Duas rodadas de perguntas quase produziram um erro grave. A pesquisa mostrou que o Programa de Aprendizagem da Lei 10.097 tem **contrato com carteira assinada**, e eu concluí que o Raul tinha **histórico formal de trabalho** em 2023–2024 — o que teria virado um segundo bloco de experiência no CV. **Ele corrigiu:** completou a formação, tirou os certificados de tecnologia e comunicação, **recusou a colocação CLT que o CAMP ofereceu** e foi estudar e trabalhar por fora. **Nunca houve empresa empregadora.** E a automação que ele descreveu — e-mail com IA, planilhas, WhatsApp B2B, chatbot, sites, leads — **é o freelance de 2025**, exatamente como o CV já dizia. **A lição de método, e ela quase custou caro:** eu ia escrever "empregado registrado em X" num documento que vai para empregador, apoiado numa **inferência a partir do regulamento do programa** — não em algo que o Raul tivesse dito. A pesquisa estava certa sobre o programa e **errada sobre ele**. **Regra: fato sobre a vida de alguém não se deriva de documentação — pergunta-se.** **O que mudou no CV:** a linha saiu de `Youth Apprenticeship` descrevendo o programa para **`Vocational Training in Technology`** descrevendo **o que ele recebeu** — formação certificada em tecnologia, comunicação empresarial e prática de trabalho. Fica em Education, que é o lugar certo. **Achado de brinde:** os certificados que ele não localizava são provavelmente **os do CAMP**, que são reais e rastreáveis — ao contrário dos três `AI Automation` soltos que saíram do CV | Concluída |
| **D-81** | 12/09 | **A seção de trabalhos passa a ter DOIS cards em destaque, não um. O órfão da grade some por construção — e a hierarquia passa a dizer a verdade do posicionamento novo** | O quarto card entrou (D-21) e o órfão previsto pelo D-14 apareceu, medido em produção a 1440 px: destaque em `col-span-2` na linha 1, dois compactos na linha 2, e **o card novo sozinho na linha 3 com 661 px vazios ao lado** — com a seção Stack começando logo abaixo, o que torna o vazio mais visível ainda. O Claude Code mediu e **não escolheu**, como mandado, e a observação dele fecha a análise: **reordenar o array não resolve, só troca quem fica órfão** — com um `span 2` mais três compactos numa grade de duas colunas, sobra um em qualquer ordem. E na ordem atual **o órfão é justamente o card mais importante do site**. **Decisão: dois destaques e dois compactos.** As linhas fecham — destaque, destaque, compacto + compacto — e **o órfão deixa de existir por construção, não por ajuste de ordem**. **O segundo destaque é o card novo, e o motivo não é geometria:** depois da D-78 o site se apresenta como **AI Solutions Engineer**, e em destaque devem estar os dois trabalhos que **provam que ele resolve problema de alguém** — o Cover Letter Assistant, que é público e dá para clicar e usar, e a automação para as quatro empresas, que é **paga e de cliente real**. Titanic e o próprio site descem para compactos: são credibilidade técnica, não prova de negócio. **Um `AI Solutions Engineer` que destaca um exercício de Kaggle e deixa o trabalho pago sozinho no rodapé da seção está com a hierarquia invertida** — a grade só tornou isso visível. **Isto revisa o D-14**, que fixou "um em destaque, dois compactos" quando existiam três projetos e nenhum era pago; a premissa mudou, a regra muda junto, e fica escrito que mudou. **O que MEDIR antes de fechar:** (1) o card sem `repo` e sem `demo` já foi medido como compacto e fecha bem — **em destaque ele é mais largo, e a ausência do bloco de links pode ficar mais aparente**; conferir e **dizer o que se vê** em vez de compensar por conta própria; (2) a seção fica mais alta, e o fio do M-25 é gerado da geometria dos rótulos, então ele se move de novo — conferir que continua atravessando; (3) a 390 px nada muda, porque a coluna já é única | **ENTREGUE em `55dbc84` — e das minhas duas previsões, AS DUAS estavam erradas, em direções opostas.** **O resultado:** três linhas, **zero espaço vazio** — destaque 1297 px, destaque 1297 px, dois compactos de 637. O órfão acabou. **O que eu escrevi errado, e ele corrigiu sem perguntar:** ligar as duas flags **não produz** o layout descrito. Com a ordem antiga do array as linhas saíam destaque / compacto+compacto / destaque — **sem órfão, mas com o trabalho pago ainda sendo a última coisa da seção**, que é exatamente a hierarquia invertida que esta decisão existia para consertar. **Eu descrevi o resultado visual e não o mecanismo**; o mecanismo exigia os dois destaques na frente do array. **Correção de leitura, de 13/09, e ela é do Claude Code:** eu classifiquei isso como erro meu que ele consertou, e ele devolveu com precisão — *"se a decisão tivesse dito só 'ligue as duas flags', eu teria ligado as duas flags e entregado a hierarquia invertida sem perceber"*. **Descrever o resultado esperado em palavras foi o que tornou o mecanismo verificável.** Não é o lado fraco da decisão: é o que a salvou. **Aceito, e vira regra de como escrever decisão aqui: dizer o RESULTADO em linguagem observável, não a lista de mudanças** — a lista de mudanças alguém executa sem conferir; o resultado alguém confere. E sobre as duas previsões falhadas, ele também repartiu o crédito com honestidade: a da altura era razoável e só caiu por coincidência aritmética; a de 390 px **ele tinha material para pegar antes**, porque havia medido aquele mesmo card a 390 px na rodada anterior e não pensou no que um badge novo faria com um título de duas linhas. Ele reordenou e conferiu que **as 37 strings continuaram idênticas** — a paranoia certa para um reordenamento. **As duas previsões erradas, e vale ver o formato do erro:** **(b) previ que a seção ficaria mais alta e o fio se moveria — não ficou**: 6.075 px, o mesmo número, as mesmas três linhas com as mesmas alturas (446 + 445 + 2 vãos de 24 = 1.361 px). **(c) previ que a 390 px nada mudaria — mudou 33 px**: o badge `FEATURED` é **novo neste card**, e com o título longo o cabeçalho vai de 28 para 89 px, porque o header é `flex-wrap`; o card cresce 583 → 616 px. Ele julgou que lê bem — badge como rótulo sob o título — **e disse que a previsão falhou em vez de deixar passar**. **A lição: previsão sobre consequência de layout é tão frágil quanto previsão sobre mecanismo**, e nos dois casos só se sabe medindo. Errei prevendo mudança onde não houve e estabilidade onde houve. **O card sem links em destaque: medido e aprovado.** Os dois destaques têm **753 px vazios à direita** — e isso é propriedade do `max-w-[30rem]` do `<dl>`, **anterior a esta mudança**, com que o Cover Letter convive desde sempre. A única diferença real é uma linha de ~24 px onde o outro diz `Live demo →`; a fileira de tags já fecha o card, e os dois terminam com os mesmos 33 px até a base. **Lê como escolha.** | Concluída |
| **D-82** | 12/09 | **Registrado e NÃO consertado: os cards em destaque têm 753 px vazios à direita. É anterior a tudo, e o conserto óbvio colide com o D-34** | Apontado pelo Claude Code ao medir a D-81, com o cuidado de separar o que era novo do que já existia: *"se há algo a criticar no destaque, é o vazio à direita dos dois — e isso é anterior a esta mudança"*. **O mecanismo:** o `<dl>` do card tem `max-w-[30rem]`, então num destaque de 1297 px o texto ocupa 480 px e **sobram 753**. Com **um** destaque isso passava como respiro editorial; com **dois**, é metade da seção de trabalhos. **Por que não estou consertando agora:** o conserto que primeiro ocorre é **levar o `impact` para a metade direita, grande** — e ele é justamente o conteúdo mais valioso do card, o número que prova o resultado. **Mas isso colide de frente com o D-34**, que fixou **um único número grande na página inteira**, com o motivo escrito: dois viram painel e o efeito se anula. Dois destaques com número grande seriam **três** números grandes na página. **As saídas reais, se um dia for feito:** (a) estreitar o card de destaque, deixando de usar a largura inteira — mexe na grade; (b) usar a metade direita para algo que **não seja número** — a fileira de tags, hoje embaixo, ou o setor do cliente; (c) aceitar o vazio como decisão e escrevê-lo como tal. **Hoje é a (c) por omissão**, e esta linha existe para que deixe de ser omissão. **Prioridade: baixa, e depois de publicar** — nenhum visitante reclamou, e o site está no ar há dias | **Adendo de 13/09 — o Claude Code trouxe um quarto caminho, e é melhor que os três que eu listei.** A saída (b) pedia "algo que não seja número" para a metade direita, sem dizer o quê. O candidato: **a fileira de tags de stack**, que hoje ocupa a largura inteira embaixo do card. **Por que ela serve e as outras não:** é **a única parte do card que não é prosa**, então **não briga com o `max-w-[30rem]`** — esse limite existe para proteger a largura de leitura do texto, e tag não se lê em linha corrida; não cria número grande nenhum, logo **não colide com o D-34**; e **não mexe na grade**, que era o custo da saída (a). O card novo tem quatro itens de stack e o Cover Letter tem três — material suficiente para ocupar a coluna sem parecer forçado. **Continua sem ser feito**, e continua depois de publicar; mas quando sair da fila, **começa por aqui.** | Aberta — depois de publicar |
| **D-83** | 13/09 | **Faxina do repositório: `EmptyState.tsx` apagado junto com os três ramos `length === 0` que o chamavam — e o registro de que eu mandei apagar pelo motivo errado** | A auditoria pediu "apague arquivos inúteis". Eu listei o `EmptyState` como **zero usos**; o Claude Code mediu e devolveu **três** — `About.tsx:28`, `Projects.tsx:16`, `Stack.tsx:13`. **A minha varredura rodou numa cópia estagiada e velha de `src/`**, e é a oitava falha muda do catálogo (§16). **Por que a remoção continua certa mesmo com a premissa errada, e é um motivo diferente do meu:** não é que ninguém use, é que **nenhum dos três usos pode acontecer**. Os arrays vêm de dados estáticos em `src/data/` com 15, 4 e 18 itens; para o ramo vazio rodar, alguém teria de esvaziar um arquivo de conteúdo. Não é código defensivo — é o andaime do começo do projeto, e o **próprio componente diz isso** no comentário: *"Apague este componente quando todas as seções estiverem prontas"*. **E ramo que nunca roda é ramo que ninguém testa:** ele não protege de nada e ainda envelhece com tokens e classes que mudaram três vezes desde que foi escrito. **O resultado observável** (e não a lista de mudanças, pela regra da v10.2): as três seções passam a renderizar a grade direto, sem ternário; `EmptyState` não existe mais em `src/`; `npm run build` passa; e a página a 1350 e a 390 px fica **pixel a pixel igual** — se mudar um pixel, a remoção estava errada | **Entregue — 4 arquivos, ~25 linhas a menos** |
| **D-84** | 13/09 | **`docs/AGENTES.md` sai de `docs/` e volta para o backlog: o documento descrevia três agentes em `.claude/agents/` que nunca foram criados** | `docs/` é contrato — descreve o que o projeto **é**. O `AGENTES.md` descrevia o que ele **não é**, e por isso é da mesma família da D-71 e da entrada 6 da §16: **um documento coerente, bem escrito e falso**. Aviso no topo conserta a leitura de hoje e não a de daqui a três meses, quando alguém contar quatro documentos vivos onde há três. **E havia duas verdades:** o `BACKLOG.md` já tinha, desde 11/09, a entrada sobre os agentes sugeridos pelo vaccari, com o motivo de adiar escrito — o mesmo assunto morando em dois arquivos é o que o cabeçalho da §14 existe para impedir. **Instalar os três agora foi descartado pelo motivo que o próprio backlog registrou:** agente paga em desenvolvimento contínuo, e a fila de código deste projeto está vazia. **Resultado:** o desenho foi para dentro da entrada de 11/09, o arquivo foi apagado, e o ponteiro para a versão antiga foi **testado**, não presumido — `git show 9788a5b:docs/AGENTES.md` devolve os 2.434 bytes | **Entregue** |
| **D-85** | 13/09 | **Faxina do repositório: `references/`, `public/models/`, `public/images/projects/` e o campo `cover?: string` apagados — 82 → 80 arquivos versionados, em seis commits (`9788a5b..5749c34`)** | **O achado mais caro não estava na lista:** o `README.md` novo — o que substitui o texto *"Este repositório é um esqueleto"* — estava **no disco e não commitado**. Eu havia relatado como entregue. Quem clicasse em *Code* no card *This site* do portfólio chegava no esqueleto. Commitado em `20cf3c0`, e virou a **entrada 9 da §16** com uma **quinta regra**: arquivo escrito não é arquivo publicado. **O campo `cover?: string` saiu junto das pastas de propósito:** deixá-lo faria o tipo prometer uma imagem que nenhum componente renderiza — tipo que mente é pior que pasta vazia. **Mantidos de propósito, e relatados em vez de editados:** `CONTENT.md:226`, que é tarja de histórico, e `PRD.md:671`, que lista as capas como item **opcional e não feito** — não é mentira, e se um dia for feito a pasta volta | **Entregue** |
| **D-86** | 13/09 | **Segundo corte do PRD: o *motivo* das decisões D-54 a D-73 vai para `DECISOES-ARQUIVADAS.md`, a linha fica** | A medição da faxina: o documento tem **258,3 KB** e as decisões **D-54+ ocupam 134,0 KB — 51,9% de tudo**. Média de **4.732 bytes cada**, contra **232** nas 53 arquivadas: **20×**. A D-73 sozinha tem 13.241 bytes, mais que as 53 antigas somadas. **O diagnóstico, e ele contraria a suspeita óbvia:** o PRD não incha por acumular decisões, incha por **decisões longas** — o que é bom quando se escreve e caro quando se lê, porque toda sessão futura paga o documento inteiro para achar o painel da §0. **Por que o corte de 07/09 funcionou e este deve repetir a mesma forma:** ele moveu o **motivo**, que é história, e deixou **ID, data, decisão e status**, que é contrato. Nenhuma referência do projeto quebrou porque nenhum ID sumiu. **A armadilha, e ela já me pegou uma vez:** na v9.4 eu fechei 15 status de uma vez sem conferir um a um e **fechei errado a D-21**, que nunca tinha sido entregue. Então a regra deste corte é: **uma decisão só é movida depois de o status dela ser lido**, e status nenhum muda no caminho — mover não é fechar. **Escopo fechado em D-54 a D-73**: são as entregues e fechadas do ciclo de setembro. **D-74 em diante ficam inteiras**, porque ainda dirigem trabalho ou são recentes demais para virar história. **Resultado observável:** o PRD abaixo de ~140 KB; toda referência a D-54..D-73 continua resolvendo no próprio PRD com a decisão e o status; e o `DECISOES-ARQUIVADAS.md` ganha o motivo de cada uma, sem reescrita — texto movido, não redigido | Aberta — depois de publicar, e é a última dívida do documento |
| **D-87** | 13/09 | **Varredura de português: nenhum texto visível para trocar. E o método da varredura vira o padrão do projeto** | Motivo do pedido: o `EmptyState` estava em português contra a regra 9 e **ninguém tinha visto em dez dias** — se um passou, era razoável supor mais. **Não havia mais nenhum.** Varrido pelos dois lados, que é o que torna o resultado confiável: **792 literais** em 49 arquivos (`src/` + `index.html`), comentários removidos antes; e **255 nós de texto e atributos no DOM renderizado**, incluindo `sr-only` — mais `aria-label`, `alt`, `title`, `placeholder`, `meta`, `lang`, o bundle em `dist/` e os arquivos públicos. **Os candidatos que sobraram são identificadores, não texto:** `'tema'` (chave do `localStorage`), `'tema:mudou'` (nome de evento), `'claro'|'escuro'` (tipo interno das cenas — o atributo no DOM é `data-theme="dark"`) e `mordida-do-tema` (id de `<mask>` SVG). **Por que ficam:** identificador é contrato com o código, não com o leitor; renomeá-los seria mexer em coisa funcionando para satisfazer uma regra que fala de **texto que o usuário lê**. **Dois achados de borda:** o `404.html` é uma superfície que varredura de DOM da SPA **nunca alcança** — o texto visível dele está em inglês, e o português que o scanner pegou estava dentro de um comentário HTML; e o `<html lang="en">` foi conferido. **O que fica como padrão:** *regra sobre o que o usuário lê se verifica no DOM renderizado, não só no código* — e o inverso também, porque o DOM não mostra o que não montou. Foi exatamente por isso que o `EmptyState` passou dez dias invisível: **ele nunca chegava ao DOM** | **Entregue — sem commit, porque não havia o que trocar** |
| **D-88** | 13/09 | **Aberta: o `aria-label` do botão de tema é fixo em `"Dark theme"`** | Achado pelo Claude Code fora do pedido, ao varrer atributos. **O que precisa ser conferido antes de consertar, e a ordem importa:** a frente 7 do D-44 registra `aria-pressed` alternando nesse botão. **Se o `aria-pressed` estiver lá, o rótulo fixo é o padrão correto** — pela prática do WAI-ARIA para botão de alternância, o rótulo nomeia a coisa e o `aria-pressed` carrega o estado; trocar o texto junto produz anúncio duplo e a ambiguidade clássica de o rótulo descrever ora o estado, ora a ação. **Se não estiver, é defeito de verdade:** o leitor de tela anuncia a mesma frase nos dois estados e a pessoa não sabe nem onde está nem o que vai acontecer — e aí o conserto é acrescentar `aria-pressed`, não mexer no rótulo. **Registrado sem conserto de propósito:** eu não consigo ler o arquivo daqui com confiança — minha cópia de `src/` é a mesma que produziu a oitava falha muda da §16, e ela não tem `aria-pressed` em lugar nenhum, o que é forte indício de estar velha e **nenhum indício sobre o código de verdade**. Conferir na árvore, não na cópia | **Verificada — sem defeito, e nada foi tocado.** O `aria-pressed` está em `ThemeToggle.tsx:70` e **alterna de verdade**, conferido em execução e não só no código: dois cliques no preview do build dão `true → false → true`, com `data-theme` acompanhando. A afirmação da frente 7 do D-44 está correta. **E o rótulo fixo também está certo — por um motivo melhor que o meu.** Eu sugeri, se fosse mexer, nomear o controle (`"Theme"`); o Claude Code mostrou que isso seria **pior**: *"Theme, botão de alternância, pressionado"* — pressionado o quê? **Nomear o estado que o botão liga é justamente o que dá sentido ao `aria-pressed`**, e `"Dark theme, pressionado"` não tem ambiguidade nenhuma. **A lição, e ela é dele sobre ele mesmo:** rótulo fixo só é defeito quando é a **única** coisa carregando o estado — avaliar o `aria-label` isolado da junção com o `aria-pressed` é o mesmo erro que a D-79 registrou de outra forma |
---

## 15. Backlog v2

Vive em `docs/BACKLOG.md`. Resumo do que já está lá: tema claro/escuro, PT/EN,
seção Serviços, estudo de caso por projeto, cena reagindo ao scroll, Memoji em
vídeo, notebook 3D com os projetos rodando na tela, formulário de contato,
certificados, blog técnico, fontes self-hospedadas.

**Depois da v1:** os projetos-âncora de AI (revenue-leak detector e o agente em
Python puro) entram como novos cards. O site vira a vitrine deles — não o
contrário.

---

## 16. Falhas mudas — catálogo

**Esta seção não é história. É a coisa mais reaproveitável que este projeto
produziu**, e existe porque o mesmo tipo de defeito apareceu **dez vezes** com
roupas diferentes.

**A definição:** uma falha muda não quebra nada. Não há erro, não há aviso, o
`build` fica verde, o lint fica limpo, e a ferramenta devolve **um resultado
bem-formado e falso**. Falha barulhenta custa minutos; falha muda custa a
sessão inteira — e às vezes custa uma decisão errada escrita com confiança.

| # | O que aconteceu | O que a ferramenta dizia | Como foi pego |
|---|---|---|---|
| 1 | `background-color` no `<body>` faz o **grão e o fio sumirem juntos** | build verde, lint limpo, zero erro no console | só olhando a página e sabendo que duas camadas vivem em `z-index: -1` |
| 2 | `three` e `r3f` **baixavam no celular** apesar do `React.lazy` correto | `npm run build` mostrava os chunks separados, como esperado | um critério novo que media **comportamento** (quem baixa) em vez de **artefato** (o chunk existe) |
| 3 | `clip` do `captureScreenshot` devolvia quadro **sem a camada WebGL** | uma captura perfeitamente uniforme — "não há aro nenhum" | calibrar o instrumento contra um resultado já conhecido |
| 4 | A emulação de viewport **não dispara `resize`** | `innerWidth` mudava, o componente não remontava — parecia defeito do código | instrumentar o instrumento: 0 eventos em 15 s |
| 5 | `innerText` respeita `text-transform: uppercase` | busca sensível a caixa devolvia **zero** para um texto que estava lá | repetir a busca insensível a caixa |
| 6 | Um `PRD.md` de nove dias atrás numa pasta acima do projeto | um documento **coerente e bem escrito**, e errado | conferir o `pwd` a cada passo que sai da pasta |
| 7 | Um número medido numa rodada anterior (339 px) virou justificativa | a medição "já tinha sido feita", então ninguém remediu | rodar o controle com o estado **antigo**, por hábito e não por obrigação |
| 8 | Uma varredura por usos de `EmptyState.tsx` rodou numa **cópia estagiada e velha de `src/`** e devolveu **zero usos** | uma busca que **terminou com sucesso** e não achou nada — nenhum erro, nenhum aviso de pasta errada | o Claude Code repetiu a busca **no repositório de verdade** e achou três usos; apagar só o arquivo teria quebrado o build |
| 9 | O `README.md` novo existia **no disco** e eu relatei como "escrito e commitado"; o GitHub continuava servindo o texto do esqueleto | o arquivo abria, o conteúdo estava certo, `Read` devolvia a versão nova | o Claude Code rodou `git status` na primeira verificação da faxina e achou o arquivo **não rastreado** |
| 10 | Um probe do Claude Code esperava `load` numa aba que **já tinha carregado**: o listener entrou depois do evento e a chamada ficou pendente **para sempre** | nenhum erro, nenhum timeout — só silêncio, como se a página fosse lenta | trocar a espera pelo evento por leitura de `readyState`, que é **estado** e não notificação |

**As três regras que saem daí, e elas se aplicam a qualquer projeto:**

1. **Instrumento novo se calibra contra um resultado conhecido** antes de valer
   como prova.
2. **Resultado que contraria o código é suspeito do instrumento antes de ser
   defeito** — sempre nessa ordem.
3. **Número herdado de outra rodada é dado de segunda mão.** Hipótese errada a
   medição derruba; **número errado a medição confirma**, porque remedir parece
   redundante — e é exatamente aí que ele passa.
4. **Busca que não achou nada é a mais fraca das provas.** Achar prova que
   existe; **não achar só prova que não achou ali** — e "ali" é a primeira coisa
   a conferir. Antes de apagar qualquer coisa por ausência de uso, a varredura
   se repete **na árvore de onde sai o `npm run build`**, nunca numa cópia.
5. **Arquivo escrito não é arquivo publicado.** Enquanto não há commit e push,
   o que o mundo vê é a versão anterior. Nenhum relato de entrega de documento
   vale sem `git status` limpo — o disco responde sobre o disco, não sobre o
   que está no ar.

**O padrão por trás das dez:** em todas elas, alguma coisa **respondeu com
sucesso** a uma pergunta ligeiramente diferente da que foi feita. O canvas
respondeu sem a camada WebGL; a busca respondeu sobre o texto original e não o
renderizado; o `build` respondeu sobre tamanho e não sobre download; e a
varredura da oitava respondeu sobre **uma cópia** de `src/`, não sobre `src/`; e a
nona respondeu sobre o **disco** quando a pergunta era sobre o que está
**publicado**.
**A pergunta que a ferramenta responde nem sempre é a pergunta que você fez.**

**A décima acrescenta uma forma nova, e vale separá-la:** as nove primeiras
**responderam errado**; essa **não respondeu**. Espera por evento é frágil porque
o evento pode já ter passado — e a espera não sabe distinguir "ainda não
aconteceu" de "aconteceu antes de eu chegar". **Estado se lê; notificação se
perde.** Onde houver as duas formas, lê-se o estado.

**E o adendo do Claude Code é a parte mais cara da décima:** o custo não foi
ficar pendente, foi que ele **esperou 21 segundos e depois mais 15 achando que
era lentidão**. As nove primeiras deram uma resposta falsa, e resposta falsa ao
menos chega e pode ser contestada. **Espera pendente parece progresso** — é a
única do catálogo que se disfarça de trabalho acontecendo, e por isso é a que
mais tempo consome antes de alguém desconfiar.

**Daí sai o sinal de alerta, e ele não é o que se costuma procurar:** não é
*"deu erro"* — é ***"está demorando mais do que deveria e eu ainda não sei por
quê"***. A armadilha é que **lentidão parece progresso lento**. Quando a espera
passa do esperado sem explicação, a suspeita correta não é máquina lenta: é
espera que nunca vai terminar.

E a oitava acrescenta uma agravante que as outras sete não tinham: ela não
apareceu numa medição de laboratório, apareceu **dentro de uma ordem de
apagar arquivo**. Falha muda em diagnóstico custa tempo; falha muda em
**instrução de remoção** custa o build — e teria custado, se o executor tivesse
obedecido em vez de conferir.
